"""Independent WSS-PINN research route.

The package is intentionally isolated from ``pipeline_wss_min`` and
``training_wss_min``.  Those directories are read-only upstreams.
"""

__version__ = "0.1.0"

