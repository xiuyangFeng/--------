---
description: 每完成一个任务或实验后，必须同步更新实验推进记录
alwaysApply: true
---

# 实验推进记录规范

## 写哪份日志

| 路线 | 推进记录 |
| --- | --- |
| V3P / V3D / 训练主线 / 通用代码 | `docs/02-推进与变更/代码修改与实验推进记录.md` |
| **WSS 独立线**（`pipeline_wss_min/`、`training_wss_min/`、`wss_mri_calculator/`、**`wss_pinn/` 当前主线**） | `docs/02-推进与变更/WSS最小化_代码修改与实验推进记录.md`（细节条目）；主日志只写一条指针/摘要 |

WSS 独立线细则见 [wss-min-log.mdc](wss-min-log.mdc) 与 `pipeline_wss_min/AGENTS.md`。**禁止**把 WSS 线详细条目写入 V3P 大日志。

## 通用规则

每次完成一个任务或实验后，**必须**在对应推进记录中同步更新；新增条目应写在文档**开头**（最新在上），不得在文末追加。

**滚动切卷**：两份推进记录已按区间切卷（主文件只留当前活跃区间；历史卷在 `docs/02-推进与变更/_archive/`）。主文件超过约 1500 行或跨季度时，把最旧月份整月切入新卷并更新文首历史卷索引；切卷只搬不改写。

## 记录内容应包含

- 完成日期
- 任务/实验编号与名称
- 主要改动内容（代码修改、参数调整等）
- 实验结果摘要（关键指标、是否符合预期）
- 遗留问题或下一步计划

## 执行时机

- 完成一个实验 run 后
- **用户要求分析实验结果时**（按 `.cursor/skills/analyze-experiment/SKILL.md` 全流程，含曲线对比与原因分析）
- 修改了核心代码逻辑后
- 完成某个任务清单中的一项后

在结束对话或完成任务前，提醒用户（或自行）在**对应**推进记录**开头**写入本次更新（V3P/主线 → `代码修改与实验推进记录.md`；WSS-min → `WSS最小化_代码修改与实验推进记录.md`）。
