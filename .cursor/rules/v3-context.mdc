---
description: V3 路线文档分层与口径；任务 A 实验/分析/训练时优先遵循
alwaysApply: true
---

# V3 路线上下文（当前主攻）

索引入口：`docs/01-任务/任务A/03-V3路线/README.md`（文首「当前状态」+「口径对照」必读）。

## 目录结构（2026-06-06）

```text
03-V3路线/
├── README.md                 ← 唯一导航入口
├── 00-当前主线/              ← 路径 G（当前主规划）
├── 01-执行与待办/            ← 待办、实验跟踪、V3P 执行计划
├── 02-历史路线/              ← A–F 历史规划
├── 03-数据与质量/
├── 04-代码诊断/
└── _archive/                 ← 只读；默认不打开
```

## V3P vs V3D（先判别再读文档）

| | V3P | V3D |
| --- | --- | --- |
| 前缀 | `V3P-` | `V3D-` |
| Split | `split_AG_v1` | `split_data_new_v3` |
| WSS 母版/带宽 | **AsymW-a 4957** · `wss_r2_wss≈0.399`（三 seed **0.394±0.005**） | WSS-01 post-4901 **0.243**（与 V3P **禁止混表**） |
| 压力参考 | test `r2_p` ~0.96 | test `r2_p` ~0.98 |

从 `config.snapshot.json` 的 `meta.exp_id`、split、`data_root` 确认；分析/对比不得跨列。

## 文档分层（按任务只读对应层）

### L0 · 每次必看（短）

1. `03-V3路线/README.md` — 当前状态一句 + 下一跳 + §2 路径传递
2. `01-执行与待办/V3_实验执行跟踪日志.md` — **顶部最新 1–2 块**
3. `docs/02-推进与变更/代码修改与实验推进记录.md` — **文首 3–5 条**

**日常三件套（P0）**：路径 G 主规划 · 待办 · 实验跟踪（均在 `00-当前主线/` 与 `01-执行与待办/`）。

### L1 · 分析/判 Go/No-Go 时

- `01-执行与待办/V3_后续优化待办.md` — 仅本次相关的 TODO 行 + 文首共识
- `V3P-G-*` → `00-当前主线/路径G_下一代架构与精度突破方案_2026-06-05.md` **§10**
- `V3P-F-*` → `02-历史路线/V3_发散优化探索路线.md` **§6**；再读 **§10 顶部一块**
- 母版对照 run：`experiment_index.csv` 查 4957/4999/5000 目录（不必读 V1 状态表「战略锚点」）

### L2 · 立项/排优先级时（分析结论已出再读）

- `00-当前主线/路径G_…md` — G0–G5、§9 执行顺序、§13 Q1–Q10
- `02-历史路线/V3_精度突破路径与发散方案.md` — 路径 A–E、执行顺序
- `01-执行与待办/V3P_后续优化执行计划.md` — V3P 六 Track（与路径 G 冲突以 G 为准）
- `02-历史路线/任务A_V3_PointNeXt双域WSS优先路线计划.md` — 改矩阵/阈值/约束时

### L3 · 仅当任务涉及时

- **三域数据 / denylist / split**：`03-数据与质量/V3_三域数据.md`
- **AG 单点 OOD**：`03-数据与质量/V3_数据质量与OOD诊断记录.md`
- **Trainer Bug**：`04-代码诊断/V3_代码问题诊断与修复计划.md`
- **历史 run 细节**：`_archive/`（不默认打开）

### 明确跳过

- V1 `A-Opt-05`、V2 WSSP、Line G/W 文档与图（除非用户明确做历史对照）
- 任务 A 总入口里「只看 V1/V2」阅读顺序
- 发散路线全文 §1–§9（无 F 立项讨论时不要通读）
- 路径 G §14 H 观察池全文（无 H 升级讨论时不要通读）

## 共享文档（V3 常用片段）

- **启动/环境**：`03-共享执行与状态/任务A配置与启动说明.md` — 只搜 `V3` / `field_v3` / `run_train_field`
- **填表**：`00-规范与记录/实验记录填写规范.md` §5.2（WSS / `test_metrics`）

## 分析实验时的回填（V3）

同步更新（各 **顶部**追加）：推进记录 → `01-执行与待办/V3_实验执行跟踪日志` →（`V3P-F-*`）发散 §10 → 待办对应行状态。详见 `analyze-experiment` 技能。
