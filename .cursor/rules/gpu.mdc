---
description: 训练任务使用GPU前须检查占用情况并与用户确认使用哪几张卡
alwaysApply: true
---

# GPU 使用规范

集群配备 4 张 GPU，训练任务启动前**必须**执行以下流程，不得跳过。

## 操作步骤

### 第一步：检查 GPU 占用情况
```bash
nvidia-smi
```
查看各卡当前显存占用、进程情况，判断哪些卡空闲。

### 第二步：向用户确认
将 `nvidia-smi` 输出结果展示后，询问用户：
1. 本次训练使用**几张卡**？
2. 具体使用**哪几张卡**（如 GPU 0、1、2、3）？

**在得到用户明确答复前，不得启动任何训练脚本。**

### 第三步：按确认配置启动训练
根据用户指定的卡号设置环境变量，例如用户指定 GPU 0 和 1：
```bash
CUDA_VISIBLE_DEVICES=0,1 python train.py ...
```
或在 SLURM 脚本中指定：
```bash
#SBATCH --gres=gpu:2
export CUDA_VISIBLE_DEVICES=0,1
```
