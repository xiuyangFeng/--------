---
description: WSS 独立线（pipeline_wss_min/training_wss_min/wss_mri_calculator/wss_pinn）代码/文档变更须同步更新专用推进记录
globs: pipeline_wss_min/**,training_wss_min/**,wss_mri_calculator/**,wss_pinn/**,docs/02-推进与变更/WSS最小化*.md,docs/02-推进与变更/WSS_PINN/**
alwaysApply: false
---

# WSS 独立线推进记录

涉及 **`pipeline_wss_min/`、`training_wss_min/`、`wss_mri_calculator/`、`wss_pinn/`（当前 `u,v,w,p` 主线）** 时，推进记录写入专用文档，**不得**与 V3P / 训练主线大日志混写。

## 必须更新的文档

`docs/02-推进与变更/WSS最小化_代码修改与实验推进记录.md`

- 新增条目写在文档**开头**（最新在上），不得在文末追加。
- 本文件已滚动切卷：只保留 2026-08 以来条目，2026-07 见 `_archive/WSS最小化_代码修改与实验推进记录_2026-07卷.md`；切卷规则见文首索引。
- V3P / 训练主线 / 通用变更仍写 `docs/02-推进与变更/代码修改与实验推进记录.md`；重要 WSS 线节点在主日志加一条**指针/摘要**条目即可。
- `WSS最小化_训练实验跟踪.md` 与 `WSS最小化_PointNet_baseline实验矩阵与进度跟踪.md` 已 🧊 冻结，**不再续写**；`wss_pinn` 实验状态写入 `docs/02-推进与变更/WSS_PINN/README.md`。

## 记录内容（每条至少包含）

- **本次主要修改**：代码、配置、QA 脚本、图件逻辑等
- **对应代码/文档**：具体路径；若有图件/审计 CSV 一并列出
- **推进到实验步骤**：推进到了哪一步（preprocess / global-stats / build-samples / 坐标 QA / 训练前验证等）
- **当前状态判断**：结论、是否符合预期、遗留问题或下一步

建议标题：`## YYYY-MM-DD｜主题 · 动作 · 状态`

## 执行时机

- 修改 `pipeline_wss_min/` 下代码、配置或集群脚本后
- 更新 WSS 最小化相关文档（交接记录、工程化注意事项、`pipeline_wss_min/README.md` 等）后
- 完成坐标 QA、STL-点云重叠检查、配准口径变更或批量 preprocess 后
- 用户要求分析/复盘 WSS 最小化实验或 QA 结果时

## 上位入口（按需只读）

- `pipeline_wss_min/README.md` · `wss_pinn/README.md`
- `docs/02-推进与变更/WSS_PINN/README.md`（当前主线真源）
- `docs/02-推进与变更/_archive/WSS最小化/WSS最小化预处理流程_搭建与交接记录_2026-07-07.md`（已归档）
- `docs/README.md` 中 WSS-only 最小化导航

任务结束前，在 `WSS最小化_代码修改与实验推进记录.md` **文首**写入或提醒用户写入本次更新。
