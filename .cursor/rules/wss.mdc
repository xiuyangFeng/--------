---
description: 除非用户明确要求 WSS 对比，禁止主动运行或建议执行 WSS 相关脚本
alwaysApply: true
---

# WSS 对比与脚本运行约束

壁面剪切应力（WSS）相关的对比、评估、出图或批量作业往往耗时且占用集群/GPU 资源。

## 必须遵守

- **除非用户在当前对话中明确要求**（例如明确说「帮我做 WSS 对比」「运行 WSS 脚本」「提交 WSS 的 slurm」等），**不得**：
  - 代为执行、撰写可直接运行的 WSS 对比命令或脚本调用；
  - 主动建议或安排 WSS 批量运行、对比实验的作业提交。

## 允许协助的范围（在未明确要求 WSS 对比时）

- 解释 WSS 指标含义、代码路径、配置字段含义；
- 阅读、审阅或修改与 WSS 相关的代码（纯代码层面，不代跑）；
- 回答「如何实现 WSS 对比」的设计问题，但不代替用户触发实际运行。

若用户意图模糊（仅提到 WSS 但未说要跑对比），**先询问**是否需要进行 WSS 运行或对比，得到肯定答复后再涉及执行类操作。
