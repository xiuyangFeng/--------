# 代码修改与实验推进记录

> 用途：持续记录每次代码修改补充了什么，以及当前推进到实验总纲的哪一步。
> 上位文档：[实验设计总纲](../实验设计总纲.md) / [任务A文档总入口](../01-任务/任务A/README.md) / [任务B指标计算规范](../01-任务/任务B/任务B指标计算规范.md)
> **滚动切卷**：本文件只保留 2026-07 以来的条目；更早条目见历史卷
> [2026-03至06卷](_archive/代码修改与实验推进记录_2026-03至06卷.md)。主文件超过约 1500 行或跨季度时，把最旧月份整月切入 `_archive/` 新卷并更新本索引。

## 2026-09-04｜WSS_PINN V4 / 非 PINN 文档与工作簿口径对齐

**本次主要修改**：确认体域 PINN V1/V2 当时采用的非 PINN 架构锚点为 PointNet P2V /
纯 PointNet++ D2 `c125-k128`；用户选择 formal Centerline-V2 V4 恢复这两个骨干。旧
BC/RCR V4 仍按实际约 250k 容量配平简化 PN/PNPP 归档。同步区分 WSS-min V4、
field-v4、BC/RCR V4 与 formal V4，并修正采样、near-wall、DATA/BC、formal
172 例 train138/test34 和历史 173 例 train138/test35 的状态说明。重绘近壁三图的标题，
使图件脱离 README 后仍明确是旧 matched-v1.2 采样；同时修复 PINN 工作簿主表和两个
V4 sheet 的打印区、横向与单页宽设置，确保 V4 第 19–34 行进入 PDF。

**对应代码/文档**：详细文件与证据见
[WSS 专用推进记录](WSS最小化_代码修改与实验推进记录.md)文首；核心入口为
[WSS_PINN README](WSS_PINN/README.md)、[V4 设计](WSS_PINN/WSS_PINN_V4大重构设计方案_2026-08-08.md)、
`wss_pinn/README.md`、[实验设计总纲](../实验设计总纲.md)及两份汇报工作簿。本次新增
`docs/03-汇报材料/tools/align_v4_backbone_lineage_workbooks.py`，只改 OOXML 描述单元格并
保留图表/公式；近壁证据与 PointNet/PointNet++ 同病例图见
[`V4_近壁采样核查_2026-09-04`](../03-汇报材料/V4汇报/V4_近壁采样核查_2026-09-04/README.md)。
已用 `build_v4_report_singlefile.py` 重新生成含 15 张内嵌图的 V4 单文件 HTML。

**推进到实验步骤**：完成正式骨干与历史结果的文档冻结；未修改训练代码、配置、数据、
checkpoint 或作业，尚未把 P2V/D2 落实到 formal V4 实现。

**当前状态判断**：formal split=train138/test34，正式训练继续
`training_ready=false / No-Go`；旧 48-run 与新 formal V4 必须分表、分路径报告。

## 2026-09-03｜磁盘清理：删除 ILO 队列 Fluent 逐步导出残留（释放约 1.17 TB）

**本次主要修改**：只删数据、不改代码。`/public` 使用率 98%（可用 1.1T）触发全项目占用审计：
项目 5.0T，其中 `data_new` 4.4T（ILO 2.5T / AG 1.2T / AAA 713G）、`external_baselines` 575G。
本次只处理"第一类"纯冗余——Fluent 作业逐步导出的全体积 ASCII，因 `fluent.slurm` 末尾清理段
未执行而残留；流水线固定窗口 1120–1280 隔步 81 帧（`ascii` 壁面目录定义步集），从未读取这些文件。
- 病例根目录残留 `<name>-0001…1280`：`ILO/LU_FU_SHAN-0/before`、`ZHANG_JIN_CHUN-1/before`、
  `WANG_JIN_MING-0/after`（文件名 `WANG_JING_MIN-*`）、`LI_SHENG_WEN-0/after`（0001–0457）全部删除；
  `XUE_YOU_TANG-0/before` 只删窗口外 1199 个，**保留 81 个窗口内文件**（见下）。合计 5496 文件 / 522.5 GB。
- `ascii_in` 塞满 1280 步：`ILO/DONG_KE_QIN-0/{before,after}`、`GUO_AI_JUN-0/{before,after}` 删除
  <1120 与奇数步，4796 文件 / 644.8 GB，四例 `ascii_in` 恢复为标准 81 帧。
- 删除前逐例核验：`ascii`/`ascii_in` 步集 = {1120,1122,…,1280}；根目录副本与 `ascii_in` 同步文件
  逐字节 cmp（LU_FU_SHAN、ZHANG_JIN_CHUN 81/81 相同；WANG_JIN_MING 74 相同、7 个仅并行分区行序与
  末位数值差异，坐标集/压力范围/vmax 一致）；无 Slurm 作业占用。
  清单：`data_new/pipeline_reports/cleanup_stray_fluent_exports_20260903.txt`。

**发现的数据问题（未处理，待决定）**：
- `ILO/XUE_YOU_TANG-0/before`：2026-01-16 原始跑的壁面 `ascii` 81 帧压力/WSS 全为 ~1e-13（死导出），
  `ascii_in` 为该次体数据；2026-01-26 重算为不同物理（vmax 0.52 vs 1.01、压力抬高约 600 Pa），
  只有 step 1120 的壁面落入 `ascii/`，其体数据 81 帧现保留在病例根目录 `XUE_YOU_TANG-1120…1280`
  （约 6.4 GB），是该次重算唯一存留。当前 `ascii`（80 帧死 + 1 帧重算）与 `ascii_in`（原始跑）不同源；
  若要救回此例需补壁面导出或整体重跑。
- `ILO/GUO_AI_JUN-0/before` 壁面全零（processed 只有 logs）、`GUO_AI_JUN-0/after` 仅 step 1120 壁面
  有效，与 2026-07-10 审计"暂排除 19"一致；2026-05 重算的体数据只剩窗口外文件（已删），窗口内未被采用。
- `DONG_KE_QIN-0/{before,after}` 壁面为 2026-05-19 重导、体数据为 2026-01-14：step 1200/1280 压力
  范围一致，可视为同一物理。

**推进到实验步骤**：数据存储维护，不影响 V4 流程；所有 `ascii`/`ascii_in` 窗口内文件、
`features`/`graphs` 均未触碰。

**当前状态判断**：`/public` 可用 1.1T → 2.3T（95%）。第二类可再生派生产物约 1 TB
（CROWN 合并 pkl 303G、`pkl/partial` 283G、`processed/normalized`+`coord_normalized` CSV 278G、
AG V3 图变体 68G、`training_wss_min/runs` 预测导出 29G、`outputs/wss_pinn` jsonl 可 gzip 18G、
`env_packages` 解压副本 5G）待用户拍板后再处理；`ascii_in` 3.1T 为 V4 现役输入，长期方案是
改 `raw_io` 支持 `.gz` 后整体压缩（实测 gzip -1 压 2.4 倍）。

## 2026-09-03｜WSS_PINN V4 正式重建前剩余整改与验收计划

**本次主要修改**：新增 V4 正式重建前剩余整改文档，确认八例低压力和 ZHOU/ZUO 原 Q
长尾已在 raw 侧修复，剩余主阻断改为曲率 feature atlas、全链 anatomy-only、解剖切面
BC、train138 条件长尾、ZHOU 收敛、raw SHA 和正式 bundle/Gate 重建。

**对应代码/文档**：详见
[WSS 专用推进记录](WSS最小化_代码修改与实验推进记录.md) 文首和
[剩余整改与验收计划](WSS_PINN/WSS_PINN_V4正式重建前剩余整改问题与验收计划_2026-09-03.md)；
同步实验总纲、WSS_PINN/项目索引及 `wss_pinn/{README.md,AGENTS.md}`。本次无代码、数据、
配置或作业变更。

**推进到实验步骤**：完成正式 Centerline V2/anatomy-only cutover 前的执行合同冻结，尚未
生成新 bundle 或 stats。

**当前状态判断**：旧 2026-08-31 staging 继续只作中间证据，`training_ready=false`；正式
训练 No-Go。

## 2026-09-02｜V4 index 5/7/8 后处理可视化包 + field-v4 诊断归档

**本次主要修改**：为 V4 矩阵第 5/7/8 条各导出速度 R² best/worst 的 ParaView 面片/点云包；
将已完成的 field-v4 核心诊断移入 `WSS_PINN/_archive/`。

**对应代码/文档**：详见
[WSS 专用推进记录](WSS最小化_代码修改与实验推进记录.md) 文首；产物
`outputs/wss_pinn/audits/v4_speed_r2_postview_20260902/`。

**当前状态判断**：可视化包可直接在 ParaView 打开；正式指标未改。现行执行入口仍是
V4 设计方案，不是已归档的 08-05 诊断文档。

## 2026-08-31｜WSS_PINN Centerline V2 173 例 staging 重建与训练护栏

**本次主要修改**：新增统一 Centerline V2 geometry/registration 与 rawfull builder，修复
steady/transient 半径/径向比值和曲率合同漂移；从完整 raw peak/81 帧重建 173 例，并补回
inlet-wall rim buffer、near-wall/core region、split 集合/resume/pilot/array audit 和
`training_ready=false` 训练硬阻断，并把 dataset content snapshot 绑定到 checkpoint/resume/evaluate。

**对应代码/文档**：`wss_pinn/v4/geometry_v2.py`、`build_centerline_v2.py`、`data.py`、
`evaluate.py`、volume tests；详细证据见
[WSS 专用推进记录](WSS最小化_代码修改与实验推进记录.md) 和
[173 例审阅与修复计划](WSS_PINN/WSS_PINN_V4_173例训练数据数值与刚性配准审阅及修复计划_2026-08-30.md)。

**推进到实验步骤**：173/173 staging Gate 通过；5,182 数组、11.1222 GiB，全数组审计
0 错误；steady/transient 默认 5k loader 各遍历 173 例，39/39 volume 测试通过。

**当前状态判断**：`staging_pass` 不等于训练授权。当前仍为 `training_ready=false`，须继续
处理 8 例压力低值簇、raw frame content SHA、速度/BC 长尾、4 例 optional outlet monitor
标签和正式 route/config/output/preflight。本次未新增训练、job 或 checkpoint 变更。

## 2026-08-30｜WSS_PINN V4 173 例训练数据审阅与修复计划

**本次主要修改**：只读核对实际训练数据的数值、Centerline V2 切换、刚性注册、几何通道、
压力、边界和 loader/resume Gate，新增
[审阅与修复计划](WSS_PINN/WSS_PINN_V4_173例训练数据数值与刚性配准审阅及修复计划_2026-08-30.md)。
本次未修改代码、数据、配置、checkpoint、作业或训练状态。

**对应代码/文档**：同步更新 [WSS_PINN 当前入口](WSS_PINN/README.md)、
[V4 设计方案](WSS_PINN/WSS_PINN_V4大重构设计方案_2026-08-08.md)、
[docs 索引](../README.md) 和 [WSS 专用推进记录](WSS最小化_代码修改与实验推进记录.md)。

**推进到实验步骤**：完成 Centerline V2 下游独立重建前的数据 No-Go 审核；现有矩阵仅作
旧注册帧历史 screen。

**当前状态判断**：新正式训练 **No-Go**。须从完整 raw 数据重建 173 例、统一几何与边界
合同、查清压力低值簇、重算 train-only stats 并通过新硬 Gate；旧 checkpoint 不得续训新数据。

## 2026-08-30｜VMTK 中心线修复方案执行完成并归档

**本次主要修改**：依据 2026-08-28 全队列结果复核原方案完成状态。Centerline V2
源中心线提取、自动修复、硬/软 Gate、逐例渲染和 173 例全队列 QA 已完成；将原方案移入
`02-推进与变更/_archive/`，标记为“已执行归档”，并保留尚未进行下游 bundle/sidecar
重建与正式 cutover 的边界说明。

**对应代码/文档**：[已执行归档方案](_archive/VMTK中心线修复与全队列优化方案_已执行_2026-08-27.md)、
[Centerline V2 全队列修复与切换记录](Centerline_V2全队列修复与切换记录_2026-08-28.md)、
[归档索引](_archive/README.md)和 [docs 索引](../README.md)。本次未修改代码、配置或数据产物。

**推进到实验步骤**：Centerline V2 版本化源产物与全队列 QA 阶段已正式收口；当前可追溯
产物仍为 `outputs/centerline_v2_full_173_20260828/`，下游数据重建和训练输入切换尚未开始。

**当前状态判断**：原方案不再作为当前执行入口，后续以全队列修复与切换记录为真源。
20 例软复核继续保留标记，但不构成硬失败。本轮仅处理文档生命周期，不改变实验路线，
不更新 `docs/实验设计总纲.md`。

## 2026-08-30｜WSS_PINN V4：`12210_{46,47}` 改到 node04 直启

`12210` 排队原为 `16-17,46-47`，不是只剩 46/47。已 `scancel 12210_{46,47}`，
在 node04 GPU0/1 全新直启 `V4-TR-PNPP-BC-PDE-F-s3456` /
`V4-TR-PNPP-BC-PDE-EMA-s3456`。细节见
[WSS 最小化推进记录](WSS最小化_代码修改与实验推进记录.md) 文首。

## 2026-08-30｜`outputs/field` 可再生性清理与归档完成

**本次主要修改**：完成 `outputs/field/` 盘点、配置/checkpoint 兼容性检查、
当前图与历史图数值重算。新增去重 legacy snapshot 格式与加载器，将 17 例 /
1377 帧历史输入固化为约 0.68 GiB；6/6 旧 checkpoint 重算通过。删除 14 个大型
prediction 目录的 17,820 个 `.pt`，共 79.987 GiB，`outputs/field` 从约 82.98 GiB
降到约 3.7 GiB。同时删除 4 个空目录，归档 8 个失败启动和 54 个 legacy run。

**对应代码/文档**：新增 `training/core/legacy_snapshot.py`、
`training/scripts/build_field_legacy_snapshot.py`、`cleanup_field_predictions.py` 及回归测试；
`predict_field.py` 新增 `compact/full`、`--legacy-snapshot`、`--max-samples`。长期口径见
[预测产物保留与可再生性规范](../00-规范与记录/field预测产物保留与可再生性规范.md)；已执行方案移入
[_archive](./_archive/outputs_field冗余审查与可再生性清理方案_已执行_2026-08-30.md)。

**推进到实验步骤**：历史任务 A / V3 `field` 产物清理完成；原 prediction
manifest、checkpoint、config、summary、history、evaluation 保留，旧图资产可通过 snapshot 重算。

**当前状态判断**：清理目标完成，且预测入口已默认 compact，防止再次膨胀。
本轮不改变实验结论或当前 `u,v,w,p` 主线，不更新 `docs/实验设计总纲.md`。

## 2026-08-28｜Centerline V2 173 例全队列修复完成

**本次主要修改**：实现并全量运行 Centerline V2：冻结权威 STL/单位，按 CFD inlet/四出口
边界匹配开口，增加三轮 VMTK 定向重试，重建显式无环图和 4 条 root→outlet 路径，逐路径
计算切线/曲率/扭率/半径导数等特征，并增加硬/软 Gate、保守终末尖刺剪除和安全长边细分。
全量结果为 173/173 硬 Gate 通过、153 `pass`、20 `pass_review`、0 错误；3 例由第 3 轮
clean + 种子内移修复，37 例共剪除 41 条终末尖刺，`SUN_SHU_MING` 的 1 条 13.876 mm
腔内稀疏长边安全细分后通过。最终逐例核验 173 份 STL 哈希、图/路径 VTP、422,388 行 CSV、
692 张病例图和 9 页总览图，错误数 0。

**对应代码/文档**：新增 `tools/centerline_v2_pilot.py`、`tools/centerline_v2_batch.py`；
新增 [Centerline V2 全队列修复与切换记录](Centerline_V2全队列修复与切换记录_2026-08-28.md)
和 [`outputs/centerline_v2_full_173_20260828/README.md`](../../outputs/centerline_v2_full_173_20260828/README.md)；
同步更新 [原优化方案（现已执行归档）](_archive/VMTK中心线修复与全队列优化方案_已执行_2026-08-27.md)、
[`pipeline/README.md`](../../pipeline/README.md)、[根 README](../../README.md) 与 [docs 索引](../README.md)；
根 README 同时移除已不存在的 `legacy/` 入口和命令。

**推进到实验步骤**：完成 Centerline V2 源中心线/路径特征的版本化生产、自动 Gate、人工复核图
和全队列 QA；输出固定在 `outputs/centerline_v2_full_173_20260828/`，未覆盖 `data_new`。

**当前状态判断**：Centerline V2 全量提取阶段完成，可作为下一步 cutover/rebuild 的源数据。
20 例软复核不属于硬失败，但切换前应保留标记并优先核查壁面覆盖高值病例。现有
`data_wss_min`、`data_wss_pinn`、旧 bundle/sidecar 仍为旧中心线口径，尚未重建，禁止与
新 V2 产物混用。本次不改变模型路线，不更新 `docs/实验设计总纲.md`；没有新增长期硬规则，
不更新 `AGENTS.md`。

## 2026-08-28｜WSS_PINN V4：index 15 已评并写入工作簿

seed1234 末臂 `E_rel_l2=0.951`，WSS R²_cb=−0.198；主表行 34 与敏感性 sheet 已补。
细节见 [WSS 专用同日记录](WSS最小化_代码修改与实验推进记录.md)。

## 2026-08-27｜WSS_PINN V4：完训臂补跑 WSS（22 个，未填 xlsx）

node04 GPU1 按 0–14 协议（峰值全场速度 + Profile-Secant V3 ×1200）评完
18–38、40。xlsx 未改。细节见
[WSS 专用同日记录](WSS最小化_代码修改与实验推进记录.md)。

## 2026-08-27｜WSS_PINN V4：完训臂 official test35（22 个，未填 xlsx）

node04 GPU1 评完 index 18–38、40；与 0–14 合计 37/48。xlsx 按用户要求未改。
细节见 [WSS 专用同日记录](WSS最小化_代码修改与实验推进记录.md)。

## 2026-08-25｜WSS_PINN V4：index 15 已在 node04 优先直启

为先齐 seed1234，已 `scancel 12210_15` 并在 node04 GPU0 全新开跑
`V4-TR-PNPP-BC-PDE-EMA-s1234`（PID `2206976`）。master 上 `31/37/38/39` 未动，
`16-17,40-47` 继续 `%4` 排队。细节见
[WSS 专用同日记录](WSS最小化_代码修改与实验推进记录.md)。

## 2026-08-23｜WSS_PINN V4：0–14 写入工作簿（含 WSS 与 milestone 敏感性）

主表已追加 seed1234 的 V4 行 0–14，index 15 留空；新增 `E_rel_l2_cb` 列与
`V4 Checkpoint敏感性` sheet。WSS 为冻结 Profile-Secant V3 ×1200。细节见
[WSS 专用同日记录](WSS最小化_代码修改与实验推进记录.md)。

## 2026-08-23｜WSS_PINN V4：node04 评完 seed1234 index 0–14 的 test35

新增 `wss_pinn/v4/evaluate.py`，在 node04 两张 A100 上跑 official 协议。
Primary `E_rel_l2`：准稳态 DATA≈1.11、PDE-EMA 最好约 0.91；瞬态 DATA≈1.04、
PDE-F 最好约 0.89。缺 index 15，单 seed，未跑 WSS，未填工作簿。细节见
[WSS 专用同日记录](WSS最小化_代码修改与实验推进记录.md)。

## 2026-08-21｜WSS_PINN：归档 field-v4 Stage 0–1 执行提示词

已把 2026-08-06 完成的 Stage 0–1 提示词移入 `WSS_PINN/_archive/`（🧊冻结）。
现行执行入口仍是 V4 设计方案与路线 README。细节见
[WSS 专用同日记录](WSS最小化_代码修改与实验推进记录.md)。

## 2026-08-21｜WSS_PINN V4：22/48 完训 train-only 中期整理（未读 test35）

已完训 22 个 run 的 raw 训练损失已整理。准稳态 seed1234 八臂齐；DATA+BC / PDE
在 train `data_total` 上系统性差于 DATA，EMA `λ_pde` 贴上限 10。这不是 §12.0
primary endpoint。细节见
[WSS 专用同日记录](WSS最小化_代码修改与实验推进记录.md) 与
[WSS_PINN README](WSS_PINN/README.md)。

## 2026-08-20｜WSS_PINN V4：node04 两卡空闲，直启 `11/14`（不能跑 4 路）

node04 两张 A100 空闲，但不在 Slurm GPU 分区且只有 2 卡。已把排队最前的
`12210_{11,14}` 从数组取消，按 V3 方式在 04 上各占一卡直启。细节见
[WSS 专用同日记录](WSS最小化_代码修改与实验推进记录.md)。

## 2026-08-20｜WSS_PINN V4：`12210` 恢复四卡并发，`12210_24` 已启动

用户授权后把数组 `ArrayTaskThrottle` 从 2 改回 4（`scontrol update`，不重复提交）。
当前跑 `12210_{22,23,24}`；其余 pending 因师姐 `12366` 占 GPU2 变为 `Resources`。
细节见 [WSS 专用同日记录](WSS最小化_代码修改与实验推进记录.md)。

## 2026-08-17｜WSS_PINN V4：隔离 `12210_{11,16,17}` 半成品并放回 `%2` 队列

已把 NODE_FAIL 留下的三臂 `last.pt` 目录与 Slurm 日志隔离，11/16/17 在原数组中
pending，下次启动为全新开跑（非 resume）。当前仍跑 `12210_{18,19}`。细节见
[WSS 专用同日记录](WSS最小化_代码修改与实验推进记录.md)。

## 2026-08-17｜WSS_PINN V4：master 失联导致 `12210_{11,16}` NODE_FAIL requeue，`17` 半成品

今早 master 对 slurmctld 失联（04:51–05:12），Slurm 默认 `Requeue=1` 把
`12210_{11,16}` 标 NODE_FAIL 后重入队；`12210_17` 刚启动又因节点 DOWN 被 requeue，
留下 epoch ~1889 半成品。当前跑 `12210_{18,19}`。这三臂有 `last.pt`，下次启动前须隔离。
细节见 [WSS 专用同日记录](WSS最小化_代码修改与实验推进记录.md)。

## 2026-08-16｜WSS_PINN V4 让出两卡：`12210_{14,15}` 重排，后续 `%2`

为给师姐空出两张 4090，已杀死 `12210_{14,15}`，把未启动任务改为
`12210_[14-47]%2`，只等 `12210_{11,13}`（GPU0/3）腾卡。14/15 半成品与日志已隔离，
重跑为全新开跑。细节见
[WSS 专用同日记录](WSS最小化_代码修改与实验推进记录.md) 与
[WSS_PINN README](WSS_PINN/README.md)。

## 2026-08-14｜WSS_PINN V4 补提交未完训 `7-47`：`12210_[7-47%4]`

`11972_{0-6}` 已全部 completed。按用户要求把剩余 41 run 补提交为
`12210_[7-47%4]`（四卡 throttle）；index 7 中断产物已隔离后全新开跑，不重跑
Stage 0 / preflight。本轮仍不评估 test35、不跑 WSS。细节见
[WSS 专用同日记录](WSS最小化_代码修改与实验推进记录.md) 与
[WSS_PINN README](WSS_PINN/README.md)。

## 2026-08-09｜WSS_PINN V4 二次截断：取消 `11972_7`，下次补跑 `7-47`

正式数组在取消 `11972_[8-47]` 后，再取消运行中的 `11972_7`
（`V4-SP-PNPP-BC-PDE-EMA-s1234`，约 epoch 797）。当前仅 `11972_{2,3,6}` 仍在
master 运行；`0/1/4/5` completed。下次补提交从 index **`7`** 起（`7-47`）。细节见
[WSS 专用同日记录](WSS最小化_代码修改与实验推进记录.md) 与
[WSS_PINN README](WSS_PINN/README.md)。

## 2026-08-08｜WSS_PINN V4 实现与 48-run 四卡 Slurm 提交

新 V4 已从设计推进到独立实现与集群队列：新增 `wss_pinn/v4/`、48 个正式配置和
Stage 0→GPU preflight→训练数组依赖链。34 项体域回归测试通过，173 例 Stage 0 本地
全量 Gate pass；PointNet/PointNet++ 参数量比 `1.0008`。最终 Slurm 作业为 CPU
`11970` → GPU `11971` → `11972_[0-47%4]`，最多 4 张 4090 并发。此前 `11964` 暴露
旧入口 monitor 最差约 2.01% 的偏差，`11967` 暴露人工延长段非目标域点过滤缺失；均已
修正，对应依赖 GPU 作业已取消且未启动训练。最终链的 `11970/11971` 已 exit 0，正式
数组 `11972` 已开始运行。
本轮不等待完训、不读取 test35 新结果，也不做 WSS/工作簿回填；详细实现与纠错见
[WSS 专用同日记录](WSS最小化_代码修改与实验推进记录.md)。

## 2026-08-08｜WSS_PINN V4 设计 v1.2：审核修正入口合同、RCR 单位与四臂矩阵

**本次主要修改**：[V4 大重构设计方案](WSS_PINN/WSS_PINN_V4大重构设计方案_2026-08-08.md)
经对抗性审核修订至 v1.2（仅改文档）：(1) 撤回“入口流量全库零方差”——6 例
`A_udf≠A_mesh`（最大约 35.6%），冻结 `U_in=Q_nom/A_udf`、`Q_actual=U_in·A_mesh`
合同并恢复 `Q_actual_peak` 输入；(2) 出口 RCR 残差改质量流量口径（UDF 按 `F_FLUX`
递推，混用差约 ρ≈1060 倍）；(3) 波形 `w=6.491≠2π/0.8`，周期重置点跳变按分段处理，
压力锚定收紧为条件成立的 raw Fluent gauge 约定；(4) 训练模式改
DATA/DATA+BC/BC+PDE-fixed/BC+PDE-EMA 四臂共 16 臂以分离边界监督与 PDE 贡献；
primary endpoint 改速度向量相对 L2 + Holm 预注册，test35 结论定性为探索性。
详细见 [WSS 专用记录同日条目](WSS最小化_代码修改与实验推进记录.md)。

**对应代码/文档**：设计方案 v1.2、[路线真源](WSS_PINN/README.md)（状态区已同步）。
本次未修改训练代码、配置或数据。

**推进到实验步骤**：Stage 0 合同扩充（`A_mesh/A_udf` 双落库、RCR 容错解析、
ρ/Carreau 冻结、共线性与离散 RCR 残差审计）；正式实现仍未开始。

## 2026-08-08｜WSS_PINN 新 V4 大重构设计与瞬态数据覆盖核对

**本次主要修改**：新增新 V4 路线级设计文档，废止“求解后出口压力作为可部署输入”的
设想，改为显式输入入口流量、入口/四出口面积与四组 `R1/R2/C`；固定 train138/test35、
无 validation、PointNet/PointNet++ × DATA/PINN-fixed/PINN-EMA-ratio 共 12 臂，以及准稳态 peak / 含 `du/dt`
瞬态双配置。动态臂采用老师建议的 detached EMA `L_data/L_phy`，默认每 50 step 更新，
并保留 `lambda_phy=1` 固定权重严格消融。设计逐项冻结 data/physics loss 日志、train-only 收敛停止和
test35 禁止选模边界。

**对应代码/文档**：
[V4 大重构设计方案](WSS_PINN/WSS_PINN_V4大重构设计方案_2026-08-08.md)、
[WSS_PINN 路线真源](WSS_PINN/README.md)、[`wss_pinn` README](../../wss_pinn/README.md)、
`wss_pinn/AGENTS.md` 和
[WSS 专用详细记录](WSS最小化_代码修改与实验推进记录.md)。本次未修改训练代码、配置或数据。

**推进到实验步骤**：完成 173 例只读文件级审计。每例均有 81 帧体域 `u/v/w/p`、
81 个精确匹配入口流量时相、0.005 s solver step、0.01 s 场导出间隔、峰值前后邻帧、
四组完整 RCR 与正值入口/四出口面积。瞬态源数据可行，但派生合同尚未建立。

**当前状态判断**：新 V4 处于 design-complete / implementation-pending；旧
`volume_uvwp_peak_field_v4` 保留为历史结果。正式实现前仍需跨时相点身份、时间映射、
pressure gauge 和 train-only stats Gate；未启动训练、未读取新 V4 test35 指标、未回填工作簿。

---

## 2026-08-07｜docs 大规模归档整理与推进记录滚动切卷

**改动**：
- 两份推进记录滚动切卷：本文件 2026-03～06 条目切入 [_archive/代码修改与实验推进记录_2026-03至06卷](_archive/代码修改与实验推进记录_2026-03至06卷.md)（726K→87K）；[WSS最小化_代码修改与实验推进记录](WSS最小化_代码修改与实验推进记录.md) 2026-07 条目切入 [_archive/WSS最小化_代码修改与实验推进记录_2026-07卷](_archive/WSS最小化_代码修改与实验推进记录_2026-07卷.md)（370K→40K）。
- 已结案专题移入 `_archive/`：新队列审计两文 + `assets_新队列审计/`（123M，终态快照见其 README 说明）、`assets_第四轮/`、`training脚本评估入口与清理建议`、GAN 扩增可行性两篇、`WSS高值区域预测优化方案`（加暂停标注）；`WSS最小化_体域物理约束与PINN训练路线_2026-07-29` 移入 `WSS_PINN/_archive/`。
- 冻结标注：`WSS最小化_PointNet_baseline实验矩阵与进度跟踪`、`WSS最小化_训练实验跟踪` 文首加 🧊 冻结说明，只作结果档案。
- `03-汇报材料/`：历史 pptx/图件/preview 移入 `_archive/`（见其 [归档索引](../03-汇报材料/_archive/README.md)）；删除 3 份 xlsx `.bak`、`实验记录表.xlsx.bak_2026-07-04`、`__pycache__`、`.DS_Store`。
- `01-任务/任务A/03-V3路线/00-当前主线/` 更名为 `00-历史主线/`（主线已收束到 `u,v,w,p`，避免误导），全库引用已同步替换。
- `paper_idea/`：Notion 导出残留 `项目/` 与 2026-06-11 阅读对照 pptx 移入 `paper_idea/_archive/`。
- 新增归档索引：[02-推进与变更/_archive/README](_archive/README.md)、[03-汇报材料/_archive/README](../03-汇报材料/_archive/README.md)；活跃文档中指向被移动文件的链接已修复，归档卷内旧链接按卷首说明解析。

**推进到实验步骤**：文档工程（无训练）。当前活跃入口不变：[docs/README](../README.md)「现在先看什么」。

---

## 2026-08-06｜V2/V3 冻结后全点 y=ax+b 线性回归审计与 Excel 回填完成

**本次主要修改**：新增下游审计工具
`docs/03-汇报材料/tools/add_wss_pinn_v2_v3_linear_regression.py`，对冻结的 V2 八臂
（`last@7500`/test35）与 V3 六臂（`best_validation_data`/test35）逐病例做带截距 OLS
`y=a·x+b` 回归诊断：速度用解剖 STL 封口裁剪去延长段后的全部体内点（保留率
56.68%），WSS 用全部冻结壁面节点 + 冻结 Profile-Secant V3 算法；旧指标不变。
490/490 逐病例结果完成并通过 verify（0 failures；顺带修复 verify 将 `*_sha256`
误当作路径检查的 bug）。

**主要结论**：全部 14 臂速度斜率 `a` 仅 0.20–0.43，高速段被系统性低估；speed fit
R² V2-PNPP 0.32–0.34 > V3 0.24–0.29 > V2-PN 0.14–0.20。

**产物**：`outputs/wss_pinn/audits/v2_v3_linear_regression_fullpoints_20260807/`
（逐病例 metrics/回归图/密度直方图 + 每臂 summary 与 6 张汇总图）；
`docs/03-汇报材料/WSS_PINN_V1_V2_V3_field-v4实验矩阵与指标汇总_2026-08-06.xlsx`
主表扩至 40 列并新增「线性回归汇总」「线性回归逐病例」两表（原件已备份）。
详见 [WSS_PINN 路线 README](WSS_PINN/README.md) 对应章节。

## 2026-08-06｜field-v4 Stage 0–1 实现、正式训练与多种子 Go/No-Go 完成

**本次主要修改**：在独立 `volume_uvwp_peak_field_v4` route 完成病例等权 validation、
B0 atlas、global/local × raw/PE 纯监督矩阵、边界数据合同、导数/support Gate、补种子
与 full-volume 晋级审计；修复旧 batch 聚合造成的 checkpoint 轮次偏移，并增加
test35/V3 写路径护栏、Slurm 分阶段提交与最终结果汇总。

**对应代码/文档**：`wss_pinn/{config.py,train.py,evaluate.py,validation.py}`、
`wss_pinn/{models,data,tools,cluster}/`、
`wss_pinn/configs/volume_uvwp_peak_field_v4*/`、
[`wss_pinn` README](../../wss_pinn/README.md)、
[路线真源](WSS_PINN/README.md)、[已完成执行合同](WSS_PINN/_archive/WSS_PINN_下一智能体目标提示词_冻结诊断后执行Stage0至Stage1_已完成_2026-08-06.md)、
[WSS 专用详细记录](WSS最小化_代码修改与实验推进记录.md)；结果真源为
`outputs/wss_pinn/volume_uvwp_peak_field_v4/stage1_multiseed_and_promotion_report.json`。

**推进到实验步骤**：Stage 0-a/0-b、B0、四臂 seed1234、前二臂 seeds 2345/3456、
八 checkpoint 导数/support Gate 和六 checkpoint full-volume val15 均完成。正式 Slurm
训练为 `11315`（Raw）、`11318`（PE）、`11321`（确认种子），full-volume 为 `11325`；
所有 task exit code 0。26 tests、CPU smoke、静态/GPU preflight 全通过。

**当前状态判断**：B0=`0.699472`；三种子 G-Raw/G-PE 为
`0.576116±0.007477 / 0.581208±0.012599`。PE 仅 1/3 seeds 改善，并触发 pressure
RMSE `+2.36%` 与 ILO `+5.58%` 护栏；最终保留 G-Raw，关闭 PE 扫频和 local decoder。
Stage 2 未启动；本轮未读取 test35、未运行 WSS、未启用 BC input/loss 或 PDE loss。

## 2026-08-06｜生成冻结诊断后的新执行提示词并归档旧六臂合同

**本次主要修改**：将已完成的准稳态平滑场六臂提示词移动到 WSS_PINN 归档，修复其
归档后相对链接并新增归档索引；基于 `FROZEN FOR IMPLEMENTATION v1.0` 新建当前
Stage 0-a/0-b → Stage 1 执行提示词，明确病例等权 `S_field^cb`、B0 atlas、outlet/
inlet-wall Gate、纯监督 global/local × raw/PE、test35/WSS 禁止选模和正式训练授权边界。

**对应代码/文档**：
[当时 Stage 0–1 执行提示词](WSS_PINN/_archive/WSS_PINN_下一智能体目标提示词_冻结诊断后执行Stage0至Stage1_已完成_2026-08-06.md)、
[已归档六臂提示词](WSS_PINN/_archive/WSS_PINN_下一智能体目标提示词_准稳态平滑场六臂实验_已完成_2026-08-05.md)、
[归档索引](WSS_PINN/_archive/README.md)、[路线真源](WSS_PINN/README.md)、
[`wss_pinn` README](../../wss_pinn/README.md)、`wss_pinn/AGENTS.md`、`docs/README.md`和
[WSS 专用详细记录](WSS最小化_代码修改与实验推进记录.md)。

**推进到实验步骤**：当前提示词已可复制到新任务，用于实现 Stage 0-a/0-b、测试、
审计、preflight 和 Stage 1 dry-run 准备；未修改训练代码、配置、split 或模型产物，
未启动正式训练。

**当前状态判断**：旧六臂提示词已从活动入口移除并保留完整历史；当前唯一执行入口是
冻结诊断后的 Stage 0–1 提示词。仅复制提示词默认不授权正式多 GPU 训练，需新任务中的
用户明确授权。

## 2026-08-06｜`u,v,w,p` 下一轮设计最终审查与执行冻结

**本次主要修改**：对核心诊断新增的第二轮零训练测量做最后一轮对抗性审查，将 6 例、
单 checkpoint、未入库脚本的结论统一限制为 B 级，纠正“PE 已排除、冲突只来自动量、
出口链已全库确认”等过强外推。冻结唯一 checkpoint 标量 `S_field^cb`，新增 B0 病例盲
atlas，按依赖拆分 Stage 0-a/0-b/0-c/0-d，并明确 Stage 1 不输入 BC、不使用 PDE、
Stage 3 从 Stage 2 胜出模型起步、瞬态 physics 分为已知源项和时相条件模型两条路线。

**对应代码/文档**：
[冻结版核心诊断](WSS_PINN/_archive/核心代码诊断与下一轮设计建议_2026-08-05.md)、
[体域 PINN 路线真源](WSS_PINN/README.md)、[`wss_pinn` 执行说明](../../wss_pinn/README.md)、
`wss_pinn/AGENTS.md`、根 `README.md`、`docs/README.md`、`docs/实验设计总纲.md`和
[WSS 专用详细记录](WSS最小化_代码修改与实验推进记录.md)。

**推进到实验步骤**：科学设计冻结为 `FROZEN FOR IMPLEMENTATION v1.0`；下一执行单元
固定为 Stage 0-a（病例等权 validation、`S_field^cb`、B0 atlas、探针入库），随后为
Stage 0-b outlet/inlet-wall/wall-zone Gate。本次未修改训练代码、配置、split 或模型产物，
未启动训练。

**当前状态判断**：文档已达到可执行冻结状态。当前 V3 继续作为历史基线；新实现必须
独立落盘，且只有 Stage 0 Gate 不可实现或新增机器可读反证时才允许改变科学范围、主指标、
阶段顺序或 Go/No-Go。

## 2026-08-06｜当前模型开发收束到 `u,v,w,p` 场重建

**本次主要修改**：根据当前研究重心，将 velocity→WSS 的 Profile-Secant V3 明确冻结为
下游验证器，不再作为算法、loss、采样或选模优化主线。重写体域 PINN 核心诊断中的
Stage 0–4、指标和 Go/No-Go：主任务统一为 `u/v/w` 分量与速度向量、near-wall/core、
截面与分支流量，以及 `p` 的病例 gauge、压力梯度和入口—出口压降；新增 velocity / pressure
分头 decoder 与共享层梯度冲突审计方向。

**对应代码/文档**：
[核心诊断与下一轮设计](WSS_PINN/_archive/核心代码诊断与下一轮设计建议_2026-08-05.md)、
[体域 PINN 路线真源](WSS_PINN/README.md)、
[历史六臂预注册提示词](WSS_PINN/_archive/WSS_PINN_下一智能体目标提示词_准稳态平滑场六臂实验_已完成_2026-08-05.md)、
[`wss_pinn` 执行说明](../../wss_pinn/README.md)、`wss_pinn/AGENTS.md`、
[CFD velocity→WSS 适配说明](../../wss_mri_calculator/README_CFD_ADAPTATION.md)、
根 `README.md`、`docs/README.md`、`docs/实验设计总纲.md`和
[WSS 专用详细记录](WSS最小化_代码修改与实验推进记录.md)。

**推进到实验步骤**：完成下一轮训练前的范围锁定与文档合同同步；未修改代码、配置、
split、checkpoint 或训练产物，未启动训练或新增 WSS 推理。

**当前状态判断**：当前唯一模型优化主线是 `u,v,w,p`。Profile-Secant V3 只允许在
基于 validation 的四通道、区域、流量和压降指标选定主 checkpoint 后运行一次，检查
速度误差是否被下游梯度计算放大；WSS 不参与训练、early stopping、checkpoint 或架构选择。

## 2026-08-05｜体域 PINN 核心诊断的第一性原理与对抗性修订

**本次主要修改**：对 V3 诊断文档做了代码、配置、训练曲线、逐病例指标、
Fluent UDF/RCR 和边界来源链的交叉复核。将“已确认事实/强证据但未入库/待证伪
假设”分级；撤回全局 latent 已是根因、validation loss=1 精确等于均值预测、
momentum 按轴强制平衡、三例 CFD residual 可作逐点地板、Fourier/Gaussian 为必然解等过强
结论。新增 val15 固定单病例 batch 双倍权重、test35 development exposure、
outlet monitor 与真实 RCR boundary profile/可部署输入的区分、配对 bootstrap、WSS 一阶导数
目标和 global/local × raw/PE 的 2×2 因果实验路线。

**对应代码/文档**：
[核心诊断与下一轮设计](WSS_PINN/_archive/核心代码诊断与下一轮设计建议_2026-08-05.md)、
[WSS-PINN 路线真源](WSS_PINN/README.md)、
[历史六臂预注册提示词](WSS_PINN/_archive/WSS_PINN_下一智能体目标提示词_准稳态平滑场六臂实验_已完成_2026-08-05.md)、
[`wss_pinn` 执行说明](../../wss_pinn/README.md)、
`wss_pinn/AGENTS.md`、`docs/实验设计总纲.md`和 [WSS 专用详细记录](WSS最小化_代码修改与实验推进记录.md)。

**推进到实验步骤**：完成下一轮训练前的文档级 adversarial design review；未改代码/配置，
未跑新训练。只读复核了六臂 epoch 日志、test35 逐例增量、V3 loss/dataset/model 实现和
Fluent 入口/RCR UDF。

**当前状态判断**：当前 V3 继续作为历史冻结基线，不续训。新训练的 P0 是修复
validation 聚合、恢复/重新定义真实 outlet boundary profile、去除 inlet-wall 交线冲突、
把一次性诊断入库，并将 test35 降为 development-exposed screen。通过以上 Gate 后，
先做无 PDE 的 2×2 表征消融；physics 只在最佳监督场上以 continuity、瞬态或弱形式重新评估。

## 2026-08-05｜体域 PINN V3 六臂完训、评估与增量裁决

**本次主要修改**：完成真实 peak wall/inlet/four-outlet 边界恢复、无 3NN 条件
PointNet 平滑场、DATA/BC/PDE 六臂、train123/val15/test35、Gate、双节点提交监控，
并完成三 checkpoint 全体域评估、pooled/case-balanced 指标、九组预注册增量和收敛
诊断；补齐老师汇报用的六臂 data/physics loss 总览、每臂最后 10% 放大、BC/PDE
分量图和 9 页 PDF，并生成含7张表和嵌入图的统一 `.xlsx` 汇报工作簿。
详细科学合同、warning 与实现血缘由 [WSS-PINN 真源](WSS_PINN/README.md)和
[WSS 专用详细记录](WSS最小化_代码修改与实验推进记录.md)维护。

**推进到实验步骤**：boundary Gate 173/173、测试/CPU smoke/static/GPU preflight
通过；master Slurm `11301_[0-3]` 与 node04 两臂均完成 2500 epoch / 155000 step，
`best_validation_data/best_validation_total/last` 的 test35 评估 18/18 完成。

**当前状态判断**：主 checkpoint 下 geom 对 speed R²_cb 稳定提升约 `+0.19`；BC 对
总体速度近中性；PDE 虽显著降低 continuity/momentum residual，却使 speed R²_cb
下降约 `0.05`，属于准稳态正则与瞬态 peak 标签竞争。最后 10% validation 未继续
下降，不续到 5000；near-wall speed R² 全负，不形成 WSS 结论。详细主表与限制见
[WSS-PINN 真源](WSS_PINN/README.md#0-v3-准稳态平滑场六臂2026-08-05-完训并评估)。

## 2026-08-04｜Profile-Secant V3 test35 全壁面推理与采样/穿模可视化

**本次主要修改**：将 Profile-Secant V3 从 test35 × 1200 历史预览升级为
`sample_count=0` 全壁面推理。新增可恢复的 8 分片执行器，完成 35 例全部
`1,328,017` 个壁面节点的基础 V4、depth-3/profile/secant 特征和冻结校准；补齐
全量同点散点、最佳/最差病例空间图、单壁面点内部采样、宽视野穿模审计和全量 VTP。

**对应代码/文档**：
`wss_mri_calculator/src/run_profile_secant_v3_fullwall.py`、
`wss_mri_calculator/src/run_frozen_wss_compat.py`、
`wss_mri_calculator/viz/{export_profile_secant_v3_visualization,visualize_v4_single_target_sampling,visualize_v4_penetration_risk}.py`、
[全壁面结果入口](../../outputs/wss_mri_calculator/pointcloud_surface_mls_v4/profile_secant_v3_fullwall/README.md)、
[WSS 专用详细记录](WSS最小化_代码修改与实验推进记录.md)。

**推进到实验步骤**：冻结 train138 模型不变，test35 不参与调参。基础缓存和
Profile-Secant 特征缓存均 35/35 完成，逐病例 `sample_index=0..N-1` 审计通过；
结果/预测 SHA256 为 `31a4bdca…d7e89` / `9aaa80ad…bdbf`。

**当前状态判断**：全壁面病例 overall R² 均值=`0.9604`、pooled high-WSS
R²=`0.9440`、逐病例 high-WSS R² 均值=`0.7735`，整体结论与 sampled 基本一致；
但平均峰值低估升至 `15.81%`，证明 1200 点峰值结果偏乐观。最佳/最差仍为
`ILO/LI_YOU_ZHI-0/before` / `AG/slow/ZHANG_WEI_XIAN`。两例穿模审计的门控后保留
风险均为 0，但不等于已证明同向相邻分支的拓扑归属。

## 2026-08-04｜体域 PINN 准稳态平滑场六臂方案完成执行交接

**本次主要修改**：新增下一阶段可执行交接提示词，冻结“Fluent 瞬态 peak 标签 +
Carreau–Yasuda 准稳态 PINN 正则”的科学定位，以及无 3NN 条件 PointNet 平滑场、
`xyz/xyz+geom × DATA/DATA+BC/DATA+BC+PDE` 六臂和真实病例边界 Gate。

**对应代码/文档**：
[六臂执行交接（已归档）](WSS_PINN/_archive/WSS_PINN_下一智能体目标提示词_准稳态平滑场六臂实验_已完成_2026-08-05.md)、
[体域 PINN 真源](WSS_PINN/README.md)、[`wss_pinn` README](../../wss_pinn/README.md)、
`docs/README.md` 与 [WSS 专用详细记录](WSS最小化_代码修改与实验推进记录.md)。

**推进到实验步骤**：完成文档级实验预注册与下一智能体目标；未改代码、数据、配置或
既有输出，未提交新训练。提示词要求边界 Gate 通过后继续到正式六臂提交、监控和评估。

**当前状态判断**：handoff ready / implementation pending。下一阶段先审计 peak
inlet/outlet 的坐标、单位、时相与压力 gauge；不能用伪造 BC 或 SAME-IDW residual
替代真实边界和独立 collocation 证据。

## 2026-08-04｜Profile-Secant V3 独立图集与 VTP 可视化包

**本次主要修改**：为当前推荐结果模型 Profile-Secant V3 建立不含 V4 final 预测的
独立 `00_quicklook`，并导出 high-WSS R² 最佳/最差病例的 ParaView VTP 面片包；
同步方法说明、CFD 适配入口与 WSS 专用推进记录。

**对应代码/文档**：
`wss_mri_calculator/viz/export_profile_secant_v3_visualization.py`、
`tools/cfdpost_cloud_export/plot_stl_mapped_triptych.py`、
[可视化结果入口](../../outputs/wss_mri_calculator/pointcloud_surface_mls_v4/profile_secant_v3/README.md)、
[WSS 专用详细记录](WSS最小化_代码修改与实验推进记录.md)。

**推进到实验步骤**：使用冻结 test35 × 1200 prediction archive 生成 quicklook、同点
CSV 和 STL 映射 VTP；未重训、未改模型或 split。

**当前状态判断**：最佳病例为 `ILO/LI_YOU_ZHI-0/before`，最差病例为
`AG/slow/ZHANG_WEI_XIAN`。两份主 VTP、色标、预览图和打开说明均已落盘，可直接在
ParaView 中查看 `wss_cfd`、`wss_profile_secant_v3` 和误差字段。

## 2026-08-04｜体域 PINN V2 SAME residual 根因与论文指标口径闭环

**本次主要修改**：对已完成的 V2 最佳 checkpoint 做零重训 exact/independent query
审计、三例 CFD 准稳态/瞬态 residual 审计和 Liao 2025 NMAE 指标桥接；新增可复现
工具和机器可读输出，并同步项目入口、实验总纲、WSS-PINN 真源和论文复现页。

**对应代码/文档**：[体域 PINN 结果真源](WSS_PINN/README.md)、
[`wss_pinn` 代码说明](../../wss_pinn/README.md)、
[老师论文与 V2 对照](../paper_reproduction/papers/hemodynamics_pointcloud_pinn/README.md)、
[WSS 专用详细记录](WSS最小化_代码修改与实验推进记录.md)、
`wss_pinn/tools/diagnose_v2_physics.py` 与对应 audit 目录。

**推进到实验步骤**：只读使用现有 `last@7500`，35 例网络 residual 对照与三域 CFD
导数审计均完成；没有训练、改权重、改 split 或覆盖既有 evaluation。

**当前状态判断**：SAME support 的 PointNet++ IDW 导数退化已被定量确认，独立 query
动量 residual 约高 `331×`；`du/dt` 可降低三例 residual，但不是全部误差来源。当前
V2 speed R² 低主要还受缺失 BC/流量条件、跨域异质性、固定 loss 尺度与近壁信息不足
影响。老师论文的 NMAE 和病例级 FR/PD R² 不可与当前点级 speed R² 直接对标。

## 2026-08-04｜Profile-Secant V3 升级为当前推荐结果模型

**本次主要修改**：根据用户明确的目标函数，纠正 velocity→WSS 项目级推荐口径：
Profile-Secant V3 用于病例总体、平均高 WSS 主结果和可视化，V4 final 作为冻结基线
与消融对照。同步修改项目入口、实验总纲、CFD 适配说明、路线真源和 WSS 专用记录。

**对应代码/文档**：[velocity→WSS 总跟踪](../../wss_mri_calculator/experiments/README.md)、
[Profile-Secant V3 推荐结果](../../wss_mri_calculator/experiments/pointcloud_surface_mls_v4/PROFILE_SECANT_HIGH_TAIL_V3_RESULTS.md)、
根 `README.md`、`docs/README.md`、`docs/实验设计总纲.md`、
[WSS 专用详细记录](WSS最小化_代码修改与实验推进记录.md)。

**推进到实验步骤**：仅修正文档状态和报告优先级；没有重训、重推理或改动任何
模型产物。Profile-Secant V3 的模型与 predictions SHA256 保持不变。

**当前状态判断**：Profile-Secant V3 是当前数值表现最好的结果模型，V4 final 的
“final”保留为历史冻结命名。两者都没有达到逐病例严格保证，后续仍需更靠壁速度信息。

## 2026-08-04｜体域 PINN 代码结构收敛与历史实现归档

**本次主要修改**：把当前峰值体域 `u,v,w,p` PINN 的核心代码和 Slurm 入口提升到
`wss_pinn/` 根层，并将旧直接 WSS/F0–F2 源码、配置、工具和测试收口到独立归档；
同步代码 README、Agent 约束、路线真源和 WSS 专用推进记录。

**对应代码/文档**：[`wss_pinn/README.md`](../../wss_pinn/README.md)、
[`wss_pinn/archive/`](../../wss_pinn/archive/README.md)、`wss_pinn/AGENTS.md`、
[体域 PINN 路线真源](WSS_PINN/README.md)、
[WSS 专用详细记录](WSS最小化_代码修改与实验推进记录.md)。

**推进到实验步骤**：完成代码归档和活动入口迁移；未启动或重跑实验，既有 v1/v2
配置、数据、checkpoint、评估产物和科学结论均保持不变。

**当前状态判断**：`wss_pinn/` 根目录现在只呈现当前体域主线及其共享基础模块；
历史 WSS-target 实现保留为只读追溯材料，后续开发不再引用归档路径。

## 2026-08-04｜velocity→WSS 高 WSS 可辨识性结论同步

**本次主要修改**：把 Surface-MLS V4 冻结版之后的 Profile-Secant V3 高 WSS
推荐结果、逐病例严格边界、oracle 上限和近壁分辨率结论同步到项目入口、实验总纲、
WSS 专用真源及推进记录；将两份候选结果文档改为中文，并补充 2026-08-04 高 WSS
对比图。

**对应代码/文档**：[velocity→WSS 总跟踪](../../wss_mri_calculator/experiments/README.md)、
[Profile-Secant V3 结果](../../wss_mri_calculator/experiments/pointcloud_surface_mls_v4/PROFILE_SECANT_HIGH_TAIL_V3_RESULTS.md)、
根 `README.md`、`docs/README.md`、`docs/实验设计总纲.md`、
[WSS 专用详细记录](WSS最小化_代码修改与实验推进记录.md)。

**推进到实验步骤**：未启动新训练或 post-test tuning；使用已冻结预测产物生成
Profile-Secant V3 test35 对比图，并完成文档口径对齐。

**当前状态判断**：整体和 pooled 高 WSS 指标已很好，但不能外推为逐病例保证。
Profile-Secant V3 在 test35 仅 4/35 病例同时达到 high-WSS R² `≥0.90` 与峰值低估
`≤10%`；继续做后校准没有证据消除剩余失败，下一阶段需要新增更靠壁的速度信息。

## 2026-08-03｜体域 PINN V2 完训与速度精度结论同步

**本次主要修改**：完成 SAME5K-E7500 v2 八实验结果审计，将 WSS-PINN 真源、Agent
边界、代码入口、根 README、docs 索引和实验总纲从 running 更新为 completed，
并同步速度、压力、物理 residual 与近壁精度的路线级结论。

**对应代码/文档**：[体域 PINN 结果真源](WSS_PINN/README.md)、
`wss_pinn/AGENTS.md`、[`wss_pinn/README.md`](../../wss_pinn/README.md)、
根 [`README.md`](../../README.md)、
`docs/README.md`、`docs/实验设计总纲.md`、
[WSS 专用详细记录](WSS最小化_代码修改与实验推进记录.md)。

**推进到实验步骤**：array `11138_[0-7%4]` 8/8 completed；每臂 7500 epoch /
517,500 step，共 48 份 SAME5K/full-volume checkpoint evaluation。当前速度锚点
PointNet++ + `xyz+geom` + PINN 的 SAME5K/full-volume speed R²=`0.2380/0.1820`。

**当前状态判断**：V2 completed / audited。物理一致性和压力收益明确，但完整体域
MAE/RMSE 与对应 data-only 基本持平，near-wall speed R² 仍为负；当前结果不能直接
支持可靠 velocity→WSS。下一步转向负 R² 病例与近壁诊断，不再只追加训练时长。

## 2026-08-03｜wss_mri_calculator V1–V4 总跟踪与项目入口同步

**本次主要修改**：新增 velocity→WSS V1–V4 路线总跟踪，将 Surface-MLS V4
冻结结论同步到 CFD 适配说明、根 README、docs 索引和实验设计总纲；保持任务 A
网络 V1/V2/V3 与 CFD 后处理 V1–V4 的命名边界。

**对应代码/文档**：
[`wss_mri_calculator/experiments/README.md`](../../wss_mri_calculator/experiments/README.md)、
[`README_CFD_ADAPTATION.md`](../../wss_mri_calculator/README_CFD_ADAPTATION.md)、
根 [`README.md`](../../README.md)、`docs/README.md`、`docs/实验设计总纲.md`、
[WSS 专用详细记录](WSS最小化_代码修改与实验推进记录.md)。

**推进到实验步骤**：V1–V4 frozen；当前推荐 V4 final，test35 raw/scaled R² mean
为 `0.95607/0.95844`，冻结模型、结果、预测和 quicklook 已落盘并通过哈希校验。

**当前状态判断**：项目级导航已对齐当前事实。详细算法和结果由各版本 README/
RESULTS 负责，总跟踪只维护状态、口径和入口；后续不得把 V4 train-only 校准结果
写成零真值纯物理 raw 前向。

## 2026-08-02｜node04 使用要点入库（UID 对齐后冒烟通过）

**本次主要修改**：固化 node04 上机约束（UID=`1006/1007`、默认 HOME 为本地
`/home/cy`、conda 用 public 绝对路径、2×A100），并完成 GPU + CFD WSS 单病例冒烟。

**对应代码/文档**：
`docs/00-规范与记录/集群node04使用要点.md`、
`.cursor/skills/node04-experiments/SKILL.md`、
`.cursor/rules/node04.mdc`；同步更新 `cluster.mdc` / `gpu.mdc` / `conda.mdc` /
`docs-context.mdc`、`docs/README.md`、`任务A配置与启动说明.md` 入口链。

**推进到实验步骤**：node04 可读写 `/public/newhome/cy`（含 `data_new` 与
`GNN`/`GNN_vmtk`）；冒烟 `LIU_JIN_LIANG` → `raw_r2≈0.828`、`truth_usable=true`。

**当前状态判断**：权限与环境可用；此后用户说「去 node04 跑实验」时按上述
skill/文档执行，不再依赖对话记忆。

## 2026-08-02｜体域 PINN 第二轮 SAME5K-E7500 已提交

**本次主要修改**：新增 random SAME5K、固定 7500 epoch 的八实验矩阵，保留首轮 SEP
结果不变；支持固定里程碑 checkpoint、SAME5K/full-volume 双评估和无时限 Slurm。

**对应代码/文档**：[体域 PINN 路线真源](WSS_PINN/README.md)、
[WSS 专用详细记录](WSS最小化_代码修改与实验推进记录.md)、
`outputs/wss_pinn/volume_uvwp_peak_same5k_e7500_v2/submission.json`。

**推进到实验步骤**：20/20 volume 单测、静态矩阵 preflight 通过；GPU preflight
`11137` 与正式 array `11138_[0-7%4]` 已提交，正式训练依赖 preflight 成功后释放。

**当前状态判断**：preflight `11137` completed，array `11138` running；首批四臂30分钟
健康监控通过，无 NaN/OOM/Traceback，checkpoint 与日志持续写入。train138/val0/
test35，主 checkpoint 固定 `last@7500`，test35 不参与训练控制。

## 2026-08-02｜体域 PINN 八实验 completed / audited

**本次主要修改**：完成峰值体域 `u,v,w,p` 八实验的配对指标、收敛曲线和机器可读
汇总，并把路线与项目入口状态更新为终态；详细数值由 WSS-PINN 真源维护。

**对应代码/文档**：[体域 PINN 结果真源](WSS_PINN/README.md)、
[WSS 专用详细记录](WSS最小化_代码修改与实验推进记录.md)、
`outputs/wss_pinn/volume_uvwp_peak_v1/summary/`。

**推进到实验步骤**：array `11128_[0-7]` 8/8 completed；每臂 400 epoch / 27,600
step。PINN 四对均显著改善物理一致性与压力，速度收益存在架构/输入依赖，只有
PointNet + `xyz+geom` 的五个数据 R² 全部提高。

**当前状态判断**：首轮 completed / audited；下一轮 `max_epochs=15000` 与
train138-only 早停为候选协议，code pending / not submitted。

## 2026-08-01｜体域 PINN 数据 Gate 通过并开始 4-GPU 正式矩阵

**本次主要修改**：完成 173 例 schema-v2 构建与 deep-source Gate，修正 Slurm
CuBLAS 确定性环境后，以 `0-7%4` 提交八实验训练 array `11128`。

**对应代码/文档**：[体域 PINN 当前入口](WSS_PINN/README.md)、
[WSS 专用详细记录](WSS最小化_代码修改与实验推进记录.md)、
`outputs/wss_pinn/volume_uvwp_peak_v1/submission.json`。

**推进到实验步骤**：GPU preflight `11127` completed；训练前 30 分钟持续监控完成。

**当前状态判断**：无 NaN/OOM/fatal error，4 卡并发和 loss 日志正常；训练仍在运行，
尚未做最终配对结论。

## 2026-08-01｜体域 PINN 活动路线切换为峰值四通道八实验

**本次主要修改**：`wss_pinn/` 当前真源改为 PointNet / PointNet++ 的峰值
`u,v,w,p` 八实验；PINN 使用 continuity、完整非牛顿 momentum 和 no-slip，禁止
data-only 热启动。同步建立 schema-v2 完整体域数据合同、loss/physics 日志、训练与
数据 Gate，并归档旧 WSS-target F0/F1/F2 路线。

**对应代码/文档**：[体域 PINN 当前入口](WSS_PINN/README.md)、
[`wss_pinn/README.md`](../../wss_pinn/README.md)、
[WSS 专用详细记录](WSS最小化_代码修改与实验推进记录.md)。

**推进到实验步骤**：完整单元测试 40/40 和静态配对 preflight 通过；默认提交器仅
dry-run，`jobs=[]`。新的 173 例 schema-v2 数据尚未构建，正式训练未提交。

**当前状态判断**：项目索引只保留状态与入口；实现、物理口径、数据审计和八臂矩阵
细节由 WSS-PINN 真源维护。

## 2026-07-31｜CFD velocity→WSS 审计图册与穿模宽视野入口整理

**本次主要修改**：重写 CFD velocity→WSS 探索审计目录的快速看图顺序，新增
`00_quicklook/` 图册入口、12 张简短顺序名原图链接和一张总览联系表；补充
RAN_QING_BO 紧缩/宽视野穿模图的分工、图例和三维判据边界。未移动或删除原始结果。

**对应代码/文档**：
[审计图目录说明](../../outputs/wss_pinn/audits/cfd_velocity_wss_explore/README.md)、
[快速图册](../../outputs/wss_pinn/audits/cfd_velocity_wss_explore/00_quicklook/README.md)、
`outputs/wss_pinn/audits/cfd_velocity_wss_explore/00_quicklook/00_overview_contact_sheet.png`。

**推进到实验步骤**：整理 P0-B CFD velocity→WSS Oracle 的单病例读图证据，覆盖
pred/truth、法向定向、近壁邻域、采样深度、穿模风险和 ParaView 面片预览；未启动
新训练、未改变采样或穿模检测算法。

**当前状态判断**：文档与输出导航已完成；RAN_QING_BO peak 1162 的既有机器可读
审计仍为全壁面穿模风险计数 `0`、三个焦点风险点数均为 `0`。宽视野 PCA 图只增强
人工可读性，不替代三维判据，也不改变 WSS-PINN full F0-UP No-Go / F2 blocked 状态。

## 2026-07-30｜WSS-PINN full F0-UP completed / audited / No-Go

**本次主要修改**：完成 full F0-UP 结果与配对科学 Gate 审计，并把 WSS-PINN
路线、项目索引和总纲从 running 更新为真实终态。未修改 baseline split。

**对应代码/文档**：
`outputs/wss_pinn/runs/PINN-F0UP-full-train138-test35-exclude-shi-v1-s1234-20260730/`、
`outputs/wss_pinn/audits/full_f0up_to_f1_gate_train138_test35_exclude_shi_v1_20260730/report.json`、
[WSS-PINN 总入口](WSS_PINN/README.md)、
[WSS-PINN 阶梯矩阵](WSS_PINN/_archive/wss_target_v1_20260730/WSS_PINN_阶梯实验矩阵与进度跟踪.md)。

**推进到实验步骤**：Jobs `11073/11074` 均 completed；best 的 train/test
velocity mean R² 为 `0.6568/0.1800`，没有病例通过 0.95 能力阈值。
Gate 报告 SHA256 `daccfd1d…75cfed`。

**当前状态判断**：full F0-UP completed / audited / No-Go；full F1
blocked / not submitted，F2 blocked。完整指标、哈希和固定 step 导致单病例
曝光不足的解释由 WSS-PINN 真源维护。

## 2026-07-30｜WSS-PINN full173 数据 Gate 通过并提交 full F0-UP

**本次主要修改**：只在 PINN 路线派生 `train138/test35`，保护 baseline 原
split；完成 173 例 source/sidecar 双审查、配对配置冻结与 full F0-UP
集群提交。详细哈希和 Job 记录由 WSS-PINN 真源维护。

**对应代码/文档**：`wss_pinn/`、`data_wss_pinn/full_train138_test35_exclude_shi_v1/`、
`outputs/wss_pinn/audits/`、[WSS-PINN 总入口](WSS_PINN/README.md)、
[WSS-PINN 阶梯矩阵](WSS_PINN/_archive/wss_target_v1_20260730/WSS_PINN_阶梯实验矩阵与进度跟踪.md)。

**推进到实验步骤**：Jobs `11070/11071/11072` completed，173/173 source
和 sidecar Gate 均 pass；full F0-UP `11073→11074` 已通过 preflight 并
进入正式训练。

**当前状态判断**：full F0-UP running；full F1 code-ready / 等待配对 Gate，
未提前提交。baseline split 未修改。

## 2026-07-30｜WSS-PINN F1 诊断选中候选；全量数据 173/174 通过

**本次主要修改**：完成 F1 八臂内部权重诊断、best/last 科学 Gate 汇总和
冻结 `train138/test36` 的全量 PINN 源数据审查；实现对应配置、完整性审计、
历史格式兼容和 Slurm 入口。另建立 full F0-UP→full F1 严格配对配置生成器，
提交前绑定 split/manifest/审查报告哈希。详细字段与失败证据由 WSS-PINN 真源维护。

**对应代码/文档**：`wss_pinn/`、
`outputs/wss_pinn/matrices/f1_diagnostics_20260730/`、
`outputs/wss_pinn/audits/full_data_train138_test36_20260730_v2/`、
[WSS-PINN 总入口](WSS_PINN/README.md)、
[WSS-PINN 阶梯矩阵](WSS_PINN/_archive/wss_target_v1_20260730/WSS_PINN_阶梯实验矩阵与进度跟踪.md)、
[WSS 专用推进记录](WSS最小化_代码修改与实验推进记录.md)。

**推进到实验步骤**：八个诊断训练 Job 和汇总 Job `11064` 全部完成；
唯一双 checkpoint 通过臂为 `continuity=1e-4`、`no-slip=10`。
全量复审 Job `11067` 按 Gate 合同为 `FAILED (2:0)`，报告为
`173/174` pass，唯一失败
`AAA/ruputer/SHI_YUN_XI` 的体域速度全零。

**当前状态判断**：F1 pilot diagnostics completed / selected；full-data
sidecar 与训练尚未提交，等待确认 PINN 专用 split 从 `train138/test36`
冻结为 `train138/test35`。正式链条 code-ready，22/22 单测通过；这不是
completed full-data 结果，F2 仍 blocked。

## 2026-07-30｜WSS-PINN F1 completed / audited / No-Go

**本次主要修改**：完成 F1 best/last、Slurm、checkpoint 与 CPU 只读重算审计，
并将 F1 结果、Gate 和耗时解释同步到 WSS-PINN 真源及项目入口。

**对应代码/文档**：
`outputs/wss_pinn/runs/PINN-F1-cont-noslip-overfit3-v2ext-s1234-20260730/`、
[WSS-PINN 总入口](WSS_PINN/README.md)、
[WSS-PINN 阶梯矩阵](WSS_PINN/_archive/wss_target_v1_20260730/WSS_PINN_阶梯实验矩阵与进度跟踪.md)、
[历史执行合同](WSS_PINN/_archive/wss_target_v1_20260730/WSS_PINN_下一智能体目标提示词_推进至F1.md)、
[体域物理约束路线](WSS_PINN/_archive/WSS最小化_体域物理约束与PINN训练路线_2026-07-29.md)、
[WSS 专用推进记录](WSS最小化_代码修改与实验推进记录.md)、
`wss_pinn/README.md`、根 `README.md`、`docs/README.md`、
`docs/实验设计总纲.md`。

**推进到实验步骤**：Jobs `11045/11046` 均 `COMPLETED (0:0)`。F1 best
把 mean continuity RMS 从 `70.1526` 降到 `0.3359`，但 mean no-slip
从 `0.12447` 升到 `0.13396`，mean velocity R² 从 `0.99502` 降到
`0.82491`；best/last 和 CPU 重算结论一致。

**当前状态判断**：F1 No-Go，F2 blocked。当前配置能优化 divergence，却未
同时保持 no-slip 与速度场准确性；不能据此宣称完整物理改善。该 run 是小型
三病例单时相一阶导数 pilot，约 17 分钟耗时与其 `613,829` 参数、
`18,432` 查询点/step 和 RTX 4090 计算规模相符。

## 2026-07-30｜WSS-PINN F1 continuity + no-slip 已提交并运行

**本次主要修改**：冻结并提交 WSS-PINN F1 正式配置；配置相对 F0-UP 只新增
continuity + no-slip 两项 0.1 权重，WSS-physics/momentum 保持关闭。完成本地
与 GPU preflight 后，由同一训练代码启动正式训练。

**对应代码/文档**：`wss_pinn/configs/f1_pilot_v2.json`、
`outputs/wss_pinn/runs/PINN-F1-cont-noslip-overfit3-v2ext-s1234-20260730/`、
[WSS-PINN 总入口](WSS_PINN/README.md)、
[WSS-PINN 阶梯矩阵](WSS_PINN/_archive/wss_target_v1_20260730/WSS_PINN_阶梯实验矩阵与进度跟踪.md)、
[WSS 专用推进记录](WSS最小化_代码修改与实验推进记录.md)。

**推进到实验步骤**：GPU preflight Job `11045` 已
`COMPLETED (0:0)`；依赖的 train/eval Job `11046` 已启动并持续写出有限
训练 loss 与 checkpoint。配置 source/resolved SHA256 为
`85ecb2c1…49ff7a` / `8eae38dd…5ad1b1`。

**当前状态判断**：F1 running / pending review，尚无科学结论。完成后必须按
continuity、no-slip、velocity、pressure 和 `WSS_direct` 护栏与 F0-UP
配对判读；当前不得提交 F2。

## 2026-07-30｜WSS-PINN F0-UP completed / Go，F1 解锁

**本次主要修改**：完成 WSS-PINN F0-UP 结果 QA 与文档回填。只读复核 Slurm、best/last、checkpoint/config/data 哈希、逐病例 velocity/pressure/WSS 和相对 F0-U v2ext 的配对变化；CPU 重算 best 评估与落盘 JSON 一致。详细数值与方法学边界由 WSS-PINN 阶梯矩阵维护。

**对应代码/文档**：`outputs/wss_pinn/runs/PINN-F0UP-overfit3-v2ext-s1234-20260730/`、[WSS-PINN 总入口](WSS_PINN/README.md)、[WSS-PINN 阶梯矩阵](WSS_PINN/_archive/wss_target_v1_20260730/WSS_PINN_阶梯实验矩阵与进度跟踪.md)、[WSS 专用推进记录](WSS最小化_代码修改与实验推进记录.md)、`wss_pinn/README.md`、根 `README.md`、`docs/README.md`、`docs/实验设计总纲.md`。

**推进到实验步骤**：Jobs `11043/11044` 均 `COMPLETED (0:0)`；F0-UP best/last 的逐病例 velocity 各项和 gauge-pressure R² 均超过 0.95，判定 completed / Go。F1 launcher dry-run 已返回前级 Gate passed、`would_submit=true`；F1 仍未正式提交。

**当前状态判断**：实现层到 F1，执行层完成 F0-UP 并解锁 F1。F0-UP 是三病例训练期 data-only 能力 Gate，不是物理或泛化结论；continuity/no-slip 的因果检验仍属于 F1。F1 配置只启用 continuity + no-slip，momentum/WSS-physics 强制为 0。

## 2026-07-30｜WSS-PINN 实现到 F1、P0/P1 与 F0-U Gate 完成并进入 F0-UP

**本次主要修改**：独立 WSS-PINN 路线已从 No-Run 落地为可执行包，包含 P0/P1 数据闭环、统一 F0-U/F0-UP/F1 训练评估代码、配置阶段校验、物理/数值单测和统一 Slurm 提交器；详细实现与失败记录由 WSS 专用入口维护。

**对应代码/文档**：`wss_pinn/`、`data_wss_pinn/pilot_v1/`、`outputs/wss_pinn/`、[WSS-PINN 总入口](WSS_PINN/README.md)、[WSS-PINN 阶梯矩阵](WSS_PINN/_archive/wss_target_v1_20260730/WSS_PINN_阶梯实验矩阵与进度跟踪.md)、[WSS 专用推进记录](WSS最小化_代码修改与实验推进记录.md)。

**推进到实验步骤**：P0-A/B/C/D 与 P1 completed；F0-U v1 Jobs `11037→11038` completed 但 velocity R² 未过 0.95，No-Go；F0-U v2 Jobs `11039→11040` completed，但逐病例 `u/v/w/speed` 严格 Gate 仍 No-Go；v2ext Jobs `11041→11042` completed，best/last 严格速度 Gate 最低 R² `0.9888/0.9896`，Go。F0-UP Jobs `11043→11044` 中 preflight completed、训练 running。F1 v2ext code-ready 且 dry-run 通过，但等待 F0-UP 科学 Gate、未提交。

**当前状态判断**：实现层到 F1，执行层已按 Gate 推进到 F0-UP running。F1 只包含 continuity + no-slip，momentum/WSS-physics 强制为 0；exact zone/connectivity 缺失只阻塞 hard flux/RCR。详细哈希、指标、监控与恢复命令见 WSS-PINN 阶梯矩阵和对应 run 的 `submission.json`。

## 2026-07-30｜WSS-PINN 推进至 F1 的执行交接提示词（No-Run）

**本次主要修改**：为下一智能体建立 P0→P1→F0-U→F0-UP→F1 的直接执行提示词，冻结独立目录、配置驱动、Slurm 正式训练和长作业交接规则；详细实施合同由 WSS 专用入口维护。

**对应代码/文档**：[下一智能体目标提示词：实现并推进至 F1](WSS_PINN/_archive/wss_target_v1_20260730/WSS_PINN_下一智能体目标提示词_推进至F1.md)、[WSS-PINN 独立路线总入口](WSS_PINN/README.md)、[WSS-only 专用推进记录](WSS最小化_代码修改与实验推进记录.md)。

**推进到实验步骤**：S0/No-Run；未改训练代码、数据或作业。下一智能体需将 F1 实现完整做成配置驱动，并按科学 Gate 用集群逐级执行。

**当前状态判断**：长训练可在提交并完成状态/日志检查后交接，但未完作业不得记作完成，也不得自动越过结果 Gate 提交下一级。

## 2026-07-30｜WSS-PINN 独立路线与记录体系指针（No-Run）

**本次主要修改**：新增独立 `wss_pinn/` 路线，将 PINN 代码、派生数据、结果和文档与既有 WSS-only/V1/V3 资产隔离；详细阶梯与记录合同由 WSS 专用入口维护。

**对应代码/文档**：[WSS-PINN 独立路线总入口](WSS_PINN/README.md)、[WSS-PINN 阶梯实验矩阵](WSS_PINN/_archive/wss_target_v1_20260730/WSS_PINN_阶梯实验矩阵与进度跟踪.md)、[WSS-only 专用推进记录](WSS最小化_代码修改与实验推进记录.md)、`wss_pinn/{AGENTS.md,README.md}`。

**推进到实验步骤**：S0 隔离与预注册完成，P0 planned；No-Run。

**当前状态判断**：最终任务仍是峰值 WSS，\(u,v,w,p\) 仅作辅助场；P0 未通过前不建立全库 sidecar 或提交训练。

## 2026-07-29｜WSS-only PINN 非滑移壁面与体域采样指针（No-Run）

**本次主要修改**：WSS-only 体域 PINN 真源已将壁面口径精确为非滑移刚性壁面，并冻结“W0 保留 `random5000` 壁面 support、PINN 另加 near-wall/core 体点”的双流分槽方案。

**对应代码/文档**：[体域物理约束与 PINN 训练路线讨论稿](WSS_PINN/_archive/WSS最小化_体域物理约束与PINN训练路线_2026-07-29.md)、[WSS-only 专用推进记录](WSS最小化_代码修改与实验推进记录.md)。

**推进到实验步骤**：仍为 P0/No-Run；未改代码、数据、配置或作业。

**当前状态判断**：壁面 5k 点不能单独支撑体域 Navier–Stokes 残差；后续详细采样合同只在 WSS 专用文档维护。

## 2026-07-29｜WSS-only PINN 非牛顿流变确认指针（No-Run）

**本次主要修改**：用户正式确认使用非牛顿流体；WSS-only 体域 PINN 真源已删除流变待定状态，并冻结 UDF 的剪切率相关黏度为主物理口径。

**对应代码/文档**：[体域物理约束与 PINN 训练路线讨论稿](WSS_PINN/_archive/WSS最小化_体域物理约束与PINN训练路线_2026-07-29.md)、[WSS-only 专用推进记录](WSS最小化_代码修改与实验推进记录.md)。

**推进到实验步骤**：仍为 P0/No-Run；未改代码、数据或作业。

**当前状态判断**：物理方程族已冻结为非滑移刚性壁面、不可压缩、\(\rho=1060\ \mathrm{kg/m^3}\)、统一 Carreau–Yasuda 型广义牛顿流变；constant-\(\mu\) 不进入主路线。

## 2026-07-29｜WSS-only PINN 边界确认与 UDF 审计指针（No-Run）

**本次主要修改**：WSS-only 体域 PINN 路线已回填用户确认的目标、BC、Fluent 导出和 `test36` 决策；只读 UDF/`.cas.gz` 证据把流变口径从“常黏度牛顿”修正为实际挂接的剪切率相关黏度。详细参数和路线影响只在 WSS 专用文档维护。

**对应代码/文档**：[体域物理约束与 PINN 训练路线讨论稿](WSS_PINN/_archive/WSS最小化_体域物理约束与PINN训练路线_2026-07-29.md)、[WSS-only 专用推进记录](WSS最小化_代码修改与实验推进记录.md)。

**推进到实验步骤**：仍为 P0/No-Run；新增 UDF 波形、面积、RCR、流变与单位审计，未改代码或数据，未提交作业。

**当前状态判断**：第一阶段冻结为峰值 WSS、共享入口 \(Q(t)\)、病例分支面积条件、variable \(\mu(\dot{\gamma})\)、现有 `test36` 工程筛选和零新增 Fluent 导出；精确出口面 hard RCR residual 后置。

## 2026-07-29｜WSS-only 体域物理约束/PINN 路线留档指针（No-Run）

**本次主要修改**：新增 WSS-only 体域物理约束第一性原理讨论稿，核清现有壁面直接 WSS 模型、`data_new/` 体内场、v4 bundle 和历史 V1 physics 实现之间的边界；详细事实只在 WSS 专用推进记录维护。

**对应代码/文档**：[体域物理约束与 PINN 训练路线讨论稿](WSS_PINN/_archive/WSS最小化_体域物理约束与PINN训练路线_2026-07-29.md)、[WSS-only 专用推进记录](WSS最小化_代码修改与实验推进记录.md)。

**推进到实验步骤**：仅完成 P0 数据/数值闭环预注册，未改训练代码、数据或作业；下一步先做 cell ID、CFD velocity→WSS、CFD PDE residual 和边界 zone 审计。

**当前状态判断**：不直接在现有壁面 WSS 单头叠加 Navier–Stokes loss；保留当前锚点，候选路线为独立 physics sidecar + 平滑体域 \(u,p\) 神经场辅助分支。本条提出的五项边界已由上方 UDF 审计指针收敛，当前等待的是 P0 数据/单位闭环，不再等待口头边界选择。

## 2026-07-29｜WSS-only H2 + log-radius 两臂完训与回填 ✅

**本次主要修改**：完成 REG-P10-LSA2 SAME-H2 的精确同-seed control / 唯一增加 `log_local_radius` 输入列处理臂，新增严格结果分析与幂等 `.xlsx` 更新工具，并同步 WSS-only 入口、策略和推进文档。

**对应代码/文档**：[PointNet baseline 实验矩阵 §0Q](WSS最小化_PointNet_baseline实验矩阵与进度跟踪.md)、[WSS 高值区域优化方案 N-02](_archive/WSS最小化/WSS高值区域预测优化方案.md)、[WSS-only 训练实验跟踪](WSS最小化_训练实验跟踪.md)、[WSS-only 专用推进记录](WSS最小化_代码修改与实验推进记录.md)。

**推进到实验步骤**：Jobs `11032`、`11033_0/1` 全部 `COMPLETED (0:0)`；两臂 400 epoch、best/last test36、36 例明细和配置完整性通过。结果 JSON/CSV 与工作簿既有三张结果表已回填，仍为5张表、6页。

**当前状态判断**：处理臂 `R²_cb=0.3506`，相对并发 control `ΔR²_cb=+0.0436`、`Δhigh-WSS nRMSE=-0.00295`，全部保护线通过，晋级为新的单 seed 开发锚点。病例全场 R² 均值差CI跨零，故保留单 seed/复用 test36 的限制；暂不做多 seed，下一项可在新锚点上单变量验证解析锚残差参数化。

## 2026-07-29｜WSS-only REG-P10-LSA2 六臂完训与回填指针 ✅

**本次主要修改**：WSS-only 完成 REG-P10-LSA2 上 SAME/IND × MSE/H1/H2 六臂训练、严格复算、文档与工作簿回填；新增结果分析和幂等 `.xlsx` 更新工具。

**对应代码/文档**：[PointNet baseline 实验矩阵 §0P](WSS最小化_PointNet_baseline实验矩阵与进度跟踪.md)、[WSS 高值区域优化方案 §4.4](_archive/WSS最小化/WSS高值区域预测优化方案.md)、[WSS-only 训练实验跟踪](WSS最小化_训练实验跟踪.md)、[WSS-only 专用推进记录](WSS最小化_代码修改与实验推进记录.md)。

**推进到实验步骤**：Jobs `11019→11020_[0-5]` 全部完成，六臂 checkpoint、best/last test36、36 例 CSV 和配置完整性通过；工作簿仍为 5 张表、6 页，未恢复冗余确认页。

**当前状态判断**：IND 和 H1 No-Go；SAME-H2 q90 pinball λ0.20 过 Gate并以 `R²_cb=0.3239` 进入后续输入臂。本节原建议先做 MSE/H2 同-seed复跑；后续用户锁定 H2并执行 H2 control / +`log(local_radius)` 两臂，其完训结果与新锚点裁决见上方最新记录。不做 IND、H1+H2、RCR 架构、面积口径或多 seed。

## 2026-07-28｜WSS-only O0 热点/高值两臂完训与回填指针 ✅

**本次主要修改**：WSS-only 路线停止扩展 RCR，回到 O0 geometry-only；H1 top10 hotspot BCE 与 H2 q90 pinball 两个单变量辅助目标已完成训练、best/last 评估、独立复算、文档和工作簿回填。

**对应代码/文档**：[PointNet baseline 实验矩阵 §0O](WSS最小化_PointNet_baseline实验矩阵与进度跟踪.md)、[WSS 高值区域优化方案 §4.3](_archive/WSS最小化/WSS高值区域预测优化方案.md)、[WSS-only 训练实验跟踪](WSS最小化_训练实验跟踪.md)、[WSS-only 专用推进记录](WSS最小化_代码修改与实验推进记录.md)。

**推进到实验步骤**：Jobs `11012→11013_[0-1]` 全部 `COMPLETED (0:0)`，两臂产物完整性通过；结构化分析和 `.xlsx` 既有三张结果表已更新，工作簿仍为 5 张表、6 页，未恢复已删除的 `D2K64三种子确认`。

**当前状态判断**：H1 `ΔIoU=+0.0170` 未到 `+0.020`，H2 `Δhigh-WSS nRMSE=-0.00072` 未到 `-0.002`；共同保护线均通过，但主 Gate 2/2 未达，No-Go、不组合。当时提出的 O0 IND 单臂已由 REG-P10-LSA2 2×3 并发矩阵取代。

## 2026-07-28｜WSS-only S3 RCR Oracle 完训与回填指针 ✅

**本次主要修改**：WSS-only 最小化路线完成 S3 `PointNeXt-R + LocalGeoPE` 上 O0 geometry、O1a true RCR、O2 shuffled RCR 三臂信息上限实验，并完成配对复算、专用文档和工作簿回填；详细实现与指标不在本文重复。

**对应代码/文档**：[PointNet baseline 实验矩阵 §0N](WSS最小化_PointNet_baseline实验矩阵与进度跟踪.md)、[WSS 高值区域优化方案 §4.2](_archive/WSS最小化/WSS高值区域预测优化方案.md)、[WSS-only 训练实验跟踪](WSS最小化_训练实验跟踪.md)、[WSS-only 专用推进记录](WSS最小化_代码修改与实验推进记录.md)。

**推进到实验步骤**：Jobs `11008→11009_[0-2]%3` 全部完成，三臂完整性通过；结构化分析和 `.xlsx` 的三张既有结果表及新增 `RCR Oracle` 专页均已更新。

**当前状态判断**：O1a 相对 O0 的 physical `ΔR²_cb=+0.0805`，O2 仅 `-0.0033`；真实 RCR 信息判 Go-to-follow-up，后续补稳健性确认和可部署代理评估。

## 2026-07-28｜WSS-only REG-P10 静态 EdgeConv 完训与回填指针 ✅

**本次主要修改**：WSS-only 最小化路线完成 REG-P10 上 SA1、SA2、SA1+SA2 三个静态几何约束 EdgeConv 单变量臂，并完成结果审计、专用文档和工作簿回填；详细实现与指标不在本文重复。

**对应代码/文档**：[PointNet baseline 实验矩阵 §0M](WSS最小化_PointNet_baseline实验矩阵与进度跟踪.md)、[WSS-only 训练实验跟踪](WSS最小化_训练实验跟踪.md)、[WSS-only 专用推进记录](WSS最小化_代码修改与实验推进记录.md)。

**推进到实验步骤**：Jobs `10993→10994_[0-2]%3` 全部完成，父模型加三臂完整性 `4/4` 通过，结构化分析与现有 `.xlsx` 三张结果表均已更新。

**当前状态判断**：三臂物理 `ΔR²_cb=-0.0053/-0.0023/-0.0338`，均未通过主指标和保护线；该静态 EdgeConv 最小实现 No-Go，不扩展动态图或多种子。

## 2026-07-15｜WSS-only PointNet self-max 评价与优化讨论入口同步 ✅

**本次主要修改**：WSS-only PointNet 五组矩阵补齐 `CFD/CFDmax` 对 `Pred/Predmax` 的同点评价、80 例 PostView 图件和负预测护栏；在专用状态真源中写入下一轮优化优先级。

**对应代码/文档**：[PointNet baseline 实验矩阵](WSS最小化_PointNet_baseline实验矩阵与进度跟踪.md)、[WSS-only 专用推进记录](WSS最小化_代码修改与实验推进记录.md)。实现与产物细节仅在 WSS-only 专用记录维护，本文不复制指标表。

**推进到实验步骤**：完成既有结果的无推理后处理回填和讨论准备；未启动下一轮训练。

**当前状态判断**：self-max 证据确认空间型态仍是主要瓶颈；下一步须先冻结开发协议和推理期可得信息，再决定目标/loss/局部结构的最小矩阵。

## 2026-07-14｜WSS-only PointNet baseline 新矩阵文档建立 📄

**本次主要修改**：新建导师 PointNet baseline 实验矩阵与进度跟踪文档，将容量、5000 点随机不放回采样、全局/逐病例归一化和 high-risk position 评价收敛为配对矩阵。

**对应代码/文档**：[PointNet baseline 实验矩阵与进度跟踪](WSS最小化_PointNet_baseline实验矩阵与进度跟踪.md)、[WSS-only 专用推进记录](WSS最小化_代码修改与实验推进记录.md)。本次未修改代码、配置或作业。

**推进到实验步骤**：已建立 `E0/E2/E3 × GLOBAL/CASE` 跟踪口径；详细实验事实和后续状态只在 WSS-only 专用文档维护。

**当前状态判断**：这是文档与预注册口径收敛，不是新实验结果；逐病例公式仍待冻结。

## 2026-07-14｜新增个人 Skill `lab-docs-engineering` 与本仓 PROFILE ✅

**本次主要修改**：落地科研文档工程化 Skill（推进记录、正式入账、知识库整理、明确归档、规范演进、分析短回填）；支持他仓安装后通过向导生成项目内 `PROJECT_PROFILE.md` 特异化。本仓已写入特异化 profile。

**对应代码/文档**：`~/.cursor/skills/lab-docs-engineering/`（`SKILL.md`、`wizard.md`、`templates.md`、`archive-checklist.md`、`boundaries.md`、`examples/`）；`.cursor/skills/lab-docs-engineering/PROJECT_PROFILE.md`。

**推进到实验步骤**：仅工具与文档工程；未改训练/数据/作业。

**当前状态判断**：本仓可直接用「记一下 / 写推进记录 / 初始化实验记录」触发；归档仍须用户明确说「归档」。他仓需先安装个人 Skill 再跑向导。

## 2026-07-12｜WSS-only 第五轮止损归档与第六轮路线补齐 ✅

**本次主要修改**：同步 WSS-only 路线级结论：第五轮诊断性结案、内部工程 No-Go，无达标候选时豁免 OOF/T16；第六轮在不改当前 A/B/D 作业的前提下，预注册 C/E、BC、架构、loss、生成模型和速度→WSS 条件路线。

**对应代码/文档**：[WSS 第六轮计划](WSS最小化_第六轮XYZ尺度诊断计划与执行.md)、[WSS 第五轮归档说明](_archive/WSS最小化/WSS最小化_第五轮结案与归档说明_2026-07-12.md)、[WSS 专用推进记录](WSS最小化_代码修改与实验推进记录.md)。

**推进到实验步骤**：只完成路线归档/预注册，未改训练代码或当前作业。

**当前状态判断**：当前继续等待 A/B/D Jobs `7029–7037`；结果交叉审查前不自动启动后续实验。WSS 详细事实仍只维护在 WSS 专用日志。

## 2026-07-10｜WSS-only 第四轮新增想法第三轮复审 v2.2 ✅

**本次主要修改**：对新增的 EMA、密度错配、multi-start FPS、rot_aug、喉部/狭窄特征等建议进行代码与实测复审。保留 split 完整性、worker/RNG、固定 train-only 分位、A5 密度诊断等 P0/A 项；将 EMA 降为预注册候选，rot_aug 维持历史 No-Go，喉部/狭窄特征先做 branch-aware 定义 QA，不进入本轮首批训练。

**对应代码/文档**：更新 [WSS最小化_第四轮优化计划执行总结与归档](_archive/WSS最小化/WSS最小化_第四轮优化计划_执行总结与归档_2026-07-10.md)、[WSS最小化_训练实验跟踪.md](WSS最小化_训练实验跟踪.md) 与 WSS 独立推进记录。本轮对既有 `r3_clean_xyzgeom_tgtw_s1234` 作只读 A5 推理诊断：同一 FPS-2000 点上，子采样/全量推理输出平均标准化 RMSE=`0.2278`、Pearson=`0.9381`（8 个 val 病例）；未改代码、未训练。

**推进到实验步骤**：Stage A 先补齐 A5 其余两个 seed、邻域 cap 审计、sampler/weight 完整性测试和 repeated-holdout；待证据满足闸门再开结构或特征消融。

**当前状态判断**：密度敏感性是明确的新诊断，不是已验证的修复方向；第四轮仍以 raw B0 与严格开发协议为锚点。

## 2026-07-10｜WSS-only 第四轮计划完成交叉审核 ✅

**本次主要修改**：以当前代码和 77 例/第三轮实测证据重写 WSS-only 第四轮计划，纠正 NLL 因果过度、全局法向方案、入口流量优先级和逐配置使用 test 的问题；补入 persistent worker 采样状态、fold-specific WSS stats、hotspot 定位指标和分阶段闸门。

**对应代码/文档**：主文档为 [WSS最小化_第四轮优化计划执行总结与归档](_archive/WSS最小化/WSS最小化_第四轮优化计划_执行总结与归档_2026-07-10.md)，同步更新 WSS 训练跟踪、独立推进记录、`docs/README.md` 与 `training_wss_min/README.md`。本轮未修改代码或配置。

**推进到实验步骤**：第四轮已具备 Stage A（正确性/假设闸门）→B（低风险修复）→C（目标/信息增量）→D（锁定/最终报告）的执行顺序，尚未开始实现或提交实验。

**当前状态判断**：应先保证开发协议和采样/权重实现正确，再讨论模型上限；legacy test16 只能作为历史比较集，后续配置选择必须依赖 val/repeated holdout。

## 2026-07-10｜WSS-only 第三轮结果回填与历史文档归档 ✅

**本次主要修改**：完成 WSS-only clean-data 第三轮 6 个 run 的三 seed 判读，收敛下一轮 P0–P3 优先级；将已完成的预处理交接、工程建议与问题诊断移入 WSS 专属归档，避免 `02-推进与变更/` 当前入口继续混放已执行清单。

**对应代码/文档**：详细指标与判读见 [WSS最小化_训练实验跟踪.md](WSS最小化_训练实验跟踪.md)；变更凭证见 [WSS最小化_代码修改与实验推进记录.md](WSS最小化_代码修改与实验推进记录.md)；归档索引见 [_archive/WSS最小化/README.md](_archive/WSS最小化/README.md)。同步更新根 `README.md`、`docs/README.md` 与 `training_wss_min/README.md`。本轮未改代码、配置或模型产物。

**推进到实验步骤**：WSS-only 第三轮从“训练产物待分析”推进到“完训判读完成”；target-weight 三 seed test `R²_field=0.225±0.034`，但 high-WSS `R²=-1.531±0.167`，下一轮先修 val 选模与早停，再做尺度/边界条件信息探针。

**当前状态判断**：target-weight 为均值正收益但尚非稳定突破；第三轮 clean-data 组合改善峰值校准而未突破整体 R² 平台，且跨轮差异不是 stats 单变量消融。WSS 详细事实继续只写独立推进/训练跟踪文档，本条作为全项目知识库同步指针。

## 2026-07-07｜推进记录拆分：WSS-only 最小化日志独立 ✅

**本次主要修改**：
- 新建 `docs/02-推进与变更/WSS最小化_代码修改与实验推进记录.md`，将 `pipeline_wss_min/` 的坐标配准、STL-点云 QA、同框叠加图和后续点数扫描记录单独维护。
- 本文件后续保留 V3P / 训练主线 / 通用代码修改记录，只在必要时放置跨路线指针，避免 WSS-only 最小化路线被 V3P 大量训练记录淹没。
- 更新 docs 索引，使 WSS-only 最小化路线可以从交接记录、代码 README 和独立推进记录三处进入。

**对应代码/文档**：
- 文档：`docs/02-推进与变更/WSS最小化_代码修改与实验推进记录.md`、`docs/02-推进与变更/代码修改与实验推进记录.md`、`docs/README.md`

**推进到实验步骤**：
- 推进到项目记录分层：V3P 训练主线与 WSS-only 最小化数据线分开维护，后续复盘、汇报和继续实验时可以按路线直接定位。

**当前状态判断**：
- WSS-only 最小化详细记录已迁移到独立文档；本文档从下一条起继续承接 2026-07-06 及之后的 V3P/主线训练记录。

## 2026-07-06｜5933 K7-Ext200 **No-Go** + 5935 K9 v2 **全项 Go** 判读 · K9 sidecar 全量生成 · K8 双分量头 + L_ray 落地并提交两档 probe（5938/5939）🚀

**判读**（详见跟踪日志 2026-07-05/07-06 块）：

1. **K7-Ext200（5933）No-Go**：test best_wss `wss_r2_wss` **0.367**（≤0.409 红线；`r2_p` 0.943 正常），而 **val `wss_r2` 升到 0.491**——训练越久 val 越好 test 越差，5907「尾段 val 上升」被证实是过拟合 val 分布的假信号。与 5886 同属 **val/test 选优错位**（已两次独立实锤）→ K7 延长训封口、收益封顶 40ep +0.009、3-seed 不解冻；「顺手修 val 选优（case-level 中位数/复合指标）」优先级提升，修好前不再开 >40ep 长训。
2. **K9 审计 v2（5935）全项 Go**：73 case/87.6 万点，覆盖率 **1.000** · SB 校正稳定性 stream/cross **0.841/0.878** · cross 对齐 calibrated R² **0.704**（优于 v1 0.671 与 5905 oracle 0.632）。v1 `weak_no_go` 确认为嵌套 k=5 口径系统性低估。**横向 WSS 第一次拥有可靠的逐点直接监督目标**。

**落地（Go 后当日执行）**：

- **K9 sidecar 全量生成**：新增 `run_v3p_k9_ray_sidecar_gen.py` + 同名 slurm（Job **5936** + **5937** 补 val case）。**81 case/6,561 图/覆盖率 1.000/0 跳过**；逐图 `processed/ray_targets_k16/*.pt`（a1 双通道 + t̂_s/t̂_c frame + valid mask，节点对齐，全量≈3GB）；train 池化 `a1_scale_pooled_std` **78.717 Pa** 作共享目标尺度（保方向组成）。教训：审计口径是 train+test，训练还需 **val_cases**——5936 漏了 8 个 val case，5937 修复（脚本已改为 train+val+test）。
- **K8 双分量头 + L_ray 代码**：`wss_profile_head.py` 新增 `DualProfileWSSHead`（2×n_basis 系数、`WSS_vec=μ(a1_s·t̂_s+a1_c·t̂_c)`、`last_a1` 供 L_ray；**参数量 2,310,162 与 K7 恰好一致**）；`models.py` `wss_profile_variant` 分支；`data.py` dataset 接 sidecar（缺失/错位即报错）；`losses.py` `lambda_wss_ray`（L_ray = 壁面∩valid 的 a1 MSE）+ breakdown 字段；`config.py` 字段与校验（dual 需 sidecar、λ_ray 需 dual、sidecar 禁旋转/反射/缩放增强——t̂_c 赝矢量）。自检 `check_k8_dual_profile_head.py` 5 项全过（旧配置回归/前反向/拼接对齐/λ=0 零项/校验报错）。
- **提交（sbatch·GPU·40ep）**：Job **5938** `V3P-K8-DualProfile-Probe-post5463_seed1`（K8-only）；Job **5939** `V3P-K8-DualProfile-Ray-Probe-post5463_seed1`（K8 + λ_ray=0.1）。判据：Gate-1 ≥0.429 且 `r2_p`>0.90；**Gate-2 主判据 `wss_x/y` R²>0.10**；No-Go ≤0.409。配对归因：5939−5938 = L_ray 贡献；5938−5907 = 双分量头结构贡献。
- **后处理脚本兼容 ✅（当日补齐）**：`eval_field.py` / `predict_field.py` / `eval_field_by_domain.py` / `recompute_dual_test_metrics.py` / `run_v3_i7_ckpt_probe.py` / `run_v3_diag00.py` / `run_efficiency_benchmark.py` 的 dataset 构造统一接 `ray_sidecar_subdir`/`ray_target_scale`（均取自 config，旧配置默认空串→行为不变）；`run_efficiency_benchmark.py` 模型构建改走 `build_field_model_from_config`（此前手拼 kwargs 对 mlp2/profile 等新式 head 的 ckpt 本就会加载失败）。端到端验证：旧 K7 5907 run（scalar·无 sidecar）与 K8 5938 训练中 ckpt（dual·自动加载 sidecar）同一 eval 路径前向均过。`pretrain_field_ssl.py`（无 WSS 头）与 `archive/` 内脚本不动。

**回填**：跟踪日志 2026-07-05 块判读更新 + 2026-07-06 新块 · README §0/§0.1 · 本记录。

---

## 2026-07-05｜前沿方向 §15 增补（K8–K12）+ K7-Ext200（5933）/K9 射线目标审计（5934）提交 🚀

**判读修正（本次增补的出发点）**：把 5905 oracle 与 K7 实现并排复核发现——oracle 是对 stream/cross **分别**拟合 `a1_s`/`a1_c`（cross 0.632），而 K7 落地的是**单标量** `a1 × t_hat`（横向只能靠 0.1 倍 dir 修正），且现配方 no-velocity（`lambda_vel_int=0`），剖面系数是从未获直接监督的纯 latent。→ 5907 Gate-2 不过不能归因「head 结构无用」：横向通道**输出侧被结构掐死 + 信号侧从未有监督**。另核实：「预测速度 oracle」探针不可行（no-velocity 配方下预测速度未训练），由 K9 Gate-0 审计替代。

**文档**：[前沿方向与见解](../01-任务/任务A/03-V3路线/00-历史主线/WSS精度突破的前沿方向与见解_2026-07-04.md) 升 v3，新增 §15（K8–K12 + 优先级依赖表）：
- **K8** 双分量局部系剖面头（coef head 出 2×n_basis，`WSS=μ(a1_s·t̂_s + a1_c·t̂_c)`，frame 推理期可得）；
- **K9** 剖面射线直接监督（GT `(a1_s,a1_c)` sidecar + `L_ray` 辅助 loss，横向首次逐点直接监督）⭐；
- **K10** 近壁速度多任务恢复（config-only 缝）· **K11** 幅值-角度解耦回归（Gate-2 备选攻角）· **K12** 系数场平滑先验（K9 目标噪声的稳定器）；
- 顺手修条目：val/test 选优错位（5886 best_model 反超 best_wss）。

**代码/配置**：
- 新增 `training/scripts/run_v3p_k9_ray_target_audit.py` + `training/cluster/run_v3p_k9_ray_target_audit.slurm`（覆盖率/k 稳定性/目标-GT 对齐三判据；smoke 6 例过：覆盖率 1.0 · cross 对齐 0.70 · 稳定性 ~0.73 待全量）；
- 新增 `V3P-K7-ProfileWSS-Ext200-post5463_seed1.json`（5907 配方单变量 epochs 40→200，已 diff 校验仅 epochs+meta 变化）。

**提交（sbatch）与结果**：
- **Job 5933**（GPU）K7-Ext200 延长训 🚀：Go = best_wss ≥ 0.45 → 解冻 3-seed；≈0.438 → 判延长训无增益；
- **Job 5934**（CPU node03）K9 Gate-0 审计 v1 ✅ **边界**：73 case · 87.6 万点 · 覆盖率 **1.000** · cross 对齐 calibrated R² **0.671**（优于 5905 的 0.632）· k-稳定性 stream/cross **0.752/0.771**（略欠 0.80）→ 形式 `weak_no_go`。复核：嵌套 k=5 子集拟合系统性低估可靠性。
- **Job 5935**（CPU node03）K9 审计 v2 复审 🚀：脚本加 `--stability-mode split_half`（独立半样本 + Spearman-Brown 校正）+ `--k-full 16`；smoke 校正后 cross 稳定性 **0.858** 过线、对齐升 **0.723**。v2 Go 后生成逐图 sidecar → K8+K9 Gate-1 立项。产物 `…_v2.json` 不覆盖 v1。

**回填**：跟踪日志顶块 · README §0/§0.1/§6 · 本记录。

---

## 2026-07-04｜K6 RigidAug 中性 + K7 ProfileWSS **Gate-1 Go**（5906/5907 完训判读）✅

**结果**（40ep 单变量 probe · 对照 I6-diag best_wss **0.429**，均 best_wss 口径）：

| run | `wss_r2_wss` | Δi6 | `wss_x`/`y` | `r2_p` | val 尾段 |
| --- | ---: | ---: | ---: | ---: | --- |
| **K6 RigidAug 5906** | 0.428 (ep6) | −0.001 | ~0 | 0.933 | 无上升势（前10ep 0.417→后10ep 0.400） |
| **K7 ProfileWSS 5907** | **0.438 (ep25)** | **+0.009** | 0.000/0.013 | **0.940** | 仍上升（0.396→0.437） |

**判读**：
- **K6 中性**：归一化感知刚体增强无害但 40ep 未兑现泛化收益 → 不扩 seed、不加档；`rigid_transform_augment` 留作组合开关。侧证「几何过拟合」非当前主瓶颈。
- **K7 Gate-1 Go**：可微剖面 head 40ep 即超 200ep 母版（+0.009）且 `r2_p` 0.940 为 band 内最高、val 尾段仍上升——结构收益体现在幅值/主分量与训练稳定性；**Gate-2 横向未过**（x/y ≤0.013 < 0.10），维持「横向瓶颈在监督信号而非 head 结构」判断（与 G1-a0 VN 一致）。
- **下一档候选**：K7 3-seed 复核（seed2/3 配置已生成待命，**用户指示暂缓提交**）+ 80–200ep 延长训。
- **对比图**：`analysis_compare/v3p_k7profilewss_vs_i6diag_20260704/`（含 K6/K7/I6 三线 val 曲线）· `v3p_k6rigidaug_vs_i6diag_20260704/`。

**回填**：跟踪日志顶块 ③④ 小节 · 待办下一跳 4/5 条 · README 当前状态与 §0.1。

## 2026-07-04｜5886 弱 No-Go 判读 + 剖面 WSS oracle Go + K1 L3 局部强改善 + K6 RigidAug / K7 ProfileWSS probe 提交 ✅🚀

**五件事**（前沿方向文档 §3/§10.1 的首批落地）：

1. **5886 K1-lite 判读：弱 No-Go**。best_wss test `wss_r2_wss` **0.400**（Δi6 −0.029 ≈ 历史 GeomB1 0.396）；best_model **0.436** 未过 Go 0.459；x/y≈0。异常：best_model 反超 best_wss，提示 val/test 选优错位。已回填跟踪日志/待办/README/后平台期路线 §7。
2. **K1 L3 分解（Job 5904）修正判读**：best_wss 口径 L3_fast **+0.207**、L3_CHEN **+0.749**（−0.825→−0.077）——branch 特征在失败簇**局部强改善**，被全局重分布抵消。K1 改判「局部有效、全局中性偏负」，入 G5 叙事；仍不扩 seed。新脚本 `training/cluster/run_v3p_k1_l3_eval.slurm`。
3. **剖面 WSS Gate-0 oracle（Job 5905）：Go**。新脚本 `training/scripts/run_v3p_profile_wss_oracle.py`：壁面点沿估计内法向取近壁体点拟合 `u_t(η)=a1η+a2η²`、`WSS=μ·a1`。73 case/32.8 万点：cross calibrated R² **0.632**（单点差分仅 0.166）、stream 0.574、幅值 0.468。**横向 WSS 信息在近壁剖面中可恢复**，历史 VelGrad 失败是求导方式问题。→ §3 可微剖面 head 立项依据成立（上界口径：oracle 用 GT 速度）。
4. **旋转/反射增强落地 + K6 probe（Job 5906 · GPU 40ep 运行中）**：
   - `pipeline/augmentation.py`：新增 `rigid_transform_augment`（**denorm→物理系 R→renorm**，修复旧 `random_rotation` 直接旋转各向异性 z-score 分量致物理速度失真 ~7–8% 的问题）；`mirror_augmentation` 补 torsion/local wss_circ 变号；`apply_augmentations` 接 `rigid_rotation_prob`/`rigid_reflection_prob`/`norm_params_file`。
   - 新自检 `training/scripts/check_augmentation_equivariance.py` 全过（物理模长 max|Δ|<1e-6、torsion 变号）。
   - 配置 `V3P-K6-RigidAug-Probe-post5463_seed1`（I6-diag 配方 + rigid 0.5/0.25）；Go ≥0.449 或不劣化+尾段上升。
5. **§3 可微剖面 WSS head 落地 + K7 probe（Job 5907 · GPU 40ep 运行中）**：
   - 新增 `training/core/wss_profile_head.py`（`DifferentiableWSSHead`：剖面系数 a1..a3 + 切向修正 → `WSS = mu·a1·t_hat` + 逐通道仿射校准）；`models.py` 加 `wss_output_mode="profile"` 分支与 `wss_profile_n_basis/mu` 传参；`config.py` 加字段与校验（仅 pointnext · wss_dim=4 · global · 与其他 wss head/context 互斥 · 禁 warm-start）。
   - smoke 全过：profile 前向/反向梯度有限、I6-diag 旧配置回归不受影响。
   - 配置 `V3P-K7-ProfileWSS-Probe-post5463_seed1`（I6-diag 配方单变量替换 head，不含 rigid 增强）；Gate-1 Go = ≥0.429 且 `r2_p>0.90`；横向信号 `wss_x/y>0.10`。
   - 5886 vs I6-diag 对比图补齐：`outputs/field/plots/analysis_compare/v3p_k1branchfeat_vs_i6diag_20260704/`（metrics_compare + training_history）。

**下一步**：等 5906（K6 RigidAug）与 5907（K7 ProfileWSS）完训判读；两者均为 40ep 单变量 probe，任一 Go → 3-seed 复核，双 Go → 组合立项（增强 × 剖面 head）。

---

## 2026-07-04｜任务 A 实验全历程梳理文档（导师汇报单页入口）✅

**文档**：[任务A_实验全历程梳理_导师汇报_2026-07-04.md](../03-汇报材料/任务A_实验全历程梳理_导师汇报_2026-07-04.md)

**内容**：V1→V2→V3P 时间线、分阶段关键实验与判读、平台期指标总表、外部 baseline（CROWN/PointNetCFD）、三重瓶颈、M-N/M-E 分轨、汇报结构建议与 Q&A；单文档替代分散阅读跟踪日志/推进记录/状态表。

---

## 2026-07-04｜WSS 综述 v4（数据增强等变正确性 + 各方案注意事项）✅

**本次主要修改**（回答"旋转/反射增强是否需重跑 Fluent + 矢量/标量变换避坑"）：
- 扩写 §10.1 数据增强为五个子节：
  - §10.1.1 **是否重跑 Fluent**：结论=刚体旋转/反射**不需要**（NS 在欧氏群下等变，只需解析变换标签）；缩放需相似律近似、须 Gate-0；弹性形变才需低保真重算。给出增强类型 × 是否重跑对照表。
  - §10.1.2 **逐字段变换规则表**：对齐 `pipeline/config.py` 确切索引，区分真标量/真矢量/赝矢量/赝标量；含 global 与 local(`wss_axial/circ/rad`) 两套 WSS frame。关键点：local 分量旋转下天然不变，**旋转增强不可再转 local 分量**。
  - §10.1.3 **反射三陷阱**：torsion（赝标量）变号、wss_circ（叉积赝标量）变号、涡量/helicity 类按 det(R) 变号；推荐"反射后从镜像几何重算派生量"而非手工记符号。
  - §10.1.4 **归一化与 kNN 坑**：按分量独立归一化会被旋转打乱→须各向同性或在物理空间增强；kNN 图刚体不变可复用。
  - §10.1.5 稳健代码钩子（区分 det=±1、处理赝量）+ 等变一致性自检。
- 新增 §10.7 **各优化方案通用注意事项与坑**：预训练（域偏移/只加载 encoder/分层 lr/归一化一致）、消息传递（相对位移平移不变/动态图开销/按 batch 隔离/over-smoothing）、残差深度（小样本过拟合/LayerScale 初值/勿混 EMA）、PINN 矫正式（λ≤1e-2 红线/只约束推理期可得量/TTR 早停）、损失归一化优化（instance norm 尺度还原/SAM 双倍成本/PCGrad 先测梯度冲突/Laplacian PE 符号歧义）、通用（一次一变量/eval 关增强/防 val 假阳性/归因四层）。

**对应代码/文档**：文档 only（综述 v4 + 本推进记录）；未改代码、未提交实验；代码片段为示意。

**当前状态判断**：等变正确性说明与本项目坐标系负担诊断（§1 约束1）一致；旋转/反射增强是零 CFD 成本的低风险起手项，可与 K1-lite 5886 并行。

## 2026-07-04｜WSS 精度突破前沿方向综述 v3（新增系统性优化章节）✅

**本次主要修改**（在 v2 详版基础上扩写）：
- 新增 §10 **系统性优化方向补充**，把训练系统拆成六个可独立调的子系统，每个都标注「是否已被本项目验证 / 差异化新做法 / Go-No-Go / 代码钩子」：
  - §10.1 **数据增强**（保物理的 SO(3) 旋转/缩放+BC 标度/弹性形变/近壁抖动；禁 mixup；`augment.py` 旋转钩子，WSS 向量同步旋转）。
  - §10.2 **预训练**（带物理的掩码场重建 MFM、跨时间步对比、V3D 预训练→AG 微调、外部 PDE 基础模型；只加载 encoder 的 warm-start 钩子；区别于已 No-Go 的 G3 几何 SSL）。
  - §10.3 **消息传递**（各向异性/方向消息 EdgeConv、注意力聚合、门控更新、BSMS 层次、动态图、中心线虚拟节点；`_local_pool_directional` 钩子）。
  - §10.4 **残差/深度**（LayerScale、Stochastic Depth、DenseNet 跳连、物理残差 WSS₀；强调是结构容量 ≠ 已 No-Go 的 EMA/SWA 事后选优）。
  - §10.5 **PINN 矫正式使用**（区分"主损失式 PINN 已 No-Go" vs "矫正式：低权重软正则 / 推理期精修 TTR / 可微物理层"；`physics_soft_regularizer` 钩子，权重 1e-3 红线）。
  - §10.6 **其他系统级杠杆**（谱域/Sinkhorn/focal loss、per-case instance norm/RevIN、SAM、PCGrad 多任务梯度手术、Laplacian PE、图重连）。
- 新增 §11 **全部方向优先级总表**（ROI/成本/是否差异化），并给出建议起手三件套（旋转增强 + LayerScale + 剖面 oracle，均可与 K1-lite 5886 并行）。
- 后续章节顺延：工程化 checklist §12、边界 §13、总结 §14。

**对应代码/文档**：
- 文档 only：上述综述 v3、本推进记录。
- 未改训练/评估/预处理代码；未新增配置；未提交或启动实验（规划层）。文档内代码片段为落地示意，尚未落盘。

**当前状态判断**：
- 所有新增方向均对照项目已 No-Go 证据标注差异化点（保物理增强/物理目标预训练/结构容量残差/矫正式 PINN 均属项目未系统尝试或换范式，非换名重跑）。
- 与在跑的 K1-lite Job 5886、后平台期路线、平台期复盘 §8 口径一致；不改变当前唯一执行主线。

## 2026-07-04｜WSS 精度突破前沿方向综述 v2 详版（规划层 · 作业）✅

**本次主要修改**：
- 扩写 [WSS精度突破的前沿方向与见解_2026-07-04.md](../01-任务/任务A/03-V3路线/00-历史主线/WSS精度突破的前沿方向与见解_2026-07-04.md) 为工程化详版（v2）：
  - 新增 §2 **通用 Go/No-Go 五级闸门框架**（Gate-0 oracle → Gate-1 seed1 → Gate-2 横向/hotspot → Gate-3 L3 → Gate-4 扩 seed），并给出每级 No-Go 的降级动作。
  - 每个主线方向补 **核心代码修改片段**（贴合 `FieldPointNeXt`/`ModelConfig`/`losses.py` 现有接口）：§3 两阶段可微 WSS head（`DifferentiableWSSHead` + `wss_output_mode="profile"` 分支 + config 校验 + oracle 脚本骨架）；§4 `PhysicsSliceAttention`（Transolver 风格 slice token）；§6 `ResidualDiffusionWSSHead`（均值+去噪残差+UQ 采样）。
  - 新增 §9 **8 条创新补充想法**（中心线虚拟节点、解析 WSS₀ 残差、谱域 loss、per-case 校准头、旋转增强蒸馏、CFD ensemble 蒸馏、课程学习、剖面速度多任务）。
  - 新增 §8 P0–P3 路线图（含并行原则）与 §10 工程化 checklist（配置命名/禁 warm-start/四层评估/回填口径/集群提交）。

**对应代码/文档**：
- 文档 only：上述综述 v2、本推进记录。
- 未改训练/评估/预处理代码；未新增配置；未提交或启动实验（规划层）。文档内代码片段为落地示意，尚未落盘。

**当前状态判断**：
- 结论沿用项目共识：平台期是「表示+信息+数据」三重瓶颈；最高投产比为 §3 可微 WSS head 与 §4 slice-attention（复用现 backbone，低成本）。
- 与在跑的 K1-lite Job 5886、后平台期路线、平台期复盘 §8 口径一致；P0 全 CPU 可与 5886 并行，不改变当前唯一执行主线。

## 2026-07-04｜实验记录表补录 + 导师汇报梳理 ✅

**任务**：对照 `outputs/field/experiment_index.csv`（169 条 field run）与 `docs/00-规范与记录/实验记录表.xlsx`，补录 2026-06-07 以来未同步的 V3P 实验；同步梳理任务 A 全历程（V1→V2→V3P）与 external baselines（PointNetCFD · CROWN）。

**xlsx 回填**：
- 扩展 `docs/03-汇报材料/tools/sync_experiment_xlsx_gaps.py`：`GOALS`/`NOTES` 新增 14 个 exp 族（G54a/G57/G58 基线 · 路径 I · M-E 探针 · J4/J5/K1 平台期 probe）。
- 运行脚本新增 **19 行**（`taskA_field` + `experiment_master`）；`xlsx` 现 **160 行**，与 `experiment_index.csv` **零缺口**。
- 备份：`实验记录表.xlsx.bak_2026-07-04`

**关键补录结论（best_wss · test `wss_r2_wss`）**：
| exp_id | wss | 判读 |
| --- | ---: | --- |
| `V3P-I6-diag-AsymW-a-post5463` | **0.429** | 平台期带宽参考 |
| `V3P-I6-a-AsymW-a-post5463` | **0.446** | M-E 短训 Go（未扩 seed） |
| `V3P-G-58-SSL-Finetune` | **0.435–0.440** | G3 No-Go |
| `V3P-J5-ActiveData` | **0.433** | 弱 No-Go |
| `V3P-K1-BranchFeat` | **0.400** | No-Go（5886 完训） |

**外部 baseline**：CROWN/PointNetCFD **不写入** xlsx（规范分表）；汇报材料见 `external_baselines/crown_beihang/experiments/CROWN_非PINN与PINN复现汇报_合并.md` 与 `docs/paper_reproduction/papers/pointnetcfd/梳理记录.md`。

---

## 2026-07-01｜V3P 后平台期优化路线复核重写 ✅

**本次主要修改**：
- 重写 [V3P_后平台期结构与训练优化路线_2026-07-01.md](../01-任务/任务A/03-V3路线/01-执行与待办/V3P_后平台期结构与训练优化路线_2026-07-01.md)：按路径 A-F、路径 G、路径 I、M-E oracle、平台期 Ideas A-G 的既有证据，重新划分“已封口 / 不作为主线 / 特定前提才重开 / 当前唯一可执行”。
- 将上一版里容易重复历史实验的 K3 mode-scale、K5 temporal/BC、K4 物理一致性、多尺度 kNN 等从“推荐候选”降级为已验证或仅低成本诊断方向。
- 同步 [V3 README](../01-任务/任务A/03-V3路线/README.md)、[V3 后续优化待办](../01-任务/任务A/03-V3路线/01-执行与待办/V3_后续优化待办.md)、[docs 总索引](../README.md)：当前 M-E 内部训练只保留 K1-lite BranchFeat Job **5886** 判读，不并行重开 K3/K5/G4/E-J/J6。

**对应代码/文档**：
- 文档 only：V3P 后平台期路线、V3 README、V3 后续优化待办、docs 总索引、本推进记录。
- 未改训练、评估、预处理代码；未新增配置；未提交或启动新实验。

**推进到实验步骤**：
- V3P 后平台期从“多方向结构候选”收敛为“历史证据压缩后的单线判读”：等待 `V3P-K1-BranchFeat-MixedProbe-post5463_seed1`（Job 5886）完成。
- 5886 若不过 Go，不自动升级 full dual-graph；若过 Go，下一步也必须证明它与旧 GeomB1 / G01+G04 的本质差异。

**当前状态判断**：
- 当前不是还有一批未试方向可横向铺开的阶段；大多数直觉路线已经有 oracle 或 GPU No-Go。
- K1 的 CPU error predictor 有明确正信号，但和历史 branch 特征 No-Go 接近，因此只能短训验证，不能提前写成新主线突破。

## 2026-07-01｜V3P 后平台期 P0 探针 K1/K3/K5 完成 + K1-lite GPU 配置 ✅

**P0 CPU 探针**（I6-diag 预测误差 · `run_v3p_post_plateau_probes.py`）：

| 探针 | 判读 | 关键指标 |
| --- | --- | --- |
| **K1** branch error | **Go ✅** | 病例级 holdout 解释 **68.3%** 分支平方误差（阈值 30%） |
| **K3** mode-scale | **No-Go ❌** | 去尺度 mode R2 **0.170 <** abs **0.235**；scale 校准增益 **−0.012** |
| **K5** temporal/BC | **No-Go ❌** | lag1 **0.99** 但 fast≈slow；BC 解释 **−0.43** |

**产物**：`f0_decision/v3p_k1_branch_error_probe_20260701.json` · `v3p_k3_mode_scale_audit_20260701.json` · `v3p_k5_temporal_bc_probe_20260701.json` · `v3p_post_plateau_probes_summary_20260701.json`

**P1 下一跳**：按路线优先级仅 **K1** 过线 → 新增 `V3P-K1-BranchFeat-MixedProbe-post5463_seed1.json`（启用 `dist_to_bifurcation` + `branch_id` · 40ep · Go ≥ I6-diag **+0.03**）；**Job 5886 已提交 GPU**。

**代码**：`training/scripts/run_v3p_post_plateau_probes.py` · `training/cluster/run_v3p_post_plateau_probes.slurm`

---

## 2026-07-01｜V3P 后平台期结构与训练优化路线 + 活跃目录瘦身 ✅

**本次主要修改**：
- 新增 [V3P_后平台期结构与训练优化路线_2026-07-01.md](../01-任务/任务A/03-V3路线/01-执行与待办/V3P_后平台期结构与训练优化路线_2026-07-01.md)：把后续精度提升从“小补数据/小调参”切换到 K1 中心线/分支 dual-graph、K2 多尺度壁面算子、K3 mode-scale-hotspot 目标、K4 预测态物理一致性、K5 temporal/BC 条件化、K6 几何约束增强、K7 分阶段训练协议。
- 更新 [V3 README](../01-任务/任务A/03-V3路线/README.md)、[V3 后续优化待办](../01-任务/任务A/03-V3路线/01-执行与待办/V3_后续优化待办.md)、[docs 总索引](../README.md)：将后平台期结构路线升为当前 M-E 精度优化入口。
- 将 G3/S0 已封口 stub 从 `01-执行与待办/` 移入 `03-V3路线/_archive/2026-07-01_活跃目录瘦身/`，并修复相关历史链接。

**对应代码/文档**：
- 文档 only：V3P 后平台期路线、V3 README、V3 后续优化待办、V3 实验执行跟踪日志历史链接、docs 总索引、本推进记录。
- 未改训练、评估、预处理代码；未新增配置；未提交或启动新实验。

**推进到实验步骤**：
- V3P 在 E-J、G4-c、J4/J5 数据线封口后，从平台期 oracle/主动选数收口，推进到“结构级可证伪探针”阶段。
- 下一批建议先做 CPU/短探针：branch/topology error predictor、mode-scale target audit、temporal/BC error autocorrelation；只在探针过线后选择一个 GPU probe。

**当前状态判断**：
- 当前精度平台更像是局部 kNN 表示、绝对 WSS 目标、共享 backbone 训练动力学与上游/分支条件缺失共同造成，不建议继续 AsymW/lr/dropout/WSS-only/PWeak 或 J6 GPU 原样扩展。
- M-N/G5 叙事可继续使用；M-E/临床替代 CFD 仍未达标，需要结构/target/训练协议层面的新证据。

## 2026-07-01｜外部优化建议对照表（P0–P4 vs 项目证据）✅

**文档**：[V3P_精度平台期复盘 §8](../01-任务/任务A/03-V3路线/01-执行与待办/V3P_精度平台期复盘与下一轮想法_2026-06-30.md#8-外部优化建议对照表2026-07-01)

**内容**：外部 AI 提出的标签审计 / 改 WSS 表示 / pipeline 特征 / 物理约束 / Pa p95 叙事，映射至 Idea A–G、路径 A/B/G2、J4/J5 等；标注 🟢已完成 · 🟡部分闭合 · 🔴已封口；重排 M-N/M-E 执行优先级。

**索引**：`V3_后续优化待办` M-E 讨论入口已链至 §8。

---

## 2026-07-01｜平台期 G5 / 主动选数文档归整 ✅

**改动**：`01-执行与待办/` 下 9 份平台期专项文档迁入子目录 `平台期-G5与主动选数/`；v2–v4 主动选数清单入 `_history/`；v5 升格为 [主动选数清单.md](../01-任务/任务A/03-V3路线/01-执行与待办/平台期-G5与主动选数/主动选数清单.md)；新增子目录 [README](../01-任务/任务A/03-V3路线/01-执行与待办/平台期-G5与主动选数/README.md) 作导航入口。

**脚本**：`run_v3p_phase1_active_data.py` · `run_v3p_pipeline_procurement_rank.py` 输出路径同步至子目录。

---

## 2026-07-01｜V3P Batch-2 CPU 采购 + Phase1 v5 重评（5884→5885）✅ · **phase1_pass**

**Batch-2**（Job 5884 · rank-skip 6 · 排除 Batch-1）：6 例 QA 全过
- `AAA/unruputer/LI_YOU_YU` · `LIU_JIE` · `AAA/ruputer/LIN_LIANG_XIAO`
- `ILO/XIE_ZHI_FU-0/before` · `LI_FA_XIANG-1/after` · `WEI_QING_FENG-1/after`

**Phase1 v5**（Job 5885 · Batch-1+2 并入 · 对照 J5）：

| 门禁 | v4 | **v5** |
| --- | ---: | ---: |
| QA 可入训 | 7 | **13** |
| neighbor 覆盖 | 5/5 | **5/5** |
| explained | 0.437 | **0.437** |
| **phase1_pass** | ✅ | **✅** |

**split 草案**：`split_AG_active_v3.json`（train **+12** 外域 · top-n-draft 12）

**判读**：数据门禁仍过线，但 **J5 已弱 No-Go（0.433）→ J6 GPU 仍封口**；v5 split 仅作 M-E 采购记录，不自动开 GPU。

**产物**：`v3p_procurement_batch2_20260701.txt` · `v3p_active_selection_v5_20260701.json` · [主动选数清单 v5](docs/01-任务/任务A/03-V3路线/01-执行与待办/平台期-G5与主动选数/主动选数清单.md)

---

**作业链**：5881 GPU 40ep · 5882 predict best_wss · 5883 L3 eval

**配置**：`V3P-J5-ActiveData-MixedProbe-post5463_seed1` · `split_AG_active_v2_j5`（train **+6 AAA** · 63 例 · 5103 graph/ep）· 对照 I6-diag **0.429** · Go ≥ **0.459**

**结果（best_wss）**：
| 指标 | I6-diag | J5 | Δ |
| --- | ---: | ---: | ---: |
| test `wss_r2_wss` | **0.429** | **0.433** | **+0.004** |
| `r2_p` | 0.930 | 0.951 | +0.021 |
| `wss_r2_wss_z` | 0.408 | 0.409 | +0.001 |

**L3 子集（vs I6-diag）**：all **+0.052** · fast **+0.145** · slow **+0.025** · CHEN **−0.241**（I6 **−0.825** · Δ **+0.584** 但仍负）

**判读**：**弱 No-Go** — 全 test 仅 +0.004，未过 Go **0.459**（差 **−0.026**）；L3 fast/CHEN 有结构性改善但 CHEN 仍负、band 未破 **0.45**。+6 AAA 跨域优于 J4（−0.006）但未达 I6-a 级（+0.017）扩 seed 门槛。**J5 线封口**，不扩 seed；Batch-2 可继续 CPU 采购但不自动开 J6 GPU。

**产物**：run `…j5activedata…20260701_002549` · `v3p_l3_subset_eval_j5_20260701.json` · 对比图 `plots/analysis_compare/v3p_j5_vs_i6diag_20260701/`

---

## 2026-07-01｜V3P Phase1 v4 · phase1_pass ✅

**代码**：Phase1 并入 Batch-1 采购 · phys neighbor 覆盖 · `split_AG_active_v2.json`（+6 AAA）

**门禁**：QA **7** · neighbor **5/5** · explained **0.437** → **phase1_pass true**

**下一跳**：可立项 J5 seed1 40ep（未自动提交 GPU）

---

## 2026-07-01｜V3P 路线 A · 257 池采购 Batch-1 ✅

**代码**：
- 新增 `run_v3p_pipeline_procurement_rank.py`（failure_cluster 物理 proxy · batch 清单）
- 修复 `run_v3p_phase1_active_data._scan_v3_pipeline_candidates` 路径 bug + 正确排名
- `submit_v3p_procurement_batch1.sh` · Job **5879** node03 CPU

**发现**：257 池 graphs **已全部就绪** → 未提交 blind pipeline；Batch-1 为 6 例 AAA near-domain 候选深审

**下一跳**：Batch-1 QA 通过后重跑 Phase1 邻居覆盖 → 仍须 phase1_pass 才评 J5

---

## 2026-07-01｜V3P 平台期 G5 叙事收口（路线 B）✅

**文档**：
- [G5_主叙事与平台期结论](../01-任务/任务A/03-V3路线/01-执行与待办/平台期-G5与主动选数/G5_主叙事与平台期结论.md) 新增 **§7**：J4 No-Go · Phase1 AG 扩池封顶 · L3 fast/CHEN/slow · M-N vs M-E 对外段落
- 同步 [V3 README](../01-任务/任务A/03-V3路线/README.md) · [实验跟踪日志](../01-任务/任务A/03-V3路线/01-执行与待办/V3_实验执行跟踪日志.md)

**结论摘要**：AG-only 靠现有 graphs 扩池无法突破 I6-diag **0.429** band；M-N 可收口，M-E 须 257 池 pipeline 或表示/target 级改造

**未做**：GPU 训练 · git commit（用户未要求）

---

## 2026-07-01｜V3P Phase1 v3 主动选数 · AG 扩池封顶 ✅

**结论**：split 外 AG 仅 **1** 例 QA 可入训（`ZHAO_XIU_XUAN` · J4 已用）· **4** 例 denylist · failure_cluster 已在 train → **phase1_pass false** → **不开 J5**

**产物**：`v3p_active_selection_v3_20260701.json` · `平台期-G5与主动选数/_history/主动选数_v3_20260701.md`

**下一跳**：257 池 pipeline 采购 或 G5 收口

---
