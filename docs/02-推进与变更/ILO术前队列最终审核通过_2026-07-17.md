# ILO 术前队列最终审核通过（2026-07-17）

## 最终结论

ILO 活动作用域仅为 `ILO/*/before`。61 例术前原始数据中，最终审核通过 **41 例**，排除 **20 例**；41/41 通过 WSS、节点对齐、peak 时间步、原始 STL landmark v4、右手坐标架及归一化硬门。人工最终签核后软旗标、WSS watch 和活动 ILO centerline 平移修复均为 0。

本次只完成数据审查、合理坐标统一和可视化，**未生成 split，未生成全局 WSS z-score，未进入训练**。

## 最终作用域

| 项目 | 数量 | 状态 |
| --- | ---: | --- |
| ILO 术前原始病例 | 61 | 原始数据保留 |
| ILO-before 最终白名单 | 41 | 41/41 硬门通过，人工审核通过 |
| ILO-before 总排除 | 20 | 不进入活动清单 |
| 人工图审排除 | 8 | 第二轮 6 例 + 最终轮 2 例 |
| 活动 ILO-after bundle/report | 0 | 禁止进入白名单、统计或训练 |
| ILO 术后原始目录 | 61 | 保留，未删除 |

最终人工排除 8 例：

- 第二轮：`DONG_KE_QIN-0`（用户提交名 `DONG_KE_QING`）、`WEI_QING_FENG-1`、`ZHANG_WAN_ZENG-1`、`LIU_CHUN_YANG-1`、`LIU_YUN_ZHANG-0`、`LI_YU_GANG-0`。
- 最终轮：`WANG_LI_MIN-0`、`YANG_QING_REN-1`。

姓名策略仍为 AAA 优先：`GUO_AI_JUN`、`SHEN_CHUN_WANG`、`ZHANG_MAO_JIN` 的 ILO 记录全部排除；近似姓名只留人工 watchlist，不自动合并。

## 数值与坐标验收

- ILO41 peak WSS p90 跨病例范围为 `4.01–35.98 Pa`，peak max 范围为 `26.67–294.65 Pa`；没有负值、非有限值、整例或整时间步全零。
- bundle 中 WSS 始终保存原始 Pa；图件仅用固定 `log1p(WSS)` 0–300 Pa 显示范围，裁剪不改数据。
- 原点为分叉点，`+Z` 指向近端主动脉，`-Z` 指向双髂支，`+X` 继承原始 STL 世界方向，旋转矩阵 `det≈1`。
- 同时保留对齐后毫米坐标和逐病例 `max_abs` 归一化坐标。
- 统一活动 bundle 终检为 AAA65 + ILO41 = **106/106 PASS**；AG76 和 AAA63 只作最终共同坐标可视化参考，既有 AG/AAA 白名单、split 和训练产物没有改动。

## 当前真源与可视化

- 最终报告：`data_wss_min/pipeline_reports/ilo_before_final_approved_20260717/`
- 最终可视化：[`assets_新队列审计/ilo_before_final_approved_20260717/`](assets_新队列审计/ilo_before_final_approved_20260717/)
- 活动白名单：`ilo_before_whitelist.json`
- 排除与人工决定：`ilo_before_exclusions.json`、`ilo_before_manual_review_decisions_final.json`
- 逐例硬门：`ilo_before_wss_geometry_audit.{json,csv}`
- 图件索引：`review_index.csv`；固定毫米/归一化叠加、WSS 对照和 20/20/1 分组页均在最终图包中。

两轮旧报告、旧图包和旧说明已移入对应 `_archive/ilo_before_review_history_20260717/` 目录，不再作为活动入口。
