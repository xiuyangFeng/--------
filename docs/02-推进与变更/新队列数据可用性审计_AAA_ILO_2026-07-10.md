# 新队列数据可用性审计 · AAA / ILO（2026-07-10）

> 状态：**只读审计 + 配准探针 + 可视化 QA 已完成，未生成 bundle、未写入 `data_wss_min/`，未改动 `training_wss_min/` 与 `pipeline_wss_min/` 任何代码。** 最终：190 单元 → **训练候选 162**，排除 28（数据层 19 + 配准层 9）。配准层 9 例本轮标记排除、暂不修（§6.5），其中 6 例原点错位已确诊为可修的 centerline 平移偏移（§6.4-1）。
> 用途：评估 `data_new/AAA` 与 `data_new/ILO` 有多少病例可进入后续训练/测试；给出待排除清单，供人工二次审计。**问题病例在你人工确认前暂不进入训练。**
> 口径（用户确认）：① 达标只审「输入完整性 + 壁面 WSS 物理有效性」，**不要求通过 AG 专用的 flow_divider/wall_branches 配准**；② ILO 的 `before/after` 各算一个独立病例。
> 证据/产物：[`assets_新队列审计/audit_new_cohorts.py`](assets_新队列审计/audit_new_cohorts.py)（只读审计脚本）/ [`audit_new_cohorts.csv`](assets_新队列审计/audit_new_cohorts.csv)（逐单元原始指标）/ [`audit_classified.csv`](assets_新队列审计/audit_classified.csv)（分类）/ [`exclude_manifest_AAA_ILO.json`](assets_新队列审计/exclude_manifest_AAA_ILO.json)（待排除清单）。
> 上位文档：[WSS 最小化训练实验跟踪](WSS最小化_训练实验跟踪.md) / [第四轮执行总结与归档 v2.3](_archive/WSS最小化/WSS最小化_第四轮优化计划_执行总结与归档_2026-07-10.md)。

## 0. 结论速览

- 扫描单元共 **190 个**：AAA 68 例（ruputer 35 + unruputer 33）+ ILO 122 单元（61 例 × before/after）。
- **数据层达标（输入完整 + 壁面 WSS 物理有效）= 171 / 190 单元**（AAA 65、ILO before 51 / after 55）。
- **最终训练候选 = 162 单元**（AAA 63：ruputer 31 + unruputer 32；ILO before 48 + after 51）。
- **本轮排除合计 28 单元**（互不重叠）：
  - 数据层 19（§3，空目录/缺 centerline/百万点混合区/WSS 全零）；
  - 配准层 9（§6.5，原点错位塌缩 6 + 主轴歪斜 3；6 例已确诊可修但本轮不修）。
- 排除清单：`exclude_manifest_AAA_ILO.json`（`exclude_now` + `registration_exclude`）。
- **列格式与 AG 完全一致**（壁面 ascii 与 centerline_points.csv 表头逐字相同），现有 `raw_io` 可直接解析，无需改 reader。

## 1. 判定口径与方法

判定复用 `pipeline_wss_min.raw_io` 按 pipeline 真实读取路径执行（`_read_ascii` / `list_timesteps` / `peak_systole_step` / `read_wall_fields`），未走 vtk/vtp、未做配准、未落盘。硬判据镜像 `qa_gate.LIMITS` 中**不依赖配准**的项：

| 判据 | 阈值 | 来源 |
| --- | --- | --- |
| 壁面 ascii 存在且有时间步文件 | 必须 | 结构完整性 |
| `centerline/centerline_points.csv` 存在且列名匹配 | 必须（几何特征/配准依赖） | 结构完整性 |
| WSS / 坐标全有限 | 无 NaN/Inf | `qa_gate` nonfinite |
| peak 步 WSS `p90 > 0` | `>0` | `qa_gate.peak_p90` |
| peak / 抽样步 零值率 | `≤0.01` | `qa_gate.peak_zero_frac / all_zero_frac` |
| `n_wall ≤ 50000` | **本次不作达标硬判**（见 §2） | `qa_gate.n_wall`（AG 专用上限） |

> 单元命名注意：ILO 时间步文件用**裸姓名前缀**（目录 `AN_GUANG_JIE-0/after`，文件 `AN_GUANG_JIE-1120`），审计从文件名推 `case_name`，reader 的 `*-<step>` glob 兜底可命中。

## 2. 分型统计

| 分组 | 干净(≤50k) | 密网格但干净(>50k) | 百万点混合区 | peak WSS 全零 | 缺 centerline | 空目录 |
| --- | --: | --: | --: | --: | --: | --: |
| AAA/ruputer | 11 | 21 | 0 | 0 | 0 | 3 |
| AAA/unruputer | 11 | 22 | 0 | 0 | 0 | 0 |
| ILO/before | 4 | 47 | 3 | 2 | 4 | 1 |
| ILO/after | 0 | 55 | 3 | 1 | 1 | 1 |
| **合计** | **26** | **145** | **6** | **3** | **5** | **5** |

**达标 = 前两列 = 26 + 145 = 171 单元。** 后四列 = 19 单元，本轮排除。

### 2.1 关键：`n_wall > 50000` 不是数据缺陷

145 个「密网格但干净」单元唯一不满足的是 AG 调出来的 `n_wall ≤ 50000` 下游硬限。新队列网格本就更密（AAA 中位 ~57–64k、ILO ~69–91k），抽验证实这些病例 WSS 完全正常（例：`ILO/AN_GUANG_JIE-0/after`，70636 点、peak p90=17.76、max=175、零值率 0%）。

对第四轮讨论的**稀疏采样对 high-WSS 覆盖不足**问题，网格更密是**利好**：FPS 采样池更大、热点更易被采到。且样本装配阶段本就 FPS 到 2000 点，`n_wall` 只是预处理内存/体积护栏。**建议**：新队列正式入库时把该上限按队列放开（或改为按分位/内存自适应），不改动现有 AG 训练口径。

## 3. 暂不进入训练的 19 单元（待人工审计）

### 3.1 空目录 / 无 ascii 导出（5）
- AAA/ruputer：`FU_GUO_JUN`、`LV_GUO_YOU`、`TANG_JIN_LIANG`
- ILO：`LI_JIE-1/before`、`LI_JIE-1/after`

### 3.2 缺 centerline（5，几何特征/配准必需）
- ILO：`GUO_AI_JUN-0/before`、`LI_FA_XIANG-1/before`、`SUN_CHUN_PU-0/before`、`SUN_CHUN_PU-0/after`、`ZHAO_JIAN_PING-0/before`
- 注：`GUO_AI_JUN-0`、`ZHAO_JIAN_PING-0`、`LI_FA_XIANG-1` 的 **after 相 centerline 齐全**，仅 before 缺。

### 3.3 百万点混合区（6，~94% 点 WSS 恰为 0）
- ILO：`DONG_JIA_JU-1/before`(1.69M)、`LIU_CHUN_YANG-1/after`(2.52M)、`LI_BO_JUN-1/before`(1.89M)、`SUN_DONG_XIN-0/after`(1.62M)、`SUN_XU_XIA-1/after`(1.78M)、`XIONG_DONG_SUO-0/before`(1.41M)
- 特征：点数 1.4–2.5M、94% 点 WSS=0，真正血管壁仅约 6%。疑似把整个表面（含 inlet/outlet 端盖等零 WSS 面）都导出了。**需按壁面 zone 过滤/重导出后才可用**，当前口径下不算干净。

### 3.4 peak WSS 全零（3）
- ILO：`WANG_CAI-0/before` — 全时间步近零（**死导出，建议弃**，扫全步 max WSS = 2.1e-14）
- ILO：`GUO_AI_JUN-0/after`、`XUE_YOU_TANG-0/before` — **可恢复**：仅峰值选步落在被清零的步上，其它步 WSS 正常（全步 max 分别为 27.4 / 19.1）。改峰值选步（跳过 WSS 近零步）即可救回，但**本轮仍列入排除，待你确认**。

## 4. 给后续训练/split 的注意事项

1. **解剖不同，慎与 AG 混池。** AAA 是腹主动脉瘤、ILO 含介入前后，与 AG 的主动脉-髂动脉分叉不是同一形态；直接混池等于假设「几何→WSS」映射跨解剖可迁移，未必成立。建议各建独立队列/split，或作为跨解剖泛化对照。
2. **ILO before/after 同一病人配对，split 必须按病人分组**，否则 before 进 train、after 进 val 会标签泄漏。
3. **可用样本大幅扩容**（AG 53 训练例 → AAA 65 + ILO 106），正好支撑第四轮建议的**病例数 learning curve**：固定最优配方分档喂 13/26/40/53… 例，判断当前「效果差」是数据量受限还是协议/表示受限。
4. **配准适配是独立后续任务**（本轮未做）。若这些队列要进入 AG 现有训练口径，需单独验证 `flow_divider` 三臂聚类与 `wall_branches` 左右轴在瘤体/介入解剖上的成功率；预期需要按队列调参或改用队列专属配准。

## 6. 配准可迁移性探针（2026-07-10 追加）

> 对 §0 的 171 个达标单元试跑 AG 现有解剖配准（`flow_divider` 原点 + `wall_branches` 左右轴），**只读几何、不生成 bundle、不写 `data_wss_min/`**。复现 preprocess 前置链 `read geometry(native) → _resolve_unit_factor → ×factor → compute_transform`，采集 `RigidTransform.to_dict()` 诊断。
> 判据按 **AG 84 份既有 report.json 的实际分布校准**（AG 自身：`origin_kind=flow_divider` 77/84、`main_axis_source=centerline_chord` 73/84、`roll_source=wall_branches` 84/84、`roll_sign_reliable=False` 4/84、`trunk_offset>0.05` 4/84 —— 即 AG 正常配准也带这些告警）。
> 脚本/证据：[`assets_新队列审计/probe_registration.py`](assets_新队列审计/probe_registration.py) / [`probe_registration.csv`](assets_新队列审计/probe_registration.csv) / [`probe_registration_tiered.csv`](assets_新队列审计/probe_registration_tiered.csv)。

### 6.1 结论：AG 配准框架基本可迁移，无需整体分离处理

| 分组 | n | TIER1 干净 | TIER2 可接受（AG 也有的告警区） | FAIL |
| --- | --: | --: | --: | --: |
| AAA/ruputer | 32 | 23 | 7 | 2 |
| AAA/unruputer | 33 | 20 | 13 | 0 |
| ILO/before | 51 | 34 | 13 | 4 |
| ILO/after | 55 | 36 | 13 | 6 |
| **合计** | **171** | **113 (66%)** | **46 (27%)** | **12 (7%)** |

- **TIER1+TIER2 = 159/171 (93%)** 在 AG 自身的配准质量包络内成立。
- `flow_divider` 三臂原点 **165/171 (96%)** 成立，**无一例退到 `wall_fallback`** → 这些队列的 `centerline.vtp` 都带可用的 `DistToBifurcation` 分叉拓扑。
- `wall_branches` 左右轴 165/171 成立；`main_axis` 用 centerline_chord 154、wall PCA 兜底 10、ambiguous 7。
- **TIER1/TIER2 判据含义**：配准框架被成功定义且内部自洽（原点找到、主轴/左右轴/滚转赋值、单位尺度合理），**不等于朝向在解剖上一定正确**。

### 6.2 需人为处理的 12 个 FAIL（分两类，均用 AG 已有机制即可）

- **单位/覆盖不一致 `unit_extent_mismatch`（6）**：AAA `LIU_YU_MING`；ILO `DONG_KE_QIN-0/after`、`LIU_CHUN_YANG-1/before`、`SHEN_CHUN_WANG-0/after`、`WEI_QING_FENG-1/before`、`WEI_QING_FENG-1/after`。中心线可能只描远端一段，`_resolve_unit_factor` 触发整十次幂兜底并标记待复核（AG 本身也有 2 例）。→ 逐例复核单位因子。
- **左右轴退到旧 `branches` 路径（6）**：AAA `ZHOU_KE_XUN`；ILO `WANG_LI_MIN-0/before+after`、`YU_XIANG_SHENG-1/after`、`ZHANG_MAO_JIN-0/before+after`。壁面双髂支左右轴未取到、退回中心线下游 PCA，L-R 符号可靠性下降。→ 用 `REGISTRATION_CASE_OVERRIDES` 逐例设 `roll_sign_mode='world_axis'`（AG 已对 9 个个案这么做）。

### 6.3 必要的下一步：可视化验收（信任前提）

本探针只保证「配准跑通且自洽」，**未验证朝向正确**。AG 当年靠 `visualize_alignment.py` / `visualize_final_case_pages.py` 逐例目视确认左右/上下无系统性翻转。新队列在进入训练前**必须做同样的可视化 QA**，重点：
- AAA 是否真含主动脉分叉（若为纯肾下瘤体、无分叉，`flow_divider` 会「自信地」定错原点，96% 成功率不代表解剖正确）；
- L-R 符号跨队列是否一致（尤其 TIER2 中 roll_sign 不可靠的 15 例、§6.2 的 6 例 branches 兜底）；
- `unit_extent_mismatch` 6 例的尺度是否被兜底修正到位。

## 6.5 排除决定（2026-07-10）：配准层 9 单元标记排除、本轮不修

> 用户决定：§6.4 定位的 9 个配准层不达标单元本轮**标记排除、暂不修复**（即使 6 例原点错位已确诊可修）。与 §3 的数据层 19 单元互不重叠，写入 `exclude_manifest_AAA_ILO.json` 的 `registration_exclude`。

- **原点错位塌缩（6）**：`ILO/WANG_LI_MIN-0/before`、`ILO/WANG_LI_MIN-0/after`、`ILO/ZHANG_MAO_JIN-0/before`、`ILO/ZHANG_MAO_JIN-0/after`、`AAA/ruputer/ZHOU_KE_XUN`、`AAA/unruputer/LIU_WEN_QI`（可修方案见 §6.4-1，本轮不实施）。
- **主轴歪斜严重（3）**：`ILO/WEI_QING_FENG-1/before`、`ILO/XIE_ZHI_FU-0/after`、`ILO/DONG_JIA_JU-1/after`。

**最终口径**：190 单元 = 训练候选 **162** + 数据层排除 19 + 配准层排除 9。训练候选按队列：AAA 63（ruputer 31 + unruputer 32）、ILO before 48、ILO after 51。三类队列内 ILO 仍需按病人分组防前后相泄漏（§4.2）。

## 6.6 共同框架复画与主轴翻转发现（2026-07-10 追加）

> 用户指出旧小图里 `FENG_LI_XIN` 位置偏高、部分病例「反过来了」。原因：旧画法**逐例按 `max|坐标|` 归一化**，带大瘤体/长段的病例在格内竖直位置被顶偏，各格不在同一框架、且掩盖真实翻转。已改为**共同框架**重画（脚本 [`assets_新队列审计/viz_common_frame.py`](assets_新队列审计/viz_common_frame.py)，图覆盖 `alignment_viz/*_small_multiples.png` 与 `*_ortho_overlay.png`）：
> - 配准原点（分叉点）统一置于 (0,0)；
> - 每队列一个**共同尺度** `S=median(各例 98 分位 |坐标|_mm)`，全格同尺、固定坐标范围；
> - 各格可直接比位置/大小/朝向；三视图叠加同尺度。

**结果**：`FENG_LI_XIN` 偏高是归一化假象，共同框架下已归位、朝向与全组一致。AAA 两队列 **0 翻转**（`FENG_LI_XIN`/`LIU_YONG_LAN` 曾被粗检误报，实为瘤体比髂支更宽的干扰，改用薄末端切片法后排除）。

**新发现——ILO 存在主轴符号翻转（髂支叉端落在 −Z、与多数相反）**，疑因 `centerline` 的 `Abscissas` 方向与多数相反、`orient_by_abscissa` 据此定号：
- **明确翻转（目视清晰、叉端在 −Z splay）**：`ILO/YANG_QING_REN-1/after`、`ILO/WEI_QING_FENG-1/after`。
- **检测命中但形态歪斜/压缩、目视不绝对**（多与 EXT extent-mismatch 重合）：`ILO/DONG_KE_QIN-0/before+after`、`ILO/LIU_CHUN_YANG-1/before`、`ILO/SHEN_CHUN_WANG-0/after`、`ILO/YANG_WEN_TAI-0/before+after`。

已登记到 `exclude_manifest_AAA_ILO.json` 的 `orientation_flip_watch`（**本轮仅登记待定，未排除、未修**）。处置选项：① 逐例排除；② 在 `preprocess` 加主轴符号一致性守卫（令髂支叉端恒为 +Z）统一修正。鉴于第一轮已证标量 WSS 任务中绝对坐标贡献很小，翻转对本任务影响有限，可作次优先。

## 6.4 可视化 QA 结果（2026-07-10 追加）

> 对 159 个可配准单元生成对齐后逐病例 X-Z 投影（颜色=z 高度）+ 三视图叠加，逐张目视 + 数值复核。脚本/图：[`assets_新队列审计/viz_new_cohorts.py`](assets_新队列审计/viz_new_cohorts.py) / [`alignment_viz/*.png`](assets_新队列审计/alignment_viz/)；数值异常：[`viz_numeric_flags.py`](assets_新队列审计/viz_numeric_flags.py) / [`viz_numeric_flags.csv`](assets_新队列审计/viz_numeric_flags.csv)。

**总体：朝向高度一致，AG 配准框架目视可用。** 4 张小图（AAA_ruputer/unruputer、ILO_before/after）中约 **150/159** 呈稳定一致的 Y 形——单一主干在下（低 z）、双髂支分叉在上（高 z），跨病例朝向统一；AAA 多数可见中段瘤体膨大（符合腹主动脉瘤）。这直接支撑 §6.1「框架可迁移」的结论。

**目视 + 数值定位到两类真实缺陷（与 §6.2 探针 FAIL 高度重合，且给出了机制）：**

1. **原点错位导致归一化塌缩（6 单元，致命但可修）**：`ILO/WANG_LI_MIN-0/before+after`、`ILO/ZHANG_MAO_JIN-0/before+after`、`AAA/ruputer/ZHOU_KE_XUN`、`AAA/unruputer/LIU_WEN_QI`（实为 3 个病人 + 2 个 AAA）。小图上表现为「底部一个小点/短条」。

   **专项诊断已确诊为「centerline 相对壁面的纯平移偏移」，非坏几何**（脚本 [`assets_新队列审计/diag_origin_offset.py`](assets_新队列审计/diag_origin_offset.py)，图 [`alignment_viz/origin_offset_check.png`](assets_新队列审计/alignment_viz/origin_offset_check.png)）：
   - **壁面与 centerline 的 bbox 对角线逐例完全相等**（如 WANG_LI_MIN/before 均 304.2mm、ZHOU_KE_XUN 均 261.2mm）→ 同尺度、同大小；
   - 质心偏移 **509–1920mm、方向以 Z 为主**（壁面 Z 心在 +385~+1452，centerline Z 心在 −580~+48）——像是壁面/CFD 网格保留了扫描床高位坐标、而 centerline 在 VMTK 局部系导出；
   - **把 centerline 按质心平移回壁面后，其每点到壁面最近距离中位数仅 6–16mm（≈血管半径）**，叠加图上绿色中心线精准穿过管腔含双髂支 → 形状完全吻合，只差一个平移。
   - 对照正常例 `AN_GUANG_JIE/after` 偏移仅 49mm、原点落在壁面内。

   机制：`flow_divider` 原点取自 centerline，被平移带偏 → `wall - origin` 落到千 mm 外 → `max|coord|` 归一化把真实解剖压成一点；`wall_branches` 左右轴也因原点错位失败（故探针报 `roll=branches`）。`outlier_ratio=max|.|/p99` 全部正常，排除离群点。

   **修复方案（低风险，可选实施）**：在 `preprocess` 的 `compute_transform` 前加一道 centerline↔壁面平移预对齐——当 `|centerline质心 − 壁面质心| > k·壁面对角线`（如 k=0.8）时，将 centerline 平移到壁面质心（或做 translation-only ICP）再配准。对 AG 及正常新队列（偏移 ~16–50% 对角线）是 no-op，只救这 6 例。**本轮未改代码**，仅给出方案待批。

2. **主轴歪斜（严重 3 例）**：`ILO/WEI_QING_FENG-1/before`（主轴与 Z 夹角 89°，几乎躺平）、`ILO/XIE_ZHI_FU-0/after`（41°）、`ILO/DONG_JIA_JU-1/after`（36°）。与 §6.2 的 `unit_extent_mismatch` 标记一致，根因为中心线只描远端一段 / 单位覆盖不一致，使主轴估计偏轴。→ 逐例复核单位因子与主轴。

**轻度歪斜（15–25°，9 例）判为正常**：AAA 瘤体/主动脉自然弯曲所致（如 `GAO_WEI_LI` 18.6°、`CAO_DIAN_HE` 18.0°），非配准错误，无需处理。

**未验证项**：X-Z 投影无法判定左右（L-R）翻转，探针标 `FLIP?`（roll_sign 不可靠）的 15 例需 X-Y 视图另行确认；但鉴于第一轮已证「标量 WSS 任务中绝对坐标几乎不贡献」，L-R 翻转对本任务风险较低，可作次优先。

**可视化 QA 净结论**：**约 150/159 单元朝向正确、可进入训练候选**；6 单元因原点错位须修复或剔除，3 单元须复核单位/主轴。缺陷集与探针 FAIL/warn 集一致，无新增系统性问题。

## 7. 后续数据补充方向（老师建议，暂记）

导师提出用**对抗生成网络（GAN）/ 扩散模型**快速生成训练几何以扩充数据集。与本审计的关系与前置条件：
- 真实可用样本（AAA 65 + ILO 106 + AG 61）应先**建立干净、已可视化验收的配准基准**，作为生成模型的训练/条件来源与保真度评估的 ground truth。
- 生成几何若要产 WSS 标签，仍需跑 CFD 或训练一个「几何→WSS」代理；单纯生成表面点云不自带 WSS，需明确标签来源。
- 生成数据的坐标框架必须与真实数据同一配准口径，否则混训会引入朝向不一致。
- 建议顺序：先用真实新队列做 §4.3 的病例数 learning curve，确认「数据量」确实是当前瓶颈后，再投入生成式扩充；若瓶颈在协议/表示，生成更多同构数据收益有限。

## 8. 待办

- [ ] 人工复核 §3 的 19 个排除单元；确认后更新 `exclude_manifest_AAA_ILO.json`（尤其 §3.4 两个可恢复单元、§3.3 六个百万点单元是否重导出）。
- [x] **对 159 个可配准单元做可视化 QA**（§6.3/§6.4 已完成）：约 150/159 朝向正确可入训；定位 6 例原点错位塌缩 + 3 例主轴歪斜。
- [x] **6 例原点错位（§6.4-1）已确诊**：centerline 相对壁面纯平移偏移（509–1920mm，主要 Z），形状完全吻合（平移后最近点中位 6–16mm），非坏几何。
- [ ] 决定 §6.4-1 修复方式：在 preprocess 加 centerline↔壁面平移预对齐守卫（AG no-op，救 6 例）vs 暂剔除这 6 单元。
- [ ] **3 例主轴歪斜（§6.4-2）** + §6.2 单位/左右轴：逐例复核单位因子 / 追加 `REGISTRATION_CASE_OVERRIDES`。
- [ ] `FLIP?` 15 例做 X-Y 视图确认 L-R（次优先，标量任务风险低）。
- [ ] 决定 `n_wall` 上限对新队列的放开方式（放开 / 分位自适应 / 依赖 FPS 子采样）。
- [ ] 决定新队列的训练口径：独立建库 vs 与 AG 混池；确定 split 的病人分组规则（ILO 配对不泄漏）。
- [ ] （若入库）为 AAA/ILO 正式跑 preprocess + QA gate 生成 bundle/report（本轮未做）。
- [ ] 生成式数据（GAN/扩散）作为后续独立立项，前置为真实数据 learning curve 结论（§7）。
