# 任务A周度检查表

> 用途：每周跟踪任务 A 的实验推进、结果结论、阻塞项和下周计划。
> 上位文档：[任务A实验清单](任务A_V1实验清单.md) / [实验设计总纲](../../../实验设计总纲.md) / [实验记录填写规范](../../../00-规范与记录/实验记录填写规范.md)

## 1. 使用说明

这份检查表不替代正式实验记录表，而是作为你每周推进任务 A 的执行面板。

建议使用方式：

1. 每周开始前，先填写“本周目标”。
2. 每做完一个实验，就在对应 `Exp ID` 后打勾，并补一句最核心结论。
3. 每周结束时，必须填写：
  - 本周新完成实验
  - 当前最优主线
  - 与预期相反的结果
  - 当前最大阻塞
  - 下周只做哪 1 到 2 件事
4. 正式数值结果仍然写入 [实验记录表.xlsx](../../../00-规范与记录/实验记录表.xlsx)。

每周使用时建议固定两个原则：

- 周内只追 1 到 2 个核心问题，不要同时开太多实验支线。
- 每周结束必须留下可以复用的产物，而不只是“跑过了”。

建议每周至少归档以下 6 项：

- 本周固定的配置版本
- 本周完成的 `Exp ID`
- 当前最好 checkpoint 路径
- 一张结果汇总表
- 一句最重要结论
- 下一周唯一优先级最高的动作

---

## 2. 总里程碑


| 里程碑 | 目标                                                | 建议截止时间     | 状态   | 备注                         |
| --- | ------------------------------------------------- | ---------- | ---- | -------------------------- |
| M1  | 跑通 4 个现有基线：`A-Base-01` 到 `A-Main-01`              | 2026-03-23 | ✅ 完成 | 3 seed 全部完成于 2026-03-22/23 |
| M2  | 完成单尺度核心消融：`A-Abl-01` 到 `A-Abl-05`                 | 2026-04-06 | 进行中  | **`A-Abl-02` ✅（2026-04-09，三 seed）**；**Line G：`G01`/`G04`/`G05` ✅ 三 seed（2026-04-14）**，`G02`/`G03` ⏹ 已结案（仅 seed1，2026-04-17 决定不补种）；**`A-*-wss-multi` ✅ 训练 + 后处理（2026-04-17）**，壁面 WSS 表 `outputs/field/wss_multitask_test_wall_wss_metrics.tsv`，**R² 主表**见 [任务A实验状态表](../03-共享执行与状态/任务A实验状态表.md)「实验记录摘要 · WSS 多任务」；**`V2P-WSSP-01` ✅（2026-04-24，seed=1）**、**`V2P-WSSP-02` ✅（2026-04-25，seed=1，全场+WSS 头+混合早停，预测与图已闭环）** 见状态表「V2P-WSSP-*」；其余消融子项待启动 |
| M3  | 跑通 `A-Base-04` Hierarchical GNN 最小版               | 2026-04-13 | 未开始  |                            |
| M4  | 完成 hierarchy 关键消融：`A-Abl-06` 到 `A-Abl-09` 的核心两组以上 | 2026-04-20 | 未开始  |                            |
| M5  | 整理任务 A 主结果表、消融表、Pareto 图                          | 2026-04-27 | 进行中  | 第一版效率柱状图与 Pareto 图已生成      |


---

## 3. 周度模板

以下模板每周复制一份继续往下写。

---

## Week 0｜2026-03-14 ~ 2026-03-16（正式开跑前冻结）

### 本周目标

- 固定唯一 `data_version / split_version / preprocess_version`
- 明确当前阶段采用 `single_split`，暂不直接进入 `k-fold`
- 跑通 1 组 baseline 的 smoke test 闭环

### 本周开跑前核对

- 已落盘正式 split 文件，并保存训练/验证/测试病例名单
- 已确认归一化统计量仅使用训练集
- 已完成图数据生成，并核对 `x / global_cond / y / case_name`
- 已确定正式结果目录与命名规范（`outputs/field/{exp_name}_split_AG_v1_seed{seed}_{timestamp}/`）
- 已生成至少 1 份 smoke test 配置

### 本周建议输出物

- 一份任务 A 冻结卡（见 [任务A冻结卡.md](任务A_V1冻结卡.md)）
- 一份正式 split 清单（`training/splits/split_AG_v1.json`）
- 一份数据处理命令清单
- 一份 smoke test 结果

### 本周实验勾选


| Exp ID      | 实验名                       | split 已固定 | 数据已处理 | Smoke Test | 一句话结论              |
| ----------- | ------------------------- | --------- | ----- | ---------- | ------------------ |
| `A-Base-01` | Point-wise MLP            | [x]       | [x]   | [x]        | 数据管线闭环正常，MLP 能稳定收敛 |
| `A-Main-01` | Transformer with geometry | [x]       | [x]   | [x]        | 几何特征注入路径验证正确       |


### 本周必填总结

- 本周固定版本：`data_version=AG_v1`，`split_version=split_AG_v1`，`preprocess_version=pipeline当前版本（混合FPS20%，kNN图）`
- 本周新完成实验：smoke test 验证 A-Base-01 和 A-Main-01 训练-评估-落盘闭环
- 当前最优主线：A-Main-01（Transformer + geometry）
- 当前最重要的结论：数据维度对齐正确，`data.x / data.global_cond / data.y` 格式已验证
- 与预期相反的结果：无
- 当前最大阻塞：slow 组集群作业 1177 仍有 19 个病例处理中
- 下周只做哪 1 到 2 件事：1. 启动 A-Base-01 ~ A-Main-01 正式训练（seed=1）；2. 补 seed=2,3

### 是否达到本周门槛

- 正式 split 已冻结
- 数据处理闭环已完成
- 至少 1 个 baseline 已跑通 smoke test
- 已具备进入 Week 1 的条件

---

## Week 1｜2026-03-17 ~ 2026-03-23

### 本周目标

- 跑通 `A-Base-01` 到 `A-Main-01` 的 `seed=1`
- 锁定正式 baseline 结果口径
- 先拿到每个 baseline 的首个可读结果

### 本周开跑前核对

- 已锁定唯一数据目录，且不混用旧版中间文件
- 已固定患者级 `train / val / test`
- 已核对 `data.x / data.global_cond / data.y` 的维度和索引
- 已确认 `global_cond` 广播方式无误
- 已确认评估脚本可独立读取 checkpoint
- 已跑通至少 1 组 baseline 的 smoke test

### 本周建议输出物

- 4 个 baseline 的配置文件（已通过 `make_field_plan.py` 生成）
- 第一版主结果表（见 `experiment_index.csv` + `任务A实验状态表.md`）
- 壁面/内部点分层结果表（`predictions_test/regional_eval/fig_A5_regional_metrics.json`）
- 散点图、per-case 箱线图、误差分布图、CDF 与区域误差图

### 本周实验勾选


| Exp ID      | 实验名                          | Smoke Test | `seed=1` 完成 | `seed=2,3` 完成 | 结果已写入实验表 | 一句话结论                                                |
| ----------- | ---------------------------- | ---------- | ----------- | ------------- | -------- | ---------------------------------------------------- |
| `A-Base-01` | Point-wise MLP               | [x]        | [x]         | [x]           | [x]      | MLP 仅压力预测有效（R2_p=0.920），速度场 R2<0.11，确立任务 A 无图下限      |
| `A-Base-02` | GraphSAGE                    | [x]        | [x]         | [x]           | [x]      | 图结构使 RMSE_|v| 从 1.600→1.361（-14.9%），w 分量收益最大         |
| `A-Base-03` | Transformer without geometry | [x]        | [x]         | [x]           | [x]      | 与 GraphSAGE 性能几乎持平，说明瓶颈不在架构而在特征                      |
| `A-Main-01` | Transformer with geometry    | [x]        | [x]         | [x]           | [x]      | 几何特征使 RMSE_|v| 降至 1.161，w 分量 R2 从 0.325→0.545，几何贡献显著 |


> **注**：本周超额完成目标，seed=1/2/3 全部在 2026-03-22/23 期间完成。

### 本周必填总结

- **本周固定版本**：`split_AG_v1`，`exp_id=A-Base-01/02/03 + A-Main-01`，物理约束全部关闭
- **本周新完成实验**：A-Base-01、A-Base-02、A-Base-03、A-Main-01（3 seed 均完成）
- **当前最优主线**：A-Main-01（Transformer + 全几何特征，RMSE_|v|=1.161±0.038）
- **当前最重要的结论**：显式几何特征（Abscissa/NormRadius/Curvature/Tangent）是速度场精度提升的关键来源，尤其对 w（轴向）分量贡献最大（R2: 0.325→0.545）；架构差异（Transformer vs GraphSAGE）在无几何特征时几乎无影响
- **与预期相反的结果**：MLP 压力预测（R2_p=0.920）不弱于 GraphSAGE（0.901），说明压力场与坐标关系更直接，图结构对压力帮助有限
- **当前最大阻塞**：~~区域标签横比口径~~（**2026-03-24 已解除**：评估阶段从 `graph_path` 读完整几何，四模型区域 mask 一致，各 run `regional_eval` 与 `plots/multimodel_baseline/fig_A5_multimodel_*` 已重算）
- **下周只做哪 1 到 2 件事**：1. 启动 **A-Abl-01**（输入特征层级消融）；2. 按需在论文/汇报中固化「复杂区域」跨模型对比表述（数据已就绪）

### 是否达到本周门槛

- 4 个 baseline 都有可读结果（3 seed mean ± std）
- 已有第一版主结果表（见实验状态表）
- 已有至少 1 张分层评估结果表
- 已确认下周进入 A-Abl-01（输入特征消融）

### Week 1 后补更新（2026-03-23 晚）

- baseline 与主模型的 `predict_field.py` 后处理产物已补齐，当前 `outputs/field/*/predictions_test/` 下已生成散点图、per-case 箱线图、误差分布图、CDF 和区域误差图
- 当前最优主线仍是 `A-Main-01`，而且优势已经从训练汇总指标扩展到节点级误差分析和分区域评估：
  - 全局 `RMSE_|v| = 1.1937 ± 0.0284`
  - 壁面 `RMSE_|v| = 0.0381 ± 0.0024`
  - 内部点 `RMSE_|v| = 2.0668 ± 0.0492`
  - 高曲率 `RMSE_|v| = 1.1219 ± 0.0209`
  - 分叉区 `RMSE_|v| = 1.1341 ± 0.0389`
- 当前新的主要判断是：几何特征不只改善总体均值，也显著压低壁面区误差，并在高曲率区和分叉区保持优势；真正的主误差已经集中在 `interior / core_flow / bifurcation`
- **（2026-03-24 更新）** 复杂区域横比的阻塞已解除：代码侧用 `graph_path` 统一区域 mask 后，四模型 `fig_A5` 与多模型汇总图已按同一几何口径重算，无需再为「标签」单独重导出预测 `.pt`（除非图文件路径缺失等异常情况）。
- **（2026-03-26）** 各区域默认名称与数值区间已写入[任务A分区域评估口径](../../../00-规范与记录/任务A分区域评估口径.md)，便于写论文与对表时引用。

---

## Week 2｜2026-03-24 ~ 2026-03-30

### 本周目标

- 补齐 baseline 的多 seed 结果
- 完成 `A-Abl-01`
- 回答“图是否优于点”“geometry 是否有用”

### 本周建议输出物

- baseline 的多 seed 汇总表
- `A-Abl-01` 的单独结果表
- 关于 `BC / is_wall / geometry` 的一句话结论草稿

### 本周实验勾选


| Exp ID            | 实验名                                                       | 本周是否完成 | 已补多 seed | 结果已写入实验表 | 一句话结论                                                                                                                                                           |
| ----------------- | --------------------------------------------------------- | ------ | -------- | -------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `A-Base-01`       | baseline 补 seed                                           | `[]`   | `[]`     | `[]`     |                                                                                                                                                                 |
| `A-Base-02`       | baseline 补 seed                                           | `[]`   | `[]`     | `[]`     |                                                                                                                                                                 |
| `A-Base-03`       | baseline 补 seed                                           | `[]`   | `[]`     | `[]`     |                                                                                                                                                                 |
| `A-Main-01`       | baseline 补 seed                                           | `[]`   | `[]`     | `[]`     |                                                                                                                                                                 |
| `A-Abl-01-01`     | `coords + t`                                              | `[]`   | `[]`     | `[]`     |                                                                                                                                                                 |
| `A-Abl-01-02`     | `coords + t + BC`                                         | `[]`   | `[]`     | `[]`     |                                                                                                                                                                 |
| `A-Abl-01-03`     | `coords + t + BC + is_wall`                               | `[]`   | `[]`     | `[]`     |                                                                                                                                                                 |
| `A-Abl-01-04`     | `coords + t + BC + geometry`                              | `[]`   | `[]`     | `[]`     |                                                                                                                                                                 |
| `A-Abl-01-05`     | `coords + t + BC + geometry + is_wall`                    | `[]`   | `[]`     | `[]`     |                                                                                                                                                                 |
| `A-Opt-01`        | P0-1：`target_weights` 速度加权                                | [x]    | [x]      | [x]      | 内部/高曲率/分叉/主干 `RMSE_                                                                                                                                             |
| `A-Opt-02`        | P0-2：Pre-Norm `FieldTransformer`                          | [x]    | [x]      | [x]      | 相对 Main 内部/多区域 `RMSE_                                                                                                                                           |
| `A-Opt-02_warmup` | P0-3：`A-Opt-02` + `optim.warmup_epochs=5`                 | [x]    | [x]      | [x]      | 三 seed + 后处理已闭环（2026-03-27）；`plots/optimization/prenorm_Main_P02_P02w`；**救 seed3 / `best_epoch` 分布改善**，`**rmse_p` 均值相对无 warmup 变差**                             |
| `A-Opt-03`        | P0-4：`A-Opt-01` + `A-Opt-02`（`target_weights` + Pre-Norm） | [x]    | [x]      | [x]      | 三 seed + 后处理已闭环（2026-03-28）；**h128 P0 线**；`**summary.rmse_p` trade-off**；**（2026-03-31）轻量对照**；汇总 `plots/optimization/prenorm_A_Opt03_vs_Opt03w/best_metrics.csv` |
| `A-Opt-03w`       | P0-4：`A-Opt-03` + `warmup_epochs=5`                       | [x]    | [x]      | [x]      | 三 seed（2026-03-28）；**未优于 `A-Opt-03`**；**不作为母版**                                                                                                                 |
| `A-Opt-04`        | P0-5：`A-Opt-03` + `hidden_dim=256`                        | [x]    | [x]      | [x]      | 三 seed + 后处理（2026-03-29）；**`A-Opt-05` 中间态**；非母版                                                                                                                            |
| `A-Opt-05`        | P0-5：`A-Opt-04` + `num_layers=4`                          | [x]    | [x]      | [x]      | 三 seed + 后处理（2026-03-29）；**均值略优 03**，**`near_wall` 等略优**；**（2026-03-31）消融/Line G/W 母版**；方差与成本高于 03                                                                                                     |
| `A-Opt-05_tune`   | P0-5 后：05 上 warmup/lr/wd/sched 等小步试跑                         | 部分   | 部分     | 部分     | **（2026-03-31）** 见 **`experiment_index.csv`** 与 [任务A实验状态表](../03-共享执行与状态/任务A实验状态表.md)；已入账 run 后处理齐；**Fig** `plots/optimization/A_Opt05_tune_vs_Opt03_seed1`；**WSS 全量对比暂缓**；`manifest_list` 中无目录项待核对 |
| `A-Opt-07`        | P1-2：`A-Opt-05` + `interior_loss_boost=3`                         | [x]    | [x]      | [x]      | **（2026-04-02）** 三 seed + **`predictions_test`** + **`regional_eval`**；**相对 05 负结果**；对照图 `plots/optimization/A_Opt07_vs_Opt05_Main01`；见状态表「A-Opt-07」 |


### 本周必填总结

- 本周固定版本：
- 本周新完成实验：`**A-Opt-01`**（三 seed + `predict_field` + `plot_taskA_regional_bar` + 多模型 Fig A5 更新，2026-03-26）；`**A-Opt-02**`（三 seed + 导出 + `error_analysis_interior` + `regional_eval` + 与 Main 多模型散点，2026-03-27）；`**A-Opt-02_warmup**`（P0-3，同上后处理链 + `prenorm_Main_P02_P02w`，2026-03-27）；`**A-Opt-03` / `A-Opt-03w**`（P0-4，2026-03-28，见状态表与 `prenorm_A_Opt03_vs_Opt03w`）；`**A-Opt-04` / `A-Opt-05**`（P0-5 容量，2026-03-29，见状态表「实验记录摘要」）
- 当前最优主线：**（2026-03-31）** 以 **`A-Opt-05`**（`h256×4L`，P0-4 组合）为 **后续实验母版**：**`interior` 与 `near_wall` 等略优于 `A-Opt-03`**，更利于端到端梯度质量叙事；**trade-off** 为更高显存/时延与更大跨 seed 方差。**`A-Opt-03`**（h128）保留 **轻量对照** 与 P0-4 历史锚点。**`summary.rmse_p`** 相对单独 P0-1/P0-2 仍偏弱，须在文中写清 **trade-off**。**下一优先（截至 2026-03-31）**：**不堆 6L**，推进 **`A-Abl-02` / Line G**。**（2026-04-09 更新）**：**`A-Abl-02` ✅**。**（2026-04-14 更新）**：**Line G 之 `G01`/`G04`/`G05` ✅ 三 seed**；**`G02`/`G03` ⏹ 已结案（仅 seed1，2026-04-17 决定不补种）** → 优先 **`A-Abl-01`** 或 **Line W**。**（2026-04-17 更新）**：**`A-*-wss-multi`** 训练 ✅（见 [任务A实验状态表](../03-共享执行与状态/任务A实验状态表.md)「第五批附」、`outputs/field/wss_multitask_test_wall_wss_metrics.tsv`）；预测与图件按 [代码修改与实验推进记录](../../../02-推进与变更/代码修改与实验推进记录.md) 2026-04-17。
- 当前最重要的结论：
- 与预期相反的结果：
- 当前最大阻塞：
- 下周只做哪 1 到 2 件事：

### 是否达到本周门槛

- baseline 已补齐核心 seed
- `A-Abl-01` 已形成可写进论文的结论
- 已确认 geometry 和 BC 是否保留为主线输入

### Week 2 后补（2026-03-26）

- 在 Week 2 模板表中补登记 `**A-Opt-01`**（P0-1）完成项与前一周「本周必填总结」摘要；完整数值与区域对照见 [任务A实验状态表](../03-共享执行与状态/任务A实验状态表.md)。
- `**（2026-03-27）**` 同表补登记 `**A-Opt-02**`（P0-2）；状态表「第三批」现为 `**A-Opt-01` ✅**、`**A-Opt-02` ✅**。
- `**（2026-03-27）`** `**A-Opt-02_warmup`（P0-3）✅**。
- `**（2026-03-28）`** `**A-Opt-03` / `A-Opt-03w`（P0-4）✅**；`**（2026-03-29）`** `**A-Opt-04` / `A-Opt-05`（P0-5 容量）✅**；**（2026-03-31）** 战略母版已切换为 `**A-Opt-05**`，见 [任务A实验状态表](../03-共享执行与状态/任务A实验状态表.md)「战略锚点」。
- `**A-Abl-01` 仍为未完成**：本周门槛勾选应以实际周进度为准。

---

## Week 3｜2026-03-31 ~ 2026-04-06

### 本周目标

- 完成 `A-Abl-02`
- 完成 `A-Abl-03`
- 补齐壁面/内部点、高曲率区域两个分层指标

### 本周建议输出物

- 几何分量消融表
- 坐标归一化消融表
- 高曲率与壁面区域的分层结果图

### 本周实验勾选


| Exp ID        | 实验名               | 本周是否完成 | 结果已写入实验表 | 一句话结论 |
| ------------- | ----------------- | ------ | -------- | ----- |
| `A-Abl-02-00` | Full geometry     | `[]`   | `[]`     |       |
| `A-Abl-02-01` | 去掉 `Abscissa`     | `[]`   | `[]`     |       |
| `A-Abl-02-02` | 去掉 `NormRadius`   | `[]`   | `[]`     |       |
| `A-Abl-02-03` | 去掉 `Curvature`    | `[]`   | `[]`     |       |
| `A-Abl-02-04` | 去掉 `Tangent`      | `[]`   | `[]`     |       |
| `A-Abl-03-01` | 原始坐标              | `[]`   | `[]`     |       |
| `A-Abl-03-02` | 仅中心化              | `[]`   | `[]`     |       |
| `A-Abl-03-03` | 中心化 + PCA 对齐      | `[]`   | `[]`     |       |
| `A-Abl-03-04` | 中心化 + PCA 对齐 + 缩放 | `[]`   | `[]`     |       |


### 本周必填总结

- 本周固定版本：
- 本周新完成实验：**（2026-03-31）** **`A-Opt-05_tune`** 已入账 run：**测试集 predict + 任务 A 图**；**`plots/optimization/A_Opt05_tune_vs_Opt03_seed1/`**；**WSS** `compare_hemo_wss_runs` **全量未跑**。**（2026-04-02）** **`A-Opt-07`**（三 seed + 预测/分区 + **`A_Opt07_vs_Opt05_Main01`** 对照图脚本；结论：**相对 `A-Opt-05` 无收益**）。
- 当前最优主线：**`A-Opt-05` 为消融/Line G/W 母版**；**`A-Opt-03`** 轻量对照——见 [任务A实验状态表](../03-共享执行与状态/任务A实验状态表.md)「战略锚点」。
- 当前最重要的结论：
- 与预期相反的结果：
- 当前最大阻塞：
- 下周只做哪 1 到 2 件事：

### 是否达到本周门槛

- 已确认哪些几何分量最关键
- 已确认坐标归一化固定方案
- 已补至少两个分层评估结果

### Week 3 后补（2026-04-09）

- **`A-Abl-02`（`A-Abl-02-01`～`04`）**：**split_AG_v1**、**seed 1/2/3** 已全部归档；**`interior.rmse_vel_mag`** 汇总见 **`outputs/field/plots/ablation/geometry_opt05_mean3seed/`** 与 [任务A实验状态表](../03-共享执行与状态/任务A实验状态表.md)「实验记录摘要 · A-Abl-02」。**NormRadius** 去掉后退化最大且 **p≈0.0015**；**Tangent** 次之；**Abscissa / Curvature** 较弱（*n*=3 下未达常规显著）。
- **`A-Abl-03`**：仍按状态表为 **未开始**（本周原目标未全部达成属正常，以状态表为准）。

---

## Week 4｜2026-04-07 ~ 2026-04-13

### 本周目标

- 完成 `A-Abl-04`
- 完成 `A-Abl-05`
- 对单尺度主线做阶段性结论

### 本周建议输出物

- 增强消融表
- physics 消融表
- 单尺度主线阶段总结草稿

### 本周实验勾选


| Exp ID        | 实验名                                  | 本周是否完成 | 结果已写入实验表 | 一句话结论 |
| ------------- | ------------------------------------ | ------ | -------- | ----- |
| `A-Abl-04-01` | 无增强                                  | `[]`   | `[]`     |       |
| `A-Abl-04-02` | 旋转                                   | `[]`   | `[]`     |       |
| `A-Abl-04-03` | 旋转 + 平移                              | `[]`   | `[]`     |       |
| `A-Abl-04-04` | 旋转 + 平移 + 小尺度扰动                      | `[]`   | `[]`     |       |
| `A-Abl-05-01` | 数据损失 only                            | `[]`   | `[]`     |       |
| `A-Abl-05-02` | 数据 + continuity                      | `[]`   | `[]`     |       |
| `A-Abl-05-03` | 数据 + continuity + no-slip            | `[]`   | `[]`     |       |
| `A-Abl-05-04` | 数据 + continuity + no-slip + momentum | `[]`   | `[]`     |       |


### 本周必填总结

- 本周固定版本：
- 本周新完成实验：
- 当前最优主线：
- 当前最重要的结论：
- 与预期相反的结果：
- 当前最大阻塞：
- 下周只做哪 1 到 2 件事：

### 是否达到本周门槛

- 已确认增强是否保留
- 已确认 physics 是否作为主线保留
- 已形成单尺度主线阶段总结

---

## Week 5｜2026-04-14 ~ 2026-04-20

### 本周目标

- 跑通 `A-Base-04`
- 只做 hierarchy 最小可运行版本
- 与 `A-Main-01` 做首轮精度-速度-显存对比（**说明**：**`A-Opt-07`** 已于 2026-04-02 闭环，V1 单尺度近期增量见 [任务A实验状态表](../03-共享执行与状态/任务A实验状态表.md)；本节模板仍保留作历史周次结构）

### 本周建议输出物

- `A-Base-04` 最小版配置
- 与 `A-Main-01` 的首轮对比表
- 第一版 Pareto 草图

### 本周实验勾选


| Exp ID      | 实验名                  | 本周是否完成 | 结果已写入实验表 | 一句话结论 |
| ----------- | -------------------- | ------ | -------- | ----- |
| `A-Base-04` | Hierarchical GNN 最小版 | `[]`   | `[]`     |       |


### 本周必填总结

- 本周固定版本：
- 本周新完成实验：
- 当前最优主线：
- 当前最重要的结论：
- 与预期相反的结果：
- 当前最大阻塞：
- 下周只做哪 1 到 2 件事：

### 是否达到本周门槛

- hierarchy 已有可运行结果
- 已拿到与 `A-Main-01` 的首轮对比
- 已明确下一周先做哪组 hierarchy 消融

---

## Week 6｜2026-04-21 ~ 2026-04-27

### 本周目标

- 完成 hierarchy 关键消融中的至少 2 组
- 建议优先 `A-Abl-06` 和 `A-Abl-07`
- 形成 Pareto 视角的判断

### 本周建议输出物

- 至少两组 hierarchy 消融结果
- 精度-速度-显存对比图
- 是否进入任务 B 的阶段判断

### 本周实验勾选


| Exp ID     | 实验名     | 本周是否完成 | 结果已写入实验表 | 一句话结论 |
| ---------- | ------- | ------ | -------- | ----- |
| `A-Abl-06` | 层次深度消融  | `[]`   | `[]`     |       |
| `A-Abl-07` | 下采样策略消融 | `[]`   | `[]`     |       |
| `A-Abl-08` | 图结构消融   | `[]`   | `[]`     |       |
| `A-Abl-09` | 解码策略消融  | `[]`   | `[]`     |       |


### 本周必填总结

- 本周固定版本：
- 本周新完成实验：
- 当前最优主线：
- 当前最重要的结论：
- 与预期相反的结果：
- 当前最大阻塞：
- 下周只做哪 1 到 2 件事：

### 是否达到本周门槛

- hierarchy 至少完成两组关键消融
- 已能画出精度-速度-显存对比
- 已判断是否满足进入任务 B 的条件

---

## 4. 通用周报模板

如果后续超过 6 周，按下面模板继续追加。

---

## Week X｜YYYY-MM-DD ~ YYYY-MM-DD

### 本周目标

-

-

### 本周建议输出物

-

-

-

### 本周实验勾选


| Exp ID | 实验名 | 本周是否完成 | 结果已写入实验表 | 一句话结论 |
| ------ | --- | ------ | -------- | ----- |
|        |     | `[]`   | `[]`     |       |


### 本周必填总结

- 本周固定版本：
- 本周新完成实验：
- 当前最优主线：
- 当前最重要的结论：
- 与预期相反的结果：
- 当前最大阻塞：
- 下周只做哪 1 到 2 件事：

### 是否达到本周门槛

- 已完成本周最重要实验
- 已输出至少一张可复用结果表
- 已明确下周唯一优先级最高的动作

## Week X｜起止日期

### 本周目标

-

### 本周实验勾选


| Exp ID | 实验名 | 本周是否完成 | 结果已写入实验表 | 一句话结论 |
| ------ | --- | ------ | -------- | ----- |
|        |     | `[]`   | `[]`     |       |


### 本周必填总结

- 本周新完成实验：
- 当前最优主线：
- 当前最重要的结论：
- 与预期相反的结果：
- 当前最大阻塞：
- 下周只做哪 1 到 2 件事：

### 是否达到本周门槛

-

---

## 5. 每周固定自检问题

每周结束时必须回答下面 6 个问题：

1. 本周完成了哪些 `Exp ID`？
2. 当前最优主线是哪个模型或配置？
3. 哪个结论已经足够写进论文？
4. 哪个结果和预期相反？
5. 当前最大阻塞是数据、训练稳定性、实现复杂度还是算力？
6. 下周只做哪 1 到 2 件最高优先级的事？

---

## 6. 当前建议的优先级提醒

在任务 A 当前阶段，优先级固定为：

1. 先站稳单尺度主线
2. 再引入 hierarchy
3. 先做能回答问题的实验
4. 再考虑更先进 backbone

如果某周出现以下情况，下一周必须优先复盘而不是继续加实验：

- 多个 seed 方向不一致
- `A-Main-01` 不能稳定优于 `A-Base-03`
- physics loss 明显拖垮训练稳定性
- hierarchy 提速但精度明显崩坏

