# BC/RCR V4 汇报材料汇总

> 建立日期：2026-09-02，口径更新：2026-09-04。本目录汇集
> `volume_uvwp_bc_rcr_v4`（seed1234 完整 16 臂 + 38/48 已完成的部分跨 seed 评估）
> 汇报所需的全部图件与数据表，便于整理 PPT。分析正文见
> [`../WSS_PINN_V4实验分析与汇报提纲_2026-09-02.md`](../WSS_PINN_V4实验分析与汇报提纲_2026-09-02.md)。
> 定性：pre-Centerline-V2 历史矩阵；完整矩阵比较只限 seed1234，38/48 不是完整三种子
> 复核。该矩阵使用约 250k 参数的容量配平简化 PN/PNPP，不是 P2V/D2。

> **正式新路线决策**：2026-09-04 用户选择 B，Centerline-V2 formal V4 将恢复
> PointNet P2V / 纯 PointNet++ D2 `c125-k128` 架构锚点并从随机初始化训练。代码、配置
> 和 preflight 尚未更新，本目录所有既有曲线与指标仍只代表旧容量配平结构。

| 子目录 | 来源（原件保留） | 内容 | 汇报用途 |
| --- | --- | --- | --- |
| `V4_seed1234_0-15_训练曲线与诊断图_2026-08-31/` | 复制自 `docs/02-推进与变更/WSS_PINN/` 同名目录 | fig1–8：训练曲线、分通道 loss、λ/残差动态、过拟合散点、主指标条形、方差比、压力离群、WSS 与近壁耦合 + 合订 PDF | 结果全景、机制（梯度/λ/压力）、WSS 归因 |
| `V4_BC-PDE_FIX_EMA_横向R2散点图_2026-09-01/` | 复制自 `docs/03-汇报材料/` 同名目录 | 8 个 PDE 臂的逐病例 speed R² 横向排序、代表病例回归三联图、full-wall WSS 回归与分布、逐病例 CSV | 逐病例与代表病例展示、WSS full-wall |
| `V4_milestone补评_epoch曲线_2026-09-02/` | 2026-09-02 生成 | fig9：speed R² 随 epoch；fig10：准稳态近壁速度 R² + 全体域压力 R²；CSV 仍含 E | 证明退步主因是协议（过拟合），物理项是正则而非提升 |
| `V4_汇报图件重绘_2026-09-03/` | 2026-09-03 生成（答疑轮） | fig4 重绘（epoch 1000→10000 轨迹，分准稳态/瞬态）、fig7 重绘（逐病例压力 R² 排序 + σ/RMSE 机制）、fig11 新增（no-slip 与数据梯度 cos ≈ −0.9 四联图）、fig12 新增（E 对 speed R²）；梯度诊断 CSV、数字汇总、两段脚本 | 替换旧图 4 / 图 7；回答老师问的"cos ≈ −0.9 什么意思""E 与 R² 为什么方向相反" |
| `V4_近壁采样核查_2026-09-04/` | 2026-09-04 生成 | 真实 mm 管腔半径、173 例 1.5 mm 覆盖率、旧 40k cap 与当前 staging 对照；同一 `RAN_QING_BO` 的 PointNet 5000 support 和 PointNet++ FPS-128/32-NN；PNG / CSV / JSON / ParaView VTP / 可复现脚本 | 回答“1.5 mm 是否全选”和“当前近壁设计是否合理” |

本地网页版（直接用浏览器打开，图件相对引用本目录）：`../WSS_PINN_V4实验分析与汇报提纲_2026-09-02.html`。
单文件版（图片以 base64 内嵌，拷到任何电脑都能打开）：`../WSS_PINN_V4实验分析与汇报提纲_2026-09-02_单文件版.html`。
**单文件版是生成物，不要手改**：它后半段是图片的 base64 编码，看起来像乱码是正常的。改中文只改本地版 `.html`（或 `.md`），
然后运行 `python3 docs/03-汇报材料/tools/build_v4_report_singlefile.py` 重建单文件版（2026-09-03 起）。

汇报建议顺序（2026-09-03 更新）：一页结论 → 2.4 三代同口径 R² 表（跨代只看 R²，E 只做 V4 内部排序）→ fig5/fig6/fig12 →
fig4（重绘轨迹）+ fig9 协议过拟合 → 数据合同表（§3.9）→ fig11 梯度冲突 → fig3a + Q5 λ 撞限与护栏 bug → fig7（重绘）压力口径 →
fig8 + Q11 近壁定义 → Gate A–D（Gate E 后置）→ 表述边界。老师可能追问的 11 个问题及答案见分析文档 §6。

工作簿：`../WSS_PINN_V1_V2_V3_field-v4实验矩阵与指标汇总_2026-08-06.xlsx`（主表行 19–34、V4 Checkpoint敏感性、V4 Full-wall WSS）。文件名中的 `field-v4` 是历史命名；行 19–34 实际是 BC/RCR V4，二者不是同一路线。
