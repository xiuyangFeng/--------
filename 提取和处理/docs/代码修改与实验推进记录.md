# 代码修改与实验推进记录

> 用途：持续记录每次代码修改补充了什么，以及当前推进到实验总纲的哪一步。  
> 上位文档：[实验设计总纲](实验设计总纲.md) / [任务A实验清单](任务A实验清单.md) / [任务B指标计算规范](任务B指标计算规范.md)

## 2026-03-11 第 1 次记录

### 本次主要修改

- 为 `pipeline.run_all` 增加 `--geometry-python` 参数，允许步骤2 `extract_features` 在独立的 `GNN_vmtk` 环境中执行
- 将 `pipeline.run_all` 改为按步骤懒加载，避免在 `GNN` 环境里仅运行步骤3-5时仍因导入 `vmtk/vtk` 失败
- 同步改造集群脚本：
  - `pipeline/cluster/run_pipeline.slurm`
  - `pipeline/cluster/run_array.slurm`
  - `pipeline/cluster/run_single_step.slurm`
  - `pipeline/cluster/batch_submit.sh`
- 同步更新主 README、`pipeline/README.md`、集群 README 与迁移说明，明确双环境运行方式

### 对应代码

- `pipeline/run_all.py`
- `pipeline/cluster/run_pipeline.slurm`
- `pipeline/cluster/run_array.slurm`
- `pipeline/cluster/run_single_step.slurm`
- `pipeline/cluster/batch_submit.sh`

### 对应文档

- `README.md`
- `pipeline/README.md`
- `pipeline/cluster/README.md`
- `docs/paper_idea/MIGRATION.md`

### 推进到实验步骤

对应 [实验设计总纲](实验设计总纲.md) 第 13 节：

- Step 1 前的数据准备链路已从“单环境假设”修正为“几何预处理环境 + 训练环境”双环境可复现方案
- 服务器侧批量预处理链路已恢复可执行，不需要再把 `vmtk` 强行并入训练环境

### 当前状态判断

- `GNN` 环境负责 `preprocess -> coord_normalize -> normalize -> convert_to_graph`
- `GNN_vmtk` 环境负责 `extract_features`
- 目前代码层面已经支持“一条命令批量跑完整流程”，但会在步骤2自动切换到几何环境解释器
- 后续正式实验可以先按这个双环境方案跑数据，再进入训练与任务 A 实验

---

## 2026-03-10 第 4 次记录

### 本次主要修改

- 重写任务 A 实验清单，补齐 4 万点快照场景下的效率优化主线
- 在任务 A 中新增层次化建模相关基线与消融：
  - `A-Base-04`
  - `A-Abl-06` 到 `A-Abl-09`
- 补充任务 A 的效率指标、资源记录、Pareto 对比和进入任务 B 的新门槛
- 同步更新实验总纲中的主贡献、研究假设和任务 A 章节

### 对应文档

- `docs/任务A实验清单.md`
- `docs/实验设计总纲.md`

### 推进到实验步骤

对应 [实验设计总纲](实验设计总纲.md) 第 13 节：

- Step 3 到 Step 4 的实验设计口径已更新为“单尺度主线先站稳，再进入层次化效率优化”
- 任务 A 的后续扩展不再只是“继续换 backbone”，而是明确增加“精度-速度-显存折中”的证据链

### 当前状态判断

- 当前代码仍保持原状，本次更新仅发生在实验设计文档层
- 任务 A 的论文逻辑已从“图是否优于点”扩展为“单尺度图模型与层次化图模型谁更适合 4 万点时空快照”
- 下一轮如果开始改代码，应优先围绕层次化数据表示、图结构和解码策略建立最小可运行骨架

---

## 2026-03-10 第 5 次记录

### 本次主要修改

- 新增任务 A 周度检查表，供后续逐周勾选实验、记录结论和阻塞项
- 将任务 A 的建议排期拆成里程碑和每周门槛
- 在实验记录填写规范中补充周度检查表入口

### 对应文档

- `docs/任务A周度检查表.md`
- `docs/实验记录填写规范.md`

### 当前状态判断

- 正式数值结果仍应写入实验记录表
- 周度检查表负责推进节奏、阶段目标和每周复盘
- 后续可以直接基于这份检查表和我逐周讨论实验结论与下一步决策

---

## 2026-03-09 第 1 次记录

### 本次主要修改

- 补齐任务 A 的实验矩阵生成与批量执行脚手架
- 新增 baseline / input ablation / geometry ablation / augment ablation 配置生成器
- 增强训练产物记录，新增 `run_manifest.json`
- 新增训练续写记录文档

### 对应代码

- `training/plan.py`
- `training/make_field_plan.py`
- `training/run_field_plan.py`
- `training/train_field.py`
- `training/trainer.py`

### 推进到实验步骤

对应 [实验设计总纲](实验设计总纲.md) 第 13 节：

- Step 2：固定任务 A 的训练框架，已基本完成
- Step 3：跑通任务 A 的最小基线，代码准备完成，待服务器实跑
- Step 4：完成任务 A 必做消融，已完成配置层准备，待服务器实跑

### 当前状态判断

- 训练主线框架已具备可复现的 baseline / ablation 入口
- 仍未正式进入 physics loss 和任务 B 指标恢复

---

## 2026-03-09 第 2 次记录

### 本次主要修改

- 为任务 A 增加了可插拔 physics loss 接口
- physics loss 目前支持：
  - data loss
  - continuity residual
  - momentum residual
  - no-slip boundary loss
- 训练日志与实验索引增加 physics 相关字段
- 新增任务 B 指标计算骨架与导出入口
- `predict_field.py` 导出内容扩充到可直接支撑任务 B 后处理
- 新增 physics 示例配置模板

### 对应代码

- `training/losses.py`
- `training/trainer.py`
- `training/train_field.py`
- `training/eval_field.py`
- `training/predict_field.py`
- `training/hemo.py`
- `training/export_hemo.py`
- `training/configs/field/ablations/transformer_physics_full.json`

### 推进到实验步骤

对应 [实验设计总纲](实验设计总纲.md) 第 13 节：

- Step 4：任务 A 必做消融中，物理约束消融已补到代码接口层
- Step 5：建立任务 B 后处理与验证链，已完成第一版骨架

### 当前状态判断

- 任务 A 已从“纯数据监督训练脚手架”推进到“可切换 physics 约束训练”
- 任务 B 已有统一输入输出、点级/病例级导出接口
- 当前 `WSS` 仍是 proxy 实现，不是最终论文版精确公式
- 区域划分规则、精确壁面法向和最终风险特征定义仍待补充

### 下一步建议

1. 在服务器上跑 `transformer_physics_full.json`，确认 physics loss 数值稳定。
2. 用 `predict_field.py` 导出主模型预测，再用 `export_hemo.py` 产出任务 B 第一版结果表。
3. 下一轮优先补：
   - 精确 WSS 实现所需的壁面法向/近壁梯度接口
   - 区域划分规则接入
   - CFD vs AI 指标一致性比较脚本

---

## 2026-03-09 第 3 次记录

### 本次主要修改

- 为 `training/` 核心代码补充了更详细的中文注释
- 注释重点覆盖：
  - 配置结构与实验字段含义
  - 数据集与特征 mask 逻辑
  - 模型设计取舍
  - physics loss 插件式接口
  - trainer 的 loss 分项记录逻辑
  - 任务 B 指标骨架与导出数据流

### 对应代码

- `training/config.py`
- `training/data.py`
- `training/models.py`
- `training/losses.py`
- `training/metrics.py`
- `training/io.py`
- `training/plan.py`
- `training/make_field_plan.py`
- `training/run_field_plan.py`
- `training/train_field.py`
- `training/eval_field.py`
- `training/predict_field.py`
- `training/trainer.py`
- `training/hemo.py`
- `training/export_hemo.py`
- `training/splits.py`
- `training/make_split.py`
- `training/utils.py`

### 推进到实验步骤

- 实验步骤本身没有前移
- 这次主要提升的是后续修改损失函数、实验配置和任务 B 代码时的可维护性

### 当前状态判断

- 当前 `training/` 已经具备“边看注释边继续改”的可读性基础
- 后续如果你要自己改 physics loss 或任务 B 指标实现，主要入口已经都带了结构说明
