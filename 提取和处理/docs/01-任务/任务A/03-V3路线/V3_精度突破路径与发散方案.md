# V3 精度突破路径与发散方案

> **索引**：[README](./README.md) · [后续待办](./V3_后续优化待办.md) · [代码诊断 §10–§11](./V3_代码问题诊断与修复计划.md) · [实验跟踪](./V3_实验执行跟踪日志.md)  
> **维护**：2026-05-23 初版；路径优先级调整时同步更新本文 + [V3_后续优化待办.md](./V3_后续优化待办.md)  
> **性质**：**规划层文档**——汇总瓶颈判读、优化路径、发散想法与推荐执行顺序；**不含代码改动**，实施前须单独立项并标注 split/归一化口径。

**口径提醒**：`V3P`（`split_AG_v1`）与 `V3D`（`split_data_new_v3_v3`）**禁止混表**；pre-fix（4771–4774 / 4664）与 post-4901 **禁止混表**。

---

## 1. 当前事实切片（2026-05-24）

### 1.1 V3P（AG-only，train ≈60）

| 配方 | seed | best_wss → `wss_r2_wss` | `wss_z` | `wss_x` | `wss_y` | `r2_p` | 作业 |
| --- | ---: | ---: | ---: | ---: | ---: | ---: | --- |
| Main-01-PW（基线） | 1 | 0.365 | 0.348 | 0.022 | 0.044 | 0.935 | 3634 |
| **AsymW-a** | 1 | **0.399** | 0.387 | 0.018 | 0.007 | 0.943 | 4957 |
| **AsymW-a** | 2 | **0.389** | 0.376 | 0.008 | 0.010 | 0.948 | 4999 |
| **AsymW-a** | 3 | **0.395** | 0.365 | 0.015 | 0.005 | 0.954 | 5000 |
| **AsymW-a 汇总** | 1–3 | **0.394±0.005** | 0.376 | ≈0 | ≈0 | 0.948 | — |
| WssDO-a | 1 | 0.379 | 0.366 | 0.030 | 0.037 | 0.943 | 4958 |
| AsymW-WssDO-a | 1 | **0.398** | 0.378 | 0.033 | 0.012 | 0.943 | 5001 |
| Probe-WSS-01（仅 WSS） | 1 | ~0.364 / 历史 ~0.397 | — | — | — | — | 3645 |

- **AsymW 三 seed 完成**（4957/4999/5000）；**建议升级 PW 母版**；AsymW+WssDO **0.398** ≈ 纯 AsymW。
- **AsymW +0.029（均值）全部来自 z 分量**；test `wss_x/y` 仍 ≈0。

### 1.2 V3D（三域，train 179）

| 域 | Probe-P `r2_p` | Probe-WSS `wss_r2_wss` |
| ---: | ---: | ---: |
| AAA（13） | 0.974 | 0.251 |
| AG（18） | 0.991 | **0.267** |
| ILO（22） | 0.982 | 0.229 |
| **全局** | **0.984** | **0.243** |

- 数据 60→179，WSS **0.397→0.243**（V3P 探针 vs V3D 探针）；P 已 **Gate-P ✅**（4901 + train-only 归一化修复生效）。
- ILO 分域 WSS 最低（0.229），AG 最高（0.267），无单域崩盘。

### 1.3 已排除的「假瓶颈」

| 曾怀疑项 | 结论 |
| --- | --- |
| val_score / 早停 / checkpoint 语义 | ✅ 已修复；best_epoch 11→104，但 WSS 带宽未因此抬升 |
| 单点 OOD（PENG） | ✅ 已剔除；P 从 ~0.05→~0.96 |
| 三域 normalize 非 train-only | ✅ 4901 修复；Probe-P 从 −4.45→0.984 |
| WSS 权重过低 | ❌ 不成立：WSS 项占 val loss ~93%，梯度已主导 |
| 模型容量不足 | ❌ 反向：wss_x/y 加深/加宽会加剧过拟合 |

---

## 2. 瓶颈根因（严谨判读）

### 2.1 主因：全局坐标系下 wss_x/y 缺乏跨 case 共性

- **wss_z（轴向）**：与主流速度梯度、几何大尺度相关 → train/val 有可学信号。
- **wss_x/y（横向）**：二次流（Dean 涡、分叉诱导）主导 → 对局部几何与入口微扰极度敏感；60–179 case 不足以在**全局 xyz** 下学到泛化规律。
- **证据**：Main-PW history 中 train `wss_x/y` R² 升至 0.24–0.30，val 降至 **−0.17 / −0.07**（记忆而非泛化）；AsymW 缓解 val 过拟合但 test x/y 仍 ≈0。

### 2.2 次因：AsymW 的增益本质是「只学 z」，非表达力突破

- 权重 `[1, 0.05, 0.05, 0.90]` 实质放弃 x/y → 标量 WSS R² 由 z 主导抬升。
- **结论**：在**不改 WSS 表示**的前提下，继续微调权重/正则，**信噪比已接近极限**（~0.36–0.40 带宽）。

### 2.3 V3D 特有：跨域分布异构 + 单一全局 z-score

- AAA / AG / ILO 壁面 `p` / `wss` 物理分布差异大；单一 `wss.std≈3.14` 对三域不平等。
- 归一化修复主要救 **P**，**未**抬升 WSS；数据量增大反而引入域偏移。

### 2.4 结构与信号缺口

- 当前 backbone：**kNN mean/max pool**，无显式**壁面法向梯度**信息；WSS 物理定义即 μ·(∂v/∂n)|_wall。
- 节点特征缺：`dist_to_wall`（连续值）、壁面法向 **n̂**、分叉距离等；`Tangent_*` 已有但未用于 **WSS 目标** 对齐。

### 2.5 评估与选优噪声

- val 仅 5–8（V3P）/ 25（V3D）例 → `best_wss_model` 选取方差大（4957 ep24 vs 4958 ep57）。
- 主报仅点级 R²；缺病例级 Pearson / 高 WSS top-k Dice（路线 §7.1 / §8.5 已规划未落地）。

---

## 3. 路径地图（按 ROI 排序）

```text
                    ┌─────────────────────────────────────┐
                    │  当前带宽：V3P ~0.40 / V3D ~0.24     │
                    └─────────────────┬───────────────────┘
                                      │
        ┌─────────────────────────────┼─────────────────────────────┐
        │                             │                             │
   路径 A（P0）                   路径 B（P1）                   路径 C（P1–P2）
   WSS 表示改造                   物理信号喂入                   数据与口径
   TODO-5,9,10,11                TODO-12,17,18                 TODO-1,3,4,19
        │                             │                             │
        └─────────────────────────────┼─────────────────────────────┘
                                      │
                              路径 D（P1，并行）
                              评估与选优
                              TODO-8,20
                                      │
                              路径 E（暂缓 / 发散储备）
                              TODO-7,13–16,21–26
```

| 路径 | 核心命题 | 预期 WSS 收益（估计） | 成本 | 对应 TODO |
| --- | --- | --- | --- | --- |
| **A** | 改变 WSS 监督对象（局部坐标 / magnitude） | **+0.05–0.15**（V3P） | 中（pipeline + 配置） | 5, 9, 10, 11 |
| **B** | 显式近壁几何与梯度上下文 | **+0.03–0.08**（与 A 相乘） | 中–高 | 12, 17, 18 |
| **C** | 三域归一化 + 数据量 + val 可靠性 | V3D 从 0.24→**0.30+**；长期 150+ case | 高 | 1, 3, 4, 19 |
| **D** | 病例级 / 区域级判读 + val_score | 不直接抬 R²，但决定「增益是否真实」 | 低–中 | 8, 20 |
| **E** | 架构 / MTL / PINN / 发散探索 | 不确定；A/B 未验证前 ROI 低 | 高 | 7, 13–16, 21–26 |

---

## 4. 路径 A：WSS 表示改造（最高优先级）

> **判断**：冲破 0.40 的**必要条件**是 (i) 局部坐标系 或 (ii) magnitude-only + 一致性；继续在全局 4 维上扫权重收益递减。

### A1 · TODO-5：WSS 局部坐标系（**推荐下一项 P0 实验**）

**问题**：血管走向不同 → 「轴向 WSS」落在 x/y/z 任意分量 → 网络需隐式学坐标系。

**方案**（预处理 + 训练开关，**不破坏**现有 `y_wss` 全局字段）：

1. 新增 pipeline 步骤（或 `extract_features` 扩展）：
   - 输入：壁面点坐标、已有 `Tangent_X/Y/Z`、壁面法向 **n̂**（CFD 或几何估计）。
   - 输出：`wss_axial = WSS·t̂`，`wss_circ` / `wss_rad`（Frenet-Serret 或 n̂×t̂ 投影）。
2. 图资产：新增 `y_wss_local`（或 CSV 列 + convert 写入）；配置 `data.wss_target_frame: "global" | "local"`。
3. `wss_head` 维度：local 模式下 3 维 + 可选 magnitude 第 4 维（由 sqrt 推导，见 A2）。
4. 归一化：local 分量 **单独** 在 train 壁面节点上统计 mean/std，写入 `normalization_params_global.json` 扩展段。

**实验设计**：

| Exp ID | 目的 | 对照 |
| --- | --- | --- |
| `V3P-Probe-WSS-Local-01` | 单目标上限（仅 WSS，local frame） | Probe-WSS-01 ~0.364 |
| `V3P-Main-01-PW-Local` | PW 主线 + local | Main-PW / AsymW-a |

**Go 判据（seed=1）**：`wss_axial` test R² **> 0.50** 或标量 `wss_r2_wss` **> 0.45** → 补 seed2/3，升 PW 母版。

**风险**：法向估计噪声、分叉处 t̂ 不连续 → 需在 Diag 输出 local 分量 train/test 分位 diff。

### A2 · TODO-9 / TODO-10：magnitude 与 4 维冗余消解

- **现状**：4 维 `[mag, x, y, z]` 冗余；AsymW 已「半 magnitude-only」。
- **方案 A2a（TODO-10）**：`wss_head` 仅 1 维 magnitude + loss 仅监督 `wss` 标量（x/y/z 由 post-hoc 不报告或置 0）。
- **方案 A2b（TODO-9，V3P-WSS-04）**：监督 3 局部分量 + `|pred_mag − sqrt(Σ pred_i²)|` 一致性项。
- **触发**：A1 验证后或与 A1 **组合**单变量对照；禁止与 global AsymW **混表**。

### A3 · TODO-11：WSS-only / 压力弱化（条件对照）

- **目的**：验证 P+WSS 是否仍有多任务轻微冲突（当前 P 0.94+，冲突可能已弱）。
- **触发**：A1 后若 PW-Local 仍低于 Probe-WSS-Local → 试 `lambda_p_*` 降档或 WSS-only 短训。
- **非主线**：论文仍需 P+WSS 双 readout。

---

## 5. 路径 B：物理信号与特征增强

### B1 · TODO-12：近壁几何特征

| 特征 | 来源 | 预期作用 |
| --- | --- | --- |
| `dist_to_wall` | 已有 KDTree 距离，作**连续节点特征** | 近壁梯度尺度 |
| 壁面 **n̂** | surface mesh / 数值估计 | WSS 方向与 local frame |
| `dist_to_bifurcation` | 中心线拓扑 | 横向 WSS 热点 |
| `branch_id` / one-hot | 预处理 | 域内结构差异 |

- **实验**：`V3P-Main-Local+Geom`（在 A1 基础上 **单变量** + dwall/bifurcation）。
- **约束**：与 A1 分表；勿同时改 frame + 特征 + loss。

### B2 · TODO-17（新）：近壁速度差分 / 梯度上下文（发散）

- **动机**：`lambda_vel_int=0` 后 backbone 无显式速度梯度；WSS ∝ ∂v/∂n。
- **方案**：对每个壁面点，聚合 k 近邻内部点（2mm 内）的速度差分或相对 n̂ 的法向分量，**拼接进 wss_head 输入**（不改 field_head）。
- **对照**：V3P-Main-Local vs Local+VelGrad；算力与实现成本高于 B1。

### B3 · TODO-18（新）：多半径 / 多 k 邻域聚合（轻量 Arch）

- 在 **不** 上完整层次化 PointNeXt 前提下，对 `edge_index` 做 2–3 档 k 或半径的并行 pool + concat（类似 V3P-Arch-02 的缩小版）。
- **触发**：A1+B1 后仍 <0.45 再试；与 TODO-14 完整 Arch 分开命名。

---

## 6. 路径 C：数据、归一化与 split

### C1 · TODO-1 / TODO-4：数据扩充（保持 P0）

- 候选池 257 → train 150+；与 denylist / Fluent 补跑衔接。
- **前提**：V3D 须先做 **C2**，否则重演「179 case WSS 更低」。

### C2 · TODO-19（新）：三域 WSS/压力归一化口径对照

| 方案 | 说明 | 实验 |
| --- | --- | --- |
| **C2a** | per-domain z-score（AAA/AG/ILO 各自 mean/std） | V3D-Probe-WSS-DomainNorm |
| **C2b** | robust scaling（median/IQR） | 同上 ablation |
| **C2c** | 评估时反物理量纲再算 R²（训练仍 z-score） | 改 eval 脚本，不重训 |

- **判据**：若 C2a 使 ILO `wss_r2_wss` 接近 AG 且全局 **+0.03** → 纳入 V3D 主线口径（**新 split 标签**，禁止与 post-4901 全局 z-score 混表）。

### C3 · TODO-3：扩大 val（10–12 例）

- 新 split 文件 → **全量重跑** AsymW / Local 等关键实验。
- 与 C2 可合并一次 split 修订，减少重复 renorm。

### C4 · ILO Tier 1 剔例（TODO-1 子项）

- 分域 WSS 0.229；视病例级 eval（TODO-8）是否识别系统性坏例再扩 denylist。

---

## 7. 路径 D：评估与 checkpoint 选优

### D1 · TODO-8：病例级 + 高 WSS 区域（路线 P1-A/B）

- `eval_wss_clinical_metrics.py`：病例 mean/p95 WSS Pearson（**Pa** 量纲）。
- 扩展 `_eval_wss_metrics_once.py`：top-k overlap / Dice。
- **意义**：点级 0.40→0.45 可能无临床差异；病例级 ρ 才是论文可宣称指标。

### D2 · TODO-20（新）：val_score / 早停增强

- EMA 平滑 val WSS 项；或多 checkpoint 集成（ep20–40 区间平均，对应 history 峰值区）。
- 与 AsymW 的 `val_score_wss_weights` 对齐 local frame 权重。

### D2 · TODO-2：重跑 WSS-01-*-PW

- V3P：修复后代码下确认最优 `lambda_wss`（历史 WSS-01-a 0.395）。
- V3D：**Gate-P ✅ 后**可排 `V3D-WSS-01-*-PW`；建议 **C2 口径确定后**再跑，避免双重重训。

---

## 8. 路径 E：暂缓、不推荐与发散储备

### 8.1 暂缓（已有实验支撑）

| 项 | 理由 |
| --- | --- |
| TODO-7 WssDO 单变量 | 0.379 vs AsymW 0.394 mean；5001 组合 **0.398** ≈ AsymW → **关闭** |
| TODO-14 完整层次化 PointNeXt | wss_x/y 过拟合；A/B 未验证前无归因价值 |
| TODO-15 GradNorm / PCGrad | 非多任务冲突主导（P 已 0.94+） |
| TODO-16 PINN | 179 case 远不足；需 C1 150+ 与 local frame 后再议 |
| 加深/加宽 backbone | 代码诊断 §11.1：**负收益** |

### 8.2 发散想法（未立项，供讨论）

| ID | 想法 | 简述 | 风险 |
| --- | --- | --- | --- |
| **TODO-21** | 测试时自适应（TTA） | 对 test 点云小扰动 ensemble | 算力；可能过拟合 test |
| **TODO-22** | 辅助任务：预测 \|WSS\| 分位数 / 高 WSS 二分类 | 仅 wall 点 binary high-WSS head | 需平衡主 loss |
| **TODO-23** | 跨域对比学习（V3D） | 同几何相似病例拉近 embedding | 实现复杂；需域标签 |
| **TODO-24** | 按域单独 WSS head（Mixture-of-Experts） | AAA/AG/ILO 各一小 head | 参数量；test 域已知时可试 |
| **TODO-25** | 时间步序列：同一病例多 t 作序列 | Mamba/1D conv over t | 路线外；命名 V3T-* |
| **TODO-26** | 物理启发 loss：\|WSS\| 与近壁 \|∇v\| 软一致 | 无 CFD 梯度时用有限差分近似 | 噪声大；第二阶段 |

---

## 9. 推荐执行顺序（讨论用草案）

### 阶段 0 · 在跑实验收尾 ✅

1. ~~汇总 **4999/5000/5001**~~ → AsymW 三 seed **0.394±0.005**；5001 vs 4957 判 TODO-7 **关闭**。
2. 4957/4958/4999/5001 补 **predict + 图件**。

### 阶段 1 · 并行启动（最高 ROI）

| 顺序 | 任务 | 产出 | 依赖 |
| ---: | --- | --- | --- |
| 1 | **TODO-8** 评估脚本 | 病例级 / top-k 基线（AsymW 4957） | 无 |
| 2 | **TODO-5** local frame pipeline + Probe | `V3P-Probe-WSS-Local-01` seed1 | 法向/切向数据确认 |
| 3 | **TODO-19** 设计文档 + V3D 小样本试算 | 是否 per-domain renorm | 4901 资产 |

### 阶段 2 · 依阶段 1 Go/No-Go

| 若… | 则… |
| --- | --- |
| Local Probe **wss_axial R²>0.5** | `V3P-Main-PW-Local` + seed2/3；AsymW 降为主对照 |
| Local Probe **无明显增益** | 优先 B1 dwall + B2 梯度特征；复查法向质量 |
| TODO-8 点级升但病例级 ρ 不升 | 论文叙事改强调区域/病例指标；加强 A2 magnitude |
| C2a per-domain **V3D +0.03** | 新口径 renorm → V3D-WSS-01-PW + Main |

### 阶段 3 · 中长期

- C1/C3/C4：150+ case + 新 val split + ILO 剔例。
- B3 / TODO-14：仅当 A+B+C 后 V3P 仍 **<0.45** 且病例级无改善。

---

## 10. 收敛性提醒（写进路线共识）

1. **0.399 是「全局坐标 + 只学 z」的上限信号**，不是结构突破；三 seed 钉死后仍须走路径 A。
2. **V3D 0.24 不是 AG 能力退化的主因**，而是跨域归一化与表示问题；勿用「加数据」 alone 解决。
3. **任何 split / 归一化 / local frame 变更** → 新时间戳 + 新表；禁止与 Main-PW 0.365 / AsymW 0.399 / Probe 0.243 混表。
4. **论文 Go** 须同时看点级 + 病例级 + 高 WSS 区域（路线 §7.1）；仅点级 R² 不足以宣称 surrogate 成立。

---

## 11. 文档与 TODO 映射

| 本文路径 | [V3_后续优化待办](./V3_后续优化待办.md) ID |
| --- | --- |
| A | TODO-5, 9, 10, 11 |
| B | TODO-12, 17, 18 |
| C | TODO-1, 3, 4, 19 |
| D | TODO-2, 8, 20 |
| E | TODO-7, 13–16, 21–26 |

变更历史：

| 日期 | 内容 |
| --- | --- |
| 2026-05-23 | 初版：瓶颈判读、路径 A–E、执行顺序、发散 TODO-17–26 |
