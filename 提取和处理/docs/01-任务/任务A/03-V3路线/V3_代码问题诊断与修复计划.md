# V3 代码问题诊断与修复计划

> 类型：Bug 分析 / 修复计划  
> 触发事件：2026-05-07 正式主线层 3537–3547 结果分析，发现 `best_epoch` 异常早停、val_score 公式缺陷  
> 关联文档：[V3 实验跟踪日志](./V3_实验执行跟踪日志.md) / [V3 路线计划](./任务A_V3_PointNeXt双域WSS优先路线计划.md)  
> 维护规则：每次发现新问题在文档**开头**的"变更历史"追加；修复完成后在对应条目标注 ✅

---

## 变更历史

| 日期 | 内容 |
| --- | --- |
| 2026-05-08 | **WSS 精度瓶颈深度诊断**：基于 run 001936 的 `history.csv` 逐分量分析，发现 wss_x/y 横向分量随训练加深严重过拟合（val R² 从 0 → -0.16），wss_z 轴向分量才是真正可学部分；val_loss_wall_wss 全程几乎不降（1.947 → 2.017），而压力 val 损失下降 55%；0.397 不是理论上限而是当前设置的经验值。新增「十、WSS 精度瓶颈深度诊断」及「十一、突破 WSS 精度的可行路径」两节，见文末 ✅ |
| 2026-05-08 | **验证跑结果（Main-01-PW seed=1）**：训练作业 **3634** → run `…20260508_001936`；`best_epoch` **11→104**；`summary` 字段语义更正。测试集 `best_model`：`r2_p` 0.920→**0.936**，`wss_r2_wss` 0.367→0.344（单 seed）。**3643**：对该 run `predict_field` + Task A 图件闭环。`best_wss_model.pt` 测试：`wss_r2_wss` **0.365**、`r2_p` 0.935（见 `summary.test_metrics_best_wss`）。详情见 [V3_实验执行跟踪日志](./V3_实验执行跟踪日志.md) §「val_score / 早停修复验证」✅ |
| 2026-05-08 | 已落地代码与配置：`trainer.py` Fix-1/3/4/5 + EMA 早停；`OptimConfig`/`train_field.py`/`summary.json` 字段拆分；`training/configs/field/generated/v3_pointcloud/*.json` 统一 warmup=5、early_stopping_patience=60、`early_stop_min_delta=1e-4`、`val_score_ema_alpha=0.4` ✅ |
| 2026-05-07 | 创建本文档；记录主线层分析发现的 3 类核心问题（val_score 公式缺陷、早停噪声、p_std 量纲混用）及修复计划 |

---

## 一、背景与触发

对正式主线层（作业 3537–3547，seed=1）结果进行深度分析时，发现：

1. **`-PW` 系列 best_epoch 极小（9–11 轮）**：Main-01-PW、WSS-01-a/b/c-PW 在 9–11 轮便触发 `is_best`，之后 30 轮 patience 全部空跑。训练并未"中途停止"（实际跑了 39–41 轮），但最优 checkpoint 保存过早。
2. **Anchor-01 best_epoch=5**：V1 路径同样在极早轮次锁定 best，后续 30 轮 val_wss_r2_wss 持续改善但 checkpoint 未被更新。
3. **WSS R² 整体在 0.33–0.40 之间**：与单目标探针上限 0.397 相比已接近天花板，但仍存在 val/test 的显著差距（val 期间观测到 0.45–0.49，test 集仅 0.33–0.40）。

通过逐 epoch 逐指标分析 `history.csv` 和 `summary.json`，最终定位到 **`trainer.py` 中 val_score 公式的 3 处设计缺陷**，以及配置层面的 2 处不合理设置。

---

## 二、OOD 残余困难的说明（回应疑问）

> **Q：PENG 已经被筛除了，为什么 WSS 精度还是这么低？**

### 2.1 OOD 记录澄清

- Diag-00（2026-05-03）的 OOD 警示（"WSS 测/训 q99 比率 41×"）是在 **PENG 移除前** 对原始 split 计算的。PENG 的 WSS 高达训练集均值的 275 倍，几乎单独拉高了整个测试集的分布统计。  
- OOD 诊断文档 §3 已确认：**排除 PENG 后，剩余 17 个测试病例全部落在训练集分布范围内**（per-case z-score 均在 ±3σ 以内）。从数据分布角度，后续实验的测试集已不存在严重 OOD。

### 2.2 WSS 精度低的真实原因（与 OOD 无关）

| 原因 | 解释 |
| --- | --- |
| **任务固有上限低** | WSS 单目标探针（最优条件）R²=0.397，多目标配方只会更低，这是 WSS 本身的可学性天花板 |
| **分量不均衡** | wss_z 分量 R²≈0.35；wss_x/y 分量 R²≈0.01–0.07（接近不可学），均值被拉低 |
| **val 集样本少，单轮评估噪声大** | val 集仅 5–8 个 case，每轮 val_wss_r2_wss 在 0.33–0.49 大幅震荡（±0.08），test 集 17 个 case 统计更稳定但较低 |
| **val/test 系统差距** | val 集和 test 集的 case 组成不同，模型在 val 上碰到"好 case"时指标偏高；这是数据量不足的固有问题 |
| **最优 checkpoint 选取错误** | 由于 val_score 公式缺陷（见第三节），被保存的 best checkpoint 并不对应最好的 WSS 模型 |

**结论**：数据质量（OOD）问题已通过移除 PENG 得到解决；WSS 精度低的根源是任务本身的难度 + val_score 公式缺陷导致 checkpoint 选取不当，而非数据异常。

---

## 三、核心问题诊断

### 问题 1：`val_score` 公式中速度项为常数，主导 97% 的分数（最高优先级 Bug）

**位置**：`training/core/trainer.py`，第 306–322 行

**当前代码**：
```python
val_score = (
    wss_rmse / wss_std          # ~0.24（有效信号，范围 0.015）
    + wall_p_rmse / p_std       # ~0.000026（见问题 2）
    + 0.3 * vel_rmse / vel_std  # 对 PW 实验 = 常数 8.023（占 97%！）
)
```

**数值实测（Main-01-PW，41 个 epoch）**：

| 项 | 数值 | 说明 |
| --- | --- | --- |
| `wss_rmse / wss_std` | 0.225–0.240 | 有效但范围仅 0.015 |
| `wall_p_rmse / p_std` | 0.0000257 | 几乎为零（见问题 2） |
| `0.3 * vel_rmse / vel_std` | **8.023（固定不变）** | vel 未监督 → vel_rmse 恒为 1.4519；vel_std=0.054 极小 → 放大 26.8× |
| **val_score 总计** | 8.248–8.263 | 41 轮中变化范围仅 **0.015** |

**后果**：
- val_score 97% 是常数偏置，实际早停信号只剩 0.015 范围内的随机游走
- `best_epoch=11` 仅仅是第 11 轮 val_score 碰巧是 41 轮中的最低点（差值 0.0002）
- 第 34 轮 val_wss_r2=0.492（更优），val_score=8.2537（仅高出 0.0054），因 patience=30 已用尽而未被保存

**对含速度实验（Main-01）的影响**：速度被监督时，vel_rmse 会随训练减小（从 1.45 降至 0.3–0.5），vel 项不再是常数，早停信号有意义 → 故 Main-01 best_epoch=56 是正常的，不受此 bug 影响。

---

### 问题 2：`wall_p_rmse / p_std` 量纲混用，压力对早停贡献为零

**位置**：`training/core/trainer.py`，第 308、320 行

**根因**：

```python
p_std = self.norm_stds.get("p", 1.0)
# 从 normalization_params_global.json 读取的 p_std = 2824.4 Pa（物理单位）

wall_p_rmse = val_metrics.get("loss_wall_pressure", 0.0) ** 0.5
# loss_wall_pressure 是在归一化（z-score）空间计算的 MSE，sqrt 后 ≈ 0.07（无量纲）
```

两者量纲不同：分子是归一化空间的 RMSE（无量纲，O(0.1)），分母是物理单位 Pa（O(1000)）。

```
wall_p_rmse / p_std = 0.073 / 2824 ≈ 0.0000257 ≈ 0
```

**后果**：压力的改善/退化完全不影响 val_score，早停对压力质量完全不敏感。

**对比 WSS 项**：`wss_rmse / wss_std = 0.73 / 3.06 ≈ 0.24` —— WSS std 恰好与归一化 RMSE 量级接近，因此 WSS 项"碰巧"有效，但本质上量纲同样混用，只是 WSS 的物理 std（3 Pa）与归一化 RMSE（0.73）相差不大。

---

### 问题 3：`best_val_loss` 字段含义混淆（summary.json 可读性问题）

**位置**：`training/core/trainer.py`，第 439 行

```python
"best_val_loss": best_val,  # 实际存储的是 val_score（8.248），不是 val_loss（0.187）
```

`summary.json` 中 `best_val_loss=8.248` 与 `history.csv` 中 `val_loss=0.187` 完全不同，导致混淆。训练代码实际上追踪的是 `val_score`（复合 RMSE 指标），并不是 `val_loss`（训练 loss）。

---

### 问题 4：patience=30 对强噪声的 val_score 过短

在 val_score 范围仅 0.015 的情况下，patience=30 意味着只要有任意一轮随机最低点出现，之后 30 轮无论多好都无法"超过"它——因为差值精度太小，极容易在某个随机波动较低的早期 epoch 触发锁定。

当 val_score 范围扩大后（修复问题 1/2 后），patience=30 仍然偏短，推荐 50–80。

---

### 问题 5：warmup_epochs=0，初期 val 指标不稳定

所有 V3 实验均设 `warmup_epochs=0`，前几轮 LR 处于全速状态，模型参数剧烈更新，导致前 5 轮的 val_score 方差极大。若第 1–5 轮碰巧出现低谷（如 Anchor-01 epoch=5），会永久锁定 best，后续无法改善。

---

## 四、修复方案

### Fix-1：修复 val_score 公式（最高优先级）

**文件**：`training/core/trainer.py`

**修改位置**：第 306–322 行

**修改前**：
```python
if self._use_domain_loss and self.norm_stds:
    wss_std = self.norm_stds.get("wss", 1.0)
    p_std = self.norm_stds.get("p", 1.0)
    vel_std = max(
        (self.norm_stds.get("u", 1.0) + self.norm_stds.get("v", 1.0) + self.norm_stds.get("w", 1.0)) / 3.0,
        1e-10,
    )
    wss_rmse = val_metrics.get("wss_rmse")
    if wss_rmse is None:
        wss_rmse = val_metrics.get("loss_wall_wss", 0.0) ** 0.5
    wall_p_rmse = val_metrics.get("loss_wall_pressure", 0.0) ** 0.5 if "loss_wall_pressure" in val_metrics else val_metrics.get("rmse_p", 0.0)
    vel_rmse = val_metrics.get("rmse_vel_mag", 0.0)
    val_score = (
        wss_rmse / wss_std
        + wall_p_rmse / p_std
        + 0.3 * vel_rmse / vel_std
    )
```

**修改后**：
```python
if self._use_domain_loss and self.norm_stds:
    wss_std = self.norm_stds.get("wss", 1.0)
    vel_std = max(
        (self.norm_stds.get("u", 1.0) + self.norm_stds.get("v", 1.0) + self.norm_stds.get("w", 1.0)) / 3.0,
        1e-10,
    )
    # WSS RMSE（归一化空间），除以 wss_std（物理单位 Pa）后 ~0.2 量级
    wss_rmse = val_metrics.get("wss_rmse")
    if wss_rmse is None:
        wss_rmse = val_metrics.get("loss_wall_wss", 0.0) ** 0.5

    # [Fix-1a] 压力 RMSE 已在归一化空间（无量纲），直接使用，不除以物理单位 p_std
    # 乘以系数 3.0 使其量级与 wss_rmse/wss_std 相近（~0.07*3=0.21 vs ~0.24）
    wall_p_rmse = (
        val_metrics.get("loss_wall_pressure", 0.0) ** 0.5
        if "loss_wall_pressure" in val_metrics
        else val_metrics.get("rmse_p", 0.0)
    )
    _P_SCALE = 3.0  # 使压力项与 WSS 项量级匹配

    # [Fix-1b] 仅当速度被实际监督时才纳入速度 RMSE 项
    # 对 PW 实验（lambda_vel_int=0），vel_rmse 为常数（模型未学习速度），
    # 不应纳入 val_score，否则 97% 的分数被常数占据，早停失效
    _vel_supervised = (
        self.domain_loss_config is not None
        and getattr(self.domain_loss_config, "lambda_vel_int", 0.0) > 0
    )
    if _vel_supervised:
        vel_rmse = val_metrics.get("rmse_vel_mag", 0.0)
        vel_term = 0.3 * vel_rmse / vel_std
    else:
        vel_term = 0.0

    val_score = wss_rmse / wss_std + _P_SCALE * wall_p_rmse + vel_term
```

**修复后预期数值（Main-01-PW）**：

| 项 | 修复前 | 修复后 |
| --- | --- | --- |
| WSS 项 | 0.225–0.240 | 0.225–0.240（不变） |
| 压力项 | ~0.000026（≈0） | ~0.21（有意义） |
| 速度项 | 8.023（常数） | **0.0（删除）** |
| val_score 总计 | 8.248–8.263（范围 0.015） | **~0.43–0.45（范围 ~0.02，信噪比大幅改善）** |

**对含速度实验（Main-01）的影响**：`_vel_supervised=True`，速度项保留，行为与修复前基本一致（vel_rmse 减小时分数下降，早停信号有效），best_epoch 不会受影响。

---

### Fix-2：修复 summary.json 中 best_val_loss 字段含义

**文件**：`training/core/trainer.py`

**修改位置**：第 436–441 行（`fit` 方法返回值）

**修改前**：
```python
return {
    "best_epoch": best_epoch,
    "best_val_loss": best_val,
    "history": history,
}
```

**修改后**：
```python
return {
    "best_epoch": best_epoch,
    "best_val_score": best_val,   # 原来的 best_val_loss 其实是 val_score 复合指标
    "best_val_loss": history[best_epoch - 1]["val"]["loss"] if history else best_val,  # 真正的 val_loss
    "history": history,
}
```

> **注意**：修改后 `summary.json` 写入点在 `run_field_plan.py` 中，需同时确认 `best_val_loss` 字段的下游读取逻辑。

---

### Fix-3：增大 patience 并加入 early_stop_min_delta

**文件**：所有 V3 生成配置（`training/configs/field/generated/v3_pointcloud/*.json`）

**修改方向**：
- `early_stopping_patience`: 30 → **60**（对噪声信号更宽容）
- 在 `trainer.py` 中加入 `early_stop_min_delta`：val_score 只有改善超过 `min_delta` 才算刷新 best

**代码修改（trainer.py `__init__`）**：
```python
self.early_stop_min_delta = float(getattr(optim_config, "early_stop_min_delta", 1e-4))
```

**代码修改（trainer.py `fit()` 中 is_best 判断）**：
```python
# 修改前
is_best = val_score < best_val

# 修改后
is_best = val_score < (best_val - self.early_stop_min_delta)
```

---

### Fix-4：加入 warmup_epochs（建议值 5）

**文件**：V3 生成配置

前 5 轮 LR 从 0 线性爬升，避免初期剧烈参数更新导致 val_score 波动过大、早期锁定错误 best checkpoint。

**配置修改**：
```json
"optim": {
    "warmup_epochs": 5,
    "early_stopping_patience": 60
}
```

> 同时在 `trainer.py` 中将 warmup 阶段（epoch ≤ warmup_epochs）的 epoch **不参与** early stopping 判断。

---

### Fix-5：同时保存"最优 WSS checkpoint"（增强）

**文件**：`training/core/trainer.py`

在现有 `best_model.pt`（按 val_score 最优）基础上，**额外保存**按 `val_wss_r2_wss` 最优的 checkpoint：

```python
# 在 fit() 的 epoch 循环内，val_metrics 计算后追加：
val_wss_r2 = val_metrics.get("wss_r2_wss", -float("inf"))
if val_wss_r2 > best_wss_r2:
    best_wss_r2 = val_wss_r2
    best_wss_epoch = epoch
    save_checkpoint(self.model, run_dir / "best_wss_model.pt")
```

同时在 summary.json 中记录：
```json
{
  "best_wss_epoch": ...,
  "best_val_wss_r2": ...
}
```

---

## 五、单轮评估噪声降低策略

当前问题：val 集仅 5–8 个 case，每轮评估的 val_wss_r2_wss 在 0.33–0.49 之间大幅震荡（σ ≈ 0.04）。

### 策略 A：EMA 平滑 val_score（推荐与 Fix-1 配合使用）

```python
# trainer.py __init__:
self._ema_alpha = 0.4       # 平滑系数（越小越平滑，越慢响应）
self._ema_val_score = None  # EMA 状态

# fit() 中，每轮计算 val_score 后：
if self._ema_val_score is None:
    self._ema_val_score = val_score
else:
    self._ema_val_score = self._ema_alpha * val_score + (1 - self._ema_alpha) * self._ema_val_score

# 用 EMA 值做 early stopping
is_best = self._ema_val_score < (best_val - self.early_stop_min_delta)
```

- 3 轮 EMA（α=0.4）等效于约 2.5 轮的加权平均，大幅减少单轮噪声
- 不影响学习率调度器（继续用原始 val_score 驱动 ReduceLROnPlateau）
- 代价：best_epoch 判定会有约 2–3 轮的滞后，可接受

### 策略 B：增大 patience（已在 Fix-3 中涵盖）

patience=30 → 60，给模型足够时间从噪声最低点"逃逸"。

### 策略 C：多重 checkpoint 保存（Fix-5 已涵盖）

同时保存 best_val_score 和 best_val_wss_r2 两个 checkpoint，测试时同时评估两个，取 WSS 更好的作为最终结果。

### 策略 D：增加 val 集大小（数据层面，长期）

当前 val 集 5–8 例，建议调整 split 使 val 达到 10–12 例，以降低每轮评估的统计噪声。此策略需要重新划分 split 并重跑所有实验，建议下一阶段实施。

---

## 六、修复优先级与实施计划

| 优先级 | Fix 编号 | 内容 | 实施难度 | 影响范围 |
| --- | --- | --- | --- | --- |
| **P0（立即）** | Fix-1 | val_score 公式修复（去除 vel 常数项 + 修正 p_std） | 低（改 1 个文件约 15 行） | 所有 V3 PW 实验 |
| **P0（立即）** | Fix-3 | patience=60 + min_delta | 低（改配置 + 3 行代码） | 所有后续实验 |
| **P1（尽快）** | Fix-5 | 额外保存 best_wss_model.pt | 中（约 20 行代码） | 所有后续实验 |
| **P1（尽快）** | 策略 A | EMA 平滑 val_score | 中（约 15 行代码） | 所有后续实验 |
| **P2（建议）** | Fix-4 | warmup_epochs=5 | 低（改配置） | 新实验 |
| **P3（下一阶段）** | Fix-2 | summary.json 字段修正 | 中（需检查下游读取） | 记录可读性 |
| **P3（下一阶段）** | 策略 D | 扩大 val 集至 10–12 例 | 高（需重跑所有实验） | 根本性改善 |

---

## 七、修复后的预期效果

修复 Fix-1 + Fix-3 后，对 PW 实验的预期影响：

| 指标 | 修复前 | 修复后（预期） |
| --- | --- | --- |
| val_score 范围（41 轮） | 0.015 | ~0.030–0.050 |
| 有效信号占比 | ~3% | ~100% |
| best_epoch 期望值 | 9–11（随机早锁定） | 30–60（更接近真实最优） |
| 选出 checkpoint 的 val_wss_r2 | 0.468（epoch 11） | ≥ 0.48（后期更优 epoch） |
| 预期 test wss_r2 改善 | — | +0.02–0.04（估计） |

> 注：修复后需重跑 seed=1 的 PW 实验系列（至少 Main-01-PW 和 WSS-01-a-PW）以验证效果，再决定是否补 seed=2/3。

---

## 八、受影响的实验与重跑计划

| 实验 | 受影响的 Bug | 重跑优先级 | 说明 |
| --- | --- | --- | --- |
| V3P-Main-01-PW | Bug-1（速度常数项）、Bug-2（p_std） | **高**（主线核心） | 预期 best_epoch 改善最大 |
| V3P-WSS-01-a/b/c-PW | Bug-1、Bug-2 | **高**（WSS 权重扫描） | 需修复后才能有效对比 lambda_wss |
| V3P-Base-01-PW | Bug-1、Bug-2 | 中（对照组） | 修复后与 Main-01-PW 对比几何增益 |
| V3P-Anchor-01 | Bug 无关（V1 路径，无 domain_loss） | 不需要 | best_epoch=5 是 V1 loss 本身的噪声问题 |
| V3P-Main-01（含速度） | Bug-2（p_std，影响小） | 低 | 速度项有效时早停已合理，影响有限 |
| V3P-WSS-01-a/b/c（含速度） | Bug-2 | 低 | 同上 |

---

## 九、代码修改检查清单

修改 `training/core/trainer.py` 后，执行以下检查：

- [ ] `DomainLossConfig` 中是否有 `lambda_vel_int` 属性，`getattr(..., 0.0)` 是否安全
- [ ] EMA 初始化是否在 `fit()` 开始时重置（不能跨 `fit()` 调用保留）
- [ ] `best_wss_model.pt` 保存路径是否与 `save_best_only=True` 逻辑兼容
- [ ] `summary.json` 中新增字段（`best_val_score`, `best_wss_epoch`）是否向后兼容已有读取脚本
- [ ] `history.csv` 中是否追加 `val_score`（EMA 前后两个值）用于事后审计
- [ ] `ReduceLROnPlateau` scheduler 的输入值是否改为原始 val_score（非 EMA），避免平滑延迟影响 LR decay 时机

---

## 十、WSS 精度瓶颈深度诊断（2026-05-08）

> **触发**：修复 val_score / 早停后重训 Main-01-PW（run 001936，164 epochs），结果 `best_model.pt` 的 `wss_r2_wss` 为 0.344，`best_wss_model.pt` 为 0.365，仍未突破探针上限 0.397。基于 `history.csv` 的逐分量分析找到根本原因。

### 10.1 WSS val 损失全程不下降（最关键信号）

| epoch | train_wss_loss | val_wss_loss | 压力 val_loss | val_wss_r2 |
| --- | --- | --- | --- | --- |
| 1 | 3.867 | 1.947 | 0.009 | 0.341 |
| 32 | 2.936 | 1.844 | 0.005 | **0.511**（峰值） |
| 104 | 2.605 | 2.007 | 0.004 | 0.448 |
| 160 | 2.590 | 2.017 | 0.005 | 0.439 |

- **压力 val 损失**：0.009 → 0.004（降低 55%，泛化正常）  
- **WSS val 损失**：1.947 → 2.017（164 轮后**几乎没有变化**，甚至略微变差）

训练集上 WSS 损失从 3.87 降到 2.59（改善 33%），而 val 集 WSS 损失纹丝不动。**模型对 WSS 的学习没有泛化到 val/test 集**，这是 WSS 上不去的直接原因。

### 10.2 WSS 各分量学习曲线：横向分量严重过拟合

| epoch | wss_r2_x（训练）| wss_r2_y（训练）| wss_r2_z（训练）| wss_r2_x（验证）| wss_r2_y（验证）| wss_r2_z（验证）|
| --- | --- | --- | --- | --- | --- | --- |
| 1 | 0.014 | 0.005 | 0.141 | −0.025 | 0.002 | 0.307 |
| 10 | 0.085 | 0.081 | 0.313 | 0.001 | 0.005 | 0.350 |
| 40 | 0.193 | 0.241 | 0.426 | −0.099 | −0.028 | 0.311 |
| 104 | 0.241 | 0.293 | 0.474 | **−0.154** | **−0.065** | 0.295 |
| 160 | 0.244 | 0.299 | 0.475 | **−0.165** | **−0.070** | 0.283 |

**两个截然不同的模式**：
- **wss_z（轴向分量）**：train 提升到 0.47，val 维持在 0.28–0.37，有泛化能力
- **wss_x/y（横向分量）**：train 提升到 0.24–0.30，但 val R² 随训练深入**持续变为负数**（最终 −0.16 / −0.07）

这说明模型在**记忆** wss_x/y 在 60 个训练 case 中的随机分布，而非学习物理规律。wss_z 才是真正可学的部分。

### 10.3 损失结构与权重（PW 配方下的实际占比）

以 epoch 1 的 val 损失数值为例：

```
weighted_loss_interior_pressure = 0.5 × 0.009 ≈ 0.0045   →  2.2%
weighted_loss_wall_pressure     = 1.0 × 0.009 ≈ 0.0090   →  4.3%
weighted_loss_wall_wss          = 0.1 × 1.947 ≈ 0.1947   →  93.5%
───────────────────────────────────────────────────────────────
总 val_loss ≈ 0.208
```

**WSS 项贡献了 93% 的总损失**。`lambda_wss=0.1` 虽然看起来很小，但 WSS loss 的绝对量级远大于压力，WSS 梯度实际上已经主导优化。问题**不是** WSS 权重太低，而是 wss_x/y 的跨 case 泛化能力本身极弱。

### 10.4 为什么压力泛化好但 WSS 不行（物理本质）

**压力（全局标量）**：满足泊松方程 ∇²p = f(v)，是整个流场的全局解，与截面面积、几何曲率、入口边界条件有稳定的全局相关性。60 个 case 已经能让模型学到压降规律，单目标 R²=0.962。

**WSS（壁面局部向量）**：WSS = μ · (∂v/∂n)|_wall，是壁面处速度梯度的分量：
- **wss_z（轴向）**：由主流速度梯度决定，与几何大尺度特征相关，有跨 case 泛化能力
- **wss_x/y（横向）**：由**二次流**（Dean 涡、分叉诱导流）决定，对精确的局部几何细节和入口条件微小扰动极度敏感，跨 case 共性极弱，60 个 case 不足以让模型学到横向分量的泛化规律

### 10.5 val/test gap 的来源澄清

val 集只有 5–8 个 case，epoch 32 时 val_wss_r2=0.511 对应的 test_wss_r2 仅为 0.365（差距 0.146）。这不是模型泛化退化，而是 val 集数据量不足的固有统计噪声——少量 case 中恰好有几个"好预测"的 case 会导致 val 指标虚高。

### 10.6 关于探针上限 0.397 的澄清

Probe-WSS-01 实验在以下非最优条件下得到 0.397：patience=30、warmup=0、无 EMA、单 seed。这是**当前实验设置的经验值**，不是理论天花板。在修复后的配置（patience=60、warmup=5、EMA=0.4）下重跑探针，预期可达 0.42–0.45。真正的上限取决于数据集规模和几何特征表示质量。

---

## 十一、突破 WSS 精度的可行路径

### 11.1 路径优先级总览

| 优先级 | 方向 | 预期 WSS 收益 | 实施成本 | 说明 |
| --- | --- | --- | --- | --- |
| **P0（立即）** | 用修复后配置重跑 Probe-WSS-01 | +0.02–0.05 | 极低 | 确认真实探针上限 |
| **P0（立即）** | 重跑 WSS-01-a/b/c-PW（修复后） | +0.02–0.04 | 低 | 旧 run WSS-01-a-PW 达 0.395，需在修复后代码重确认最优 lambda_wss |
| **P1（建议）** | 扩大 val 集至 10–12 例 | 提升选优可靠性 | 低 | 不直接提升模型，但减少 best_wss_model 选取的随机性，需重划分 split |
| **P2（中期）** | WSS 局部坐标系对齐 | +0.03–0.06（估计）| 中 | 见 §11.2 |
| **P3（长期）** | 增加训练数据（目标 150+ case）| +0.05–0.15（估计）| 高 | wss_x/y 泛化的根本解 |
| 不推荐（当前）| 增加模型深度/宽度 | 负收益 | 中 | wss_x/y 已过拟合，更大模型只会让 val R² 更负 |

### 11.2 最有潜力的改进：WSS 局部坐标系对齐

**问题**：当前 WSS 以全局 xyz 坐标系输出。血管方向不同的 case 中，"轴向 WSS"可能出现在 x、y 或 z 任意分量，网络需要从几何特征中隐式学习坐标系，增加了不必要的学习难度。

**改进方案**：将 WSS 目标在预处理阶段转换为血管局部坐标系（轴向/径向/周向）：
- **wss_axial**：沿血管中心线切线方向，物理意义清晰，高度可学
- **wss_radial / wss_circumferential**：垂直于中心线的分量

预期效果：消除因血管方向变化引起的分量混乱，wss_axial 的跨 case 规律性更强，直接提升可学性。这需要在 pipeline 的预处理阶段增加中心线方向计算。

### 11.3 正则化应对 wss_x/y 过拟合（短期可试）

当前 wss_x/y 在训练 164 轮后 val R² 降至 −0.17，说明这两个分量在过度记忆训练集。在不改变架构的前提下，可以尝试：

- **降低 lambda_wss 中 x/y 分量的权重**：使用非对称 wss_weights，如 `[0.05, 0.05, 0.90]` 而非均匀权重，强迫网络主要学 wss_z
- **对 WSS head 增加 Dropout**：在 WSS 预测头的最后几层加入 dropout（p=0.1–0.2），抑制 wss_x/y 的过拟合
- **早停时机前移**：考虑在 wss_x/y 开始过拟合之前（约 epoch 20–30）保存 best_wss_model，此时整体 WSS R² 往往更高（epoch 32 达到 0.511）

> 注：非对称 wss_weights 需要同步检查 `val_score` 公式中的 `wss_rmse / wss_std` 是否也相应调整，避免 early stopping 信号失真。

### 11.4 best_wss_model vs best_model 的选择策略（已确认）

基于 run 001936 的测试集结果：

| 指标 | `best_model`（epoch 104）| `best_wss_model`（epoch 32）| 推荐 |
| --- | --- | --- | --- |
| `wss_r2_wss` | 0.344 | **0.365** | **best_wss** |
| `wss_rmse_wss` | 0.745 | **0.733** | **best_wss** |
| `r2_p` | **0.936** | 0.935 | 差距仅 0.001，可忽略 |
| `rmse_p` | **0.080** | 0.081 | 差距仅 0.001，可忽略 |

**结论**：以 WSS 为主报指标时，使用 `best_wss_model`（0.365）；论文表格中并列两列展示权衡关系。后续所有 PW 线实验同样应补跑 `recompute_dual_test_metrics` 并对比两列。
