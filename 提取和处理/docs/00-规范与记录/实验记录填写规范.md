# 实验记录填写规范

> 上位文档：[实验设计总纲](../实验设计总纲.md) | 相关文档：[任务A](../01-任务/任务A/任务A实验清单.md) / [任务B](../01-任务/任务B/任务B指标计算规范.md) / [任务C](../01-任务/任务C/任务C风险建模规范.md)

## 1. 文档定位

本规范用于统一填写 [实验记录表.xlsx](实验记录表.xlsx)（若工作簿尚未纳入仓库，可暂以 §6.1.2 清单 + `outputs/field/experiment_index.csv` 为入账对照）。

如果你还需要按周推进和复盘任务 A，请同步使用：

- [任务A周度检查表](../01-任务/任务A/任务A周度检查表.md)

目标是保证：

- 实验可追溯
- 结果可比较
- 图表可回溯
- 后续写论文时不用重新翻日志

同时，任务 A 现在新增了两份配套文档：

- [任务A冻结卡](../01-任务/任务A/任务A冻结卡.md)
- [任务A实验状态表](../01-任务/任务A/任务A实验状态表.md)

三者分工固定如下：

- `任务A冻结卡`：记录正式实验阶段不可中途变更的固定口径
- `任务A实验状态表`：记录任务 A 每个实验当前做到哪一步
- `实验记录表.xlsx`：记录最终结构化实验条目，便于汇总、统计和论文制表

---

## 2. 工作簿结构说明

工作簿包含以下 sheet：

1. `README`
2. `experiment_master`
3. `split_registry`
4. `taskA_field`（含 **速度分量与 \(\|\mathbf v\|\)** 的 **测试集 R²** 及 **分区域速度 R²** 列，见 §5.2）
5. `taskB_hemo`
6. `taskC_risk`
7. `figures_tables`

---

## 3. 填写顺序

推荐严格按下面顺序填写。

## 3.1 第零步：先确认冻结卡与状态表

正式填写工作簿前，先确认：

- `任务A冻结卡` 中的 `data_version`、`data_root`、`preprocess_version`、`seed_plan` 已冻结
- `任务A实验状态表` 中需要启动的 `Exp ID` 已明确

如果 `split_file` 还没有真实生成，则只允许预填实验草稿，不要伪造患者级 split 明细。

## 3.2 第一步：填写 `split_registry`

记录本次实验所用的患者级划分。

### 必填字段

- `split_version`
- `patient_id`
- `subset`
- `label`

### 作用

这是所有实验的根。没有 split，就不要填后面的结果。

## 3.3 第二步：填写 `experiment_master`

记录每个实验的总信息。

### 必填字段

- `exp_id`
- `task`
- `status`
- `goal`
- `hypothesis`
- `data_version`
- `split_version`
- `seed`
- `model`
- `feature_set`

### 作用

用于统一追踪所有实验，不论任务 A/B/C。

## 3.4 第三步：填写任务细表

按任务分别填写：

- 场重建写 `taskA_field`
- 指标恢复写 `taskB_hemo`
- 风险建模写 `taskC_risk`

## 3.5 第四步：填写 `figures_tables`

只要某个实验产出图表，就要登记。

---

## 4. 字段填写原则

## 4.1 `exp_id`

必须唯一。若工作簿需要长期维护，推荐把“实验族”和“具体运行实例”分开：

- 文档中的实验族 ID：如 `A-Main-01`
- 工作簿中的单次运行条目：建议写成 `A-Main-01_seed1`

也就是说，`experiment_master` 和 `taskA_field` 推荐按“`exp_id + seed` 一行”填写，不要把多个 seed 合并在同一行。

如果你希望 `exp_id` 保持与状态表完全一致，也可以：

- `exp_id` 写 `A-Main-01`
- `seed` 单独写 `1`

但仍然必须保证一个 seed 一行。

另一种可选格式是：

`[task]_[model]_[feature]_[split]_[seed]`

示例：

- `field_transformer_geom_splitv1_seed1`
- `hemo_mainmodel_splitv1_seed1`
- `risk_xgb_clinicalGeomAI_splitv1_seed2`

## 4.2 `split_version`

必须和 `split_registry` 中一致。

不允许写模糊值：

- 错误：`latest`
- 正确：`split_v1`

## 4.3 `feature_set`

必须写清楚输入来源，不能只写“full”。

推荐写法：

- `coords+t+BC`
- `coords+t+BC+geometry+is_wall`
- `Clinical+Geometry+AI-hemodynamics`

## 4.4 `notes`

用于记录异常情况，例如：

- 数据缺失
- 中断重跑
- 某折样本不足
- 模型训练不稳定

---

## 5. 各 sheet 重点字段说明

## 5.1 `experiment_master`

### 必填重点

- `goal`
  - 本实验要回答什么问题
- `hypothesis`
  - 对应哪条研究假设
- `primary_metric`
  - 该实验的主指标
- `primary_value`
  - 对应主指标数值

### 任务 A 预填建议

如果实验尚未正式跑完，可以先填写以下字段：

- `status`: `planned` 或 `running`
- `task`: `taskA`
- `data_version`: 来自冻结卡，如 `AG_v1`
- `split_version`: 来自冻结卡，如 `split_AG_v1`
- `code_version`: 当前 git commit short hash
- `primary_metric`: **论文叙事仍以 `RMSE_|v|`（或 `interior.rmse_vel_mag`）为主结论**；若实验设计以“速度可解释方差”为主，可改为 `R2_vel_mag` / `R2_u` 等，并在 `notes` 写明对照哪一列
- `secondary_metric`: 可填 `R2_vel_mag`、`RMSE_p` 等，与 `secondary_value` 对齐即可
- `output_path`: 先写预期输出目录

以下字段在正式结果出来后再补：

- `date_finished`
- `best_epoch`
- `runtime_hours`
- `primary_value`
- `secondary_value`
- `checkpoint_path`

### 示例

- `goal`: 验证显式几何是否提升患者级场重建
- `hypothesis`: H1
- `primary_metric`: RMSE_p
- `primary_value`: 0.184

## 5.2 `taskA_field`

### 必填重点

- `model`
- `backbone`
- `feature_set`
- `geometry`
- `BC`
- `is_wall`
- `physics_loss`
- `RMSE_u`
- `RMSE_v`
- `RMSE_w`
- `RMSE_p`
- `R2_p`

### 5.2.1 速度分量与速度标量的 R²（测试集）

除压力 `R2_p` 外，`taskA_field` 增加与 **`summary.json` → `test_metrics`** 对齐的 **测试集** R²（与同行的 `RMSE_u/v/w`、`RMSE_|v|` 同一归一化 / 评估口径）：

| 列名 | 来源字段 | 含义 |
| --- | --- | --- |
| `R2_u` | `test_metrics.r2_u` | 流向速度分量 u 的决定系数 |
| `R2_v` | `test_metrics.r2_v` | 流向速度分量 v |
| `R2_w` | `test_metrics.r2_w` | 流向速度分量 w |
| `R2_vel_mag` | `test_metrics.r2_vel_mag` | 速度标量 **\(\|\mathbf v\|\)** 的一元 R²（非 \(u,v,w\) 的简单算术组合） |

**填写注意：**

- 若 `summary.json` 中缺少某字段（旧 run），对应单元格 **留空**。
- 若训练日志显示该 seed 未完整跑完评估，**不得手填推测值**。

### 5.2.2 分区域速度 R²（与 `regional_eval` 一致）

在 **`predictions_test/regional_eval/fig_A5_regional_metrics.json`** 中，各区域块会给出 `r2_u`、`r2_v`、`r2_w`；自 **2026-03-29** 起实现还会写入 **`r2_vel_mag`**（见 [任务A分区域评估口径](任务A分区域评估口径.md) §5）。

工作簿中为与已有 `inner_RMSE`、`wall_RMSE` 等命名一致，采用以下列名（前缀为区域，后缀为指标）：

**区域 `all`（全图节点）**

- `all_R2_u`，`all_R2_v`，`all_R2_w`，`all_R2_vel_mag`

**区域 `interior`（内部点，`is_wall ≤ 0.5`）**

- `inner_R2_u`，`inner_R2_v`，`inner_R2_w`，`inner_R2_vel_mag`

**区域 `wall`（壁面点）**

- `wall_R2_u`，`wall_R2_v`，`wall_R2_w`，`wall_R2_vel_mag`

**区域 `high_curvature`（曲率 P75 以上）**

- `high_curvature_R2_u`，`high_curvature_R2_v`，`high_curvature_R2_w`，`high_curvature_R2_vel_mag`

**区域 `near_wall`（内部且 `NormRadius > 0.8`）**

- `near_wall_R2_u`，`near_wall_R2_v`，`near_wall_R2_w`，`near_wall_R2_vel_mag`

**必须留空的情况（不要为了“填满表格”而写数）：**

1. JSON **不存在**该 key（例如旧版 `fig_A5` 未写入 `r2_vel_mag`）→ **整格留空**。
2. 数值 **非有限**（`NaN` / `Inf`）→ 留空。
3. **`r2_u/v/w` 因方差过小或掩膜退化出现天文级数值**（例如 \(|R^2|\gg 10^3\)）→ **留空**，并在 `notes` 可注明“壁面分量 R² 数值病态，已省略”。
4. **合理范围内的负 R²**（如 \(-1 \lesssim R^2 < 0\)）在技术仍可供对照 → **可填**，表示该区域预测不优于“取均值”基线。
5. 未跑 `regional_eval`、未生成 `fig_A5_regional_metrics.json` → 上述分区域 R² **全部留空**。

区域定义、near_wall 与 wall 的区别等，以 [任务A分区域评估口径](任务A分区域评估口径.md) 为准。

### 5.2.3 RMSE 类分区域列（与原版一致）

若表中填写 `wall_RMSE`、`inner_RMSE`、`high_curvature_RMSE` 等与分区域相关的列，**默认区域 key 与区间**与 `training/analysis/regional_eval.py` 一致；上述 **`inner_R2_*` 与 `inner_RMSE` 同属 `interior` 区域**，便于同一行横向对照。

### 使用原则

一个实验一行，不要把多个 seed 混在一行。

如果结果尚未生成，可以先预填结构性字段，以下指标列留空：

- `train_loss`
- `val_loss`
- `RMSE_u`
- `RMSE_v`
- `RMSE_w`
- `RMSE_|v|`
- `RMSE_p`
- `MAE_p`
- `R2_p`
- `R2_u`，`R2_v`，`R2_w`，`R2_vel_mag`
- `all_R2_*`，`inner_R2_*`，`wall_R2_*`，`high_curvature_R2_*`，`near_wall_R2_*`
- `wall_RMSE`
- `inner_RMSE`
- `high_curvature_RMSE`
- `inference_time_sec`
- `speedup_vs_CFD`
- `best_epoch`

推荐在 `notes` 中明确写：

- `等待数据处理完成后 smoke test`
- `seed=1 预填草稿`
- `结果待回填`

## 5.3 `taskB_hemo`

### 必填重点

- `source_model`
- `metric_level`
- `WSS_RMSE`
- `TAWSS_R2`
- `OSI_R2`
- `RRT_R2`
- `pearson_case`

### 使用原则

优先记录病例级与区域级结果，不要只记录局部截图结论。

## 5.4 `taskC_risk`

### 必填重点

- `prediction_target`
- `feature_group`
- `model`
- `AUROC`
- `AUPRC`
- `Brier`

### 使用原则

每个特征组和模型组合单独一行。

## 5.5 `figures_tables`

### 必填重点

- `item_id`
- `type`
- `task`
- `title`
- `source_exp_id`
- `file_path`

### 作用

后面写论文时可以快速知道哪张图来自哪个实验。

---

## 6. 推荐填写示例

## 6.1 一个任务 A 实验

- `exp_id`: `field_transformer_geom_splitv1_seed1`
- `task`: `field`
- `goal`: `验证显式几何先验是否提升场重建`
- `feature_set`: `coords+t+BC+geometry+is_wall`
- `primary_metric`: `RMSE_p`
- `primary_value`: `0.184`

## 6.1.1 任务 A 首批基线建议映射

结合当前 [任务A实验状态表](../01-任务/任务A/任务A实验状态表.md)，可以先在工作簿中预填以下 4 条 seed=1 草稿：

- `A-Base-01_seed1`
  - `model/backbone`: `MLP`
  - `feature_set`: `coords+t+BC`
  - `geometry`: `no`
  - `BC`: `yes`
  - `is_wall`: `no`
  - `physics_loss`: `no`
- `A-Base-02_seed1`
  - `model/backbone`: `GraphSAGE`
  - `feature_set`: `coords+t+BC+is_wall`
  - `geometry`: `no`
  - `BC`: `yes`
  - `is_wall`: `yes`
  - `physics_loss`: `no`
- `A-Base-03_seed1`
  - `model/backbone`: `Transformer`
  - `feature_set`: `coords+t+BC+is_wall`
  - `geometry`: `no`
  - `BC`: `yes`
  - `is_wall`: `yes`
  - `physics_loss`: `no`
- `A-Main-01_seed1`
  - `model/backbone`: `Transformer`
  - `feature_set`: `coords+t+BC+geometry+is_wall`
  - `geometry`: `yes`
  - `BC`: `yes`
  - `is_wall`: `yes`
  - `physics_loss`: `no`

这些条目当前属于“预填实验草稿”，不是最终完成记录。

## 6.1.2 已入账任务 A 场重建实验与 `taskA_field` 行映射（对齐 `experiment_index.csv`）

> **数据来源**：仓库 `outputs/field/experiment_index.csv`（训练结束由脚手架追加；列为粗粒度汇总）。  
> **填写原则**：`experiment_master` 与 `taskA_field` **每个 `exp_id` × 每个 seed 单独一行**；指标以各 run 目录下 **`summary.json`**（全图 `test_metrics`）与 **`predictions_test/regional_eval/fig_A5_regional_metrics.json`**（分区域）为准，与 §5.2 列名对齐。  
> **叙事母版**：后续新开跑以 **`A-Opt-05`** 为控制变量起点；**`A-Opt-07`** 为已完成**负结果**实验，亦须入账便于论文制表。  
> **详细文字结论**：[任务A实验状态表](../01-任务/任务A/任务A实验状态表.md)「第三批 / 实验记录摘要」。

下列 **41 条 run** 已在当前索引中出现（`split_AG_v1`，任务 `field`）：

| `exp_id` | `study_group` | seeds | `taskA_field` 中建议写明的结构差异 |
| --- | --- | --- | --- |
| `A-Base-01` | baseline | 1–3 | `MLP`；无图；`coords+t+BC`；无 `is_wall`/geometry |
| `A-Base-02` | baseline | 1–3 | `GraphSAGE`；`coords+t+BC+is_wall` |
| `A-Base-03` | baseline | 1–3 | `Transformer`；`coords+t+BC+is_wall`；无 geometry |
| `A-Main-01` | baseline | 1–3 | `Transformer`；`coords+t+BC+geometry+is_wall`；**无 Pre-Norm** |
| `A-Opt-01` | optimization | 1–3 | 同 Main + **`target_weights=[2,2,2,0.5]`**；无 Pre-Norm |
| `A-Opt-02` | optimization | 1–3 | 同 Main + **`FieldTransformer` Pre-Norm**；默认权重 `1,1,1,1` |
| `A-Opt-02_warmup` | optimization | 1–3 | 同 `A-Opt-02` + **`optim.warmup_epochs=5`** |
| `A-Opt-03` | optimization | 1–3 | **`A-Opt-01` + `A-Opt-02` 组合**（tw22205 + Pre-Norm）；`h128`、`L3` |
| `A-Opt-03w` | optimization | 1–3 | `A-Opt-03` + **`warmup_epochs=5`** |
| `A-Opt-04` | optimization | 1–3 | `A-Opt-03` + **`hidden_dim=256`**（仍为 `L3`）；容量线① |
| `A-Opt-05` | optimization | 1–3 | `A-Opt-04` + **`num_layers=4`**；**当前消融/Line G/W 母版** |
| `A-Opt-05t_warmup10` | optimization | 1,2 | `A-Opt-05` + **`warmup_epochs=10`**（与 CSV `exp_id` 一致） |
| `A-Opt-05t_warmup10_lr3e-4` | optimization | 1 | 在 warmup10 骨架上 **`lr=3e-4`** |
| `A-Opt-05t_warmup10_wd2e-4` | optimization | 1 | 在 warmup10 骨架上 **`weight_decay=2e-4`**（明显变差） |
| `A-Opt-05t_warmup10_schedpat15` | optimization | 1 | 在 warmup10 骨架上 **`scheduler_patience=15`** |
| `A-Opt-07` | optimization | 1–3 | 同 **`A-Opt-05`** + **`optim.interior_loss_boost=3`**（训练损失加权；**不改变**分区域 mask 定义）；**相对 05 主指标未改善** |

**`primary_metric` / `primary_value` 建议**：论文主叙事以 **`interior.rmse_vel_mag`**（或表内 `inner_RMSE`）为主列，**`test_metrics.rmse_vel_mag`**（全图）作对照；`A-Opt-07` 等负结果在 `notes` 写明「相对 `A-Opt-05` 未提升」。

**`output_path` / `checkpoint_path`**：填 `experiment_index.csv` 同列 **`run_dir`**，checkpoint 一般为 `<run_dir>/best_model.pt`。

## 6.1.3 `figures_tables` 建议补充登记（已定稿的优化子课题图）

下列路径相对于仓库根目录，便于与 §5.5 字段 **`file_path`** / **`source_exp_id`** 对齐（`source_exp_id` 写主要对比涉及的 `exp_id`，`notes` 可写「多模型汇总」）。

| `item_id`（示例） | `type` | `source_exp_id`（示例） | `file_path` |
| --- | --- | --- | --- |
| `fig_A5_opt07_vs_05_main_vel` | `png` | `A-Opt-05` / `A-Opt-07` / `A-Main-01` | `outputs/field/plots/optimization/A_Opt07_vs_Opt05_Main01/fig_A5_multimodel_regional_bar_rmse_vel_mag_geo_only.png` |
| `fig_A5_opt07_vs_05_main_p` | `png` | 同上 | `.../fig_A5_multimodel_regional_bar_rmse_p_geo_only.png` |
| `fig_A3_opt07_interior_vel_seed1` | `png` | 同上 | `.../fig_A3_multimodel_scatter_vel_mag_interior_geo_only_seed1.png`（seed2/3 同理） |
| `fig_A4_opt07_interior_box` | `png` | 同上 | `.../fig_A4_multimodel_per_case_boxplot_interior_exp_subset.png` |
| `fig_val_loss_opt07_campaign` | `png` | 同上 | `.../compare_val_loss.png` |

其他已归档子课题（若尚未写入工作簿）：`outputs/field/plots/optimization/prenorm_A_Opt02_vs_Main01/`、`prenorm_Main_P02_P02w/`、`prenorm_A_Opt03_vs_Opt03w/`、`A_Opt05_tune_vs_Opt03_seed1/` 等——见 [任务A论文可视化与指标建议](../01-任务/任务A/任务A论文可视化与指标建议.md)。

## 6.2 一个任务 B 实验

- `exp_id`: `hemo_mainmodel_splitv1_seed1`
- `source_model`: `field_transformer_geom_splitv1_seed1`
- `metric_level`: `case`
- `TAWSS_R2`: `0.81`
- `OSI_R2`: `0.73`

## 6.3 一个任务 C 实验

- `exp_id`: `risk_xgb_clinicalGeomAI_splitv1_seed1`
- `prediction_target`: `ILO`
- `feature_group`: `Clinical+Geometry+AI-hemodynamics`
- `model`: `XGBoost`
- `AUROC`: `0.84`
- `AUPRC`: `0.69`

---

## 7. 常见错误

以下错误必须避免：

1. 不记录 `split_version`
2. 一个实验重复覆盖上一行
3. 多个 seed 的均值直接写进单次实验行
4. 图表文件没有回填 `source_exp_id`
5. `feature_set` 写得过于模糊
6. 结果异常但没有写 `notes`

---

## 8. 推荐使用规则

## 8.1 单次实验完成后立刻记录

不要攒一周后补录，否则最容易丢关键信息。

## 8.2 先记录原始结果，再单独汇总均值

原始 seed 结果必须保留。均值和标准差可以后续单独汇总。

## 8.3 图表一旦定稿立即登记

否则论文写作阶段很容易忘记图片来源。

---

## 9. 建议后续补充

后续如果你开始大规模实验，建议再新增两个 sheet：

1. `seed_summary`
2. `paper_tables_final`

但当前阶段先用现有结构就够了。
